name: Questionnaire upstream adaptation

on:
  push:
    branches:
      - test/questionnaire-visual-polish-5594c1f4

permissions:
  contents: read

jobs:
  hosted-upstream-acquisition:
    if: github.repository == 'decode2/gentle-pi'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout exact revision
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - name: Pull and pin Node 24 acquisition image
        id: image
        shell: bash
        run: |
          set -euo pipefail
          command -v docker >/dev/null
          docker info >/dev/null
          docker pull node:24-bookworm
          image_ref="$(docker image inspect --format '{{index .RepoDigests 0}}' node:24-bookworm)"
          image_id="$(docker image inspect --format '{{.Id}}' node:24-bookworm)"
          case "$image_ref" in node@sha256:*) ;; *) echo "missing immutable image digest" >&2; exit 1;; esac
          test -n "$image_id"
          printf 'image_ref=%s\nimage_id=%s\n' "$image_ref" "$image_id"
          printf 'ref=%s\nid=%s\n' "$image_ref" "$image_id" >> "$GITHUB_OUTPUT"

      - name: Acquire exact upstream and frozen dependencies in networked container
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          test ! -e "$acquisition_dir"
          mkdir "$acquisition_dir"
          chmod 0777 "$acquisition_dir"
          docker run --rm -i --network=bridge --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$acquisition_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin \
            HOME=/artifact/home TMPDIR=/artifact/tmp NPM_CONFIG_CACHE=/artifact/npm-cache \
            NPM_CONFIG_USERCONFIG=/artifact/home/user.npmrc NPM_CONFIG_GLOBALCONFIG=/artifact/home/global.npmrc PNPM_HOME=/artifact/pnpm-home \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /usr/local/bin/node --input-type=module <<'NODE'
          import { execFileSync } from "node:child_process";
          import { mkdir, readFile } from "node:fs/promises";
          import { join } from "node:path";

          const workspace = "/workspace";
          const artifact = "/artifact";
          const source = join(artifact, "upstream");
          const manifest = JSON.parse(await readFile(`${workspace}/.github/workflows/fixtures/hosted-upstream-questionnaire.json`, "utf8"));
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          const limit = 256 * 1024;
          const excerpt = (value) => {
            const text = Buffer.isBuffer(value) ? value.toString("utf8") : typeof value === "string" ? value : "";
            return text.length > 4096 ? `${text.slice(0, 4096)}\n...[truncated]` : text;
          };
          const run = (file, args, options = {}) => {
            try {
              return execFileSync(file, args, { cwd: source, encoding: "utf8", ...options, maxBuffer: limit });
            } catch (error) {
              const status = Number.isInteger(error?.status) ? error.status : "unknown";
              console.error(`command failed file=${file} status=${status} stdout=${JSON.stringify(excerpt(error?.stdout))} stderr=${JSON.stringify(excerpt(error?.stderr))}`);
              throw new Error(`command failed: ${file}`);
            }
          };
          const git = (args, options = {}) => run(
            "git",
            ["-c", "core.hooksPath=/dev/null", "-c", "diff.external=", "-c", "credential.helper=", "-C", source, ...args],
            options,
          );

          fail(manifest.schema === "gentle-pi.hosted-upstream-questionnaire-acquisition/v1", "upstream manifest schema changed");
          fail(manifest.repository === "https://github.com/Gentleman-Programming/gentle-shell.git", "upstream repository changed");
          fail(manifest.commit === "f2d9d073ffc2299eb501902753b42789f7cdc461", "upstream commit changed");
          fail(manifest.tree === "6fb42857f728f131da054e592ec5d22c691ce870", "upstream tree changed");
          fail(!Object.keys(process.env).some((key) => /TOKEN|PASSWORD|SECRET|CREDENTIAL|AWS_|GITHUB_/i.test(key)), "network acquisition received credential-like environment");
          await mkdir(source, { recursive: true });
          for (const directory of ["home", "tmp", "npm-cache", "pnpm-home"]) await mkdir(join(artifact, directory), { recursive: true });
          git(["init", "--initial-branch=main"], { stdio: "ignore" });
          git(["remote", "add", "origin", manifest.repository], { stdio: "ignore" });
          git(["fetch", "--no-tags", "origin", manifest.commit], { stdio: "ignore" });
          git(["checkout", "--detach", manifest.commit], { stdio: "ignore" });
          const head = git(["rev-parse", "HEAD"]).trim();
          const tree = git(["rev-parse", "HEAD^{tree}"]).trim();
          fail(head === manifest.commit, "Git HEAD does not match the pinned upstream commit");
          fail(tree === manifest.tree, "Git tree does not match the pinned upstream tree");
          const sourceBefore = git(["ls-tree", "-r", "-z", "--full-tree", "HEAD"]);
          const records = sourceBefore.split("\0").filter(Boolean);
          fail(records.length === manifest.trackedFileCount, "upstream tracked inventory changed");
          for (const path of manifest.questionnairePaths) fail(records.some((record) => record.endsWith(`\t${path}`)), `missing questionnaire path: ${path}`);

          const bootstrap = join(artifact, "pnpm-bootstrap");
          run("npm", ["install", "--prefix", bootstrap, "--no-save", "--package-lock=false", "--ignore-scripts", "--no-audit", "--no-fund", "pnpm@11.1.1"], { cwd: artifact });
          const pnpm = join(bootstrap, "node_modules/.bin/pnpm");
          fail(run(pnpm, ["--version"]).trim() === "11.1.1", "pnpm bootstrap version changed");
          run(pnpm, ["install", "--frozen-lockfile", "--ignore-scripts", "--ignore-pnpmfile", "--reporter=append-only"], { stdio: "ignore" });
          fail(git(["ls-tree", "-r", "-z", "--full-tree", "HEAD"]) === sourceBefore, "tracked upstream source changed after dependency installation");
          fail(git(["status", "--porcelain=v1", "--untracked-files=all"]) === "", "upstream checkout is not clean after dependency installation");
          fail(git(["rev-parse", "HEAD"]).trim() === head && git(["rev-parse", "HEAD^{tree}"]).trim() === tree, "Git identity moved during dependency installation");
          console.log(`acquisition-proof root=${artifact} repository=${manifest.repository} commit=${head} tree=${tree} tracked-files=${records.length} pnpm=11.1.1 frozen-lockfile=true scripts=false native=false`);
          NODE

      - name: Validate upstream artifact offline and run scoped strict TypeScript
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            ACQUIRED_UPSTREAM_ROOT=/acquired-upstream GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              /usr/local/bin/node --experimental-strip-types --test /workspace/tests/hosted-upstream-questionnaire-acquisition.test.ts
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ES2022 --module NodeNext --moduleResolution NodeNext \
                --allowImportingTsExtensions --typeRoots /acquired-upstream/node_modules/@types --types node \
                /workspace/tests/hosted-upstream-questionnaire-acquisition.test.ts
            '

      - name: Compose derived questionnaire artifact offline and run strict RED contract
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          composition_dir="$RUNNER_TEMP/questionnaire-upstream-composition"
          test ! -e "$composition_dir"
          mkdir "$composition_dir"
          chmod 0777 "$composition_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --mount "type=bind,source=$composition_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            ACQUIRED_UPSTREAM_ROOT=/acquired-upstream COMPOSITION_ARTIFACT_ROOT=/artifact GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ES2022 --module NodeNext --moduleResolution NodeNext \
                --allowImportingTsExtensions --typeRoots /acquired-upstream/node_modules/@types --types node \
                /workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts \
                /workspace/tests/hosted-upstream-questionnaire-composition.test.ts
              /usr/local/bin/node --experimental-strip-types --test /workspace/tests/hosted-upstream-questionnaire-composition.test.ts
            '

      - name: Verify staged derived questionnaire artifact is read-only
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          composition_dir="$RUNNER_TEMP/questionnaire-upstream-composition"
          derived_dir="$composition_dir/zero-patch/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=256m --pids-limit=64 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin \
            /bin/sh -eu -c '
              test -f /derived/package.json
              if touch /derived/.read-only-probe 2>/dev/null; then exit 1; fi
            '

      - name: Compose Herdr compatibility artifact offline
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          herdr_dir="$RUNNER_TEMP/questionnaire-upstream-herdr"
          test ! -e "$herdr_dir"
          mkdir "$herdr_dir"
          chmod 0777 "$herdr_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --mount "type=bind,source=$herdr_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            ACQUIRED_UPSTREAM_ROOT=/acquired-upstream HERDR_ARTIFACT_ROOT=/artifact GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              /usr/local/bin/node --experimental-strip-types --input-type=module <<'"'"'NODE'"'"'
          import { execFileSync } from "node:child_process";
          import { copyFile, mkdir, readdir, readFile } from "node:fs/promises";
          import { join } from "node:path";
          import { composeUpstreamQuestionnaireArtifact } from "/workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts";

          const workspace = "/workspace";
          const artifact = "/artifact";
          const basePath = `${workspace}/.github/workflows/fixtures/hosted-upstream-questionnaire.json`;
          const manifestPath = `${workspace}/.github/workflows/fixtures/hosted-upstream-questionnaire-herdr.json`;
          const base = JSON.parse(await readFile(basePath, "utf8"));
          const manifest = JSON.parse(await readFile(manifestPath, "utf8"));
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          fail(manifest.schema === "gentle-pi.hosted-upstream-questionnaire-herdr/v1", "Herdr manifest schema changed");
          fail(manifest.baseManifest === ".github/workflows/fixtures/hosted-upstream-questionnaire.json", "Herdr base manifest changed");
          for (const key of ["repository", "commit", "tree"]) fail(manifest.base?.[key] === base[key], `Herdr base ${key} changed`);
          const composition = manifest.composition;
          fail(composition.schema === "gentle-pi.hosted-upstream-questionnaire-composition/v1", "Herdr composition schema changed");
          fail(composition.derivedDirectory === "derived" && composition.patchDirectory === "patches/questionnaire-upstream", "Herdr composition directories changed");
          const expectedFiles = ["001-blocked-event-producer-test.patch", "002-blocked-event-herdr-integration-test.patch", "003-blocked-event-compatibility.patch"];
          const expectedTargets = ["tests/ask-user-question.test.ts", "tests/gentle-ai.test.ts", "extensions/ask-user-question.ts"];
          const expectedAllowed = ["extensions/ask-user-question.ts", "tests/ask-user-question.test.ts", "tests/gentle-ai.test.ts"];
          const expectedProtected = ["extensions/gentle-ai.ts", "lib/native-fullscreen-interaction.ts"];
          fail(JSON.stringify(composition.patchInventory.map((patch) => patch.file)) === JSON.stringify(expectedFiles), "Herdr patch ordering changed");
          fail(JSON.stringify(composition.patchInventory.map((patch) => patch.target)) === JSON.stringify(expectedTargets), "Herdr patch targets changed");
          fail(JSON.stringify(composition.allowedTargets) === JSON.stringify(expectedAllowed), "Herdr allowed targets changed");
          fail(JSON.stringify(composition.protectedTargets) === JSON.stringify(expectedProtected), "Herdr protected targets changed");

          const herdrRoot = join(artifact, "herdr");
          const patchRoot = join(herdrRoot, composition.patchDirectory);
          const outputRoot = join(herdrRoot, composition.derivedDirectory);
          await mkdir(patchRoot, { recursive: true });
          for (const patch of composition.patchInventory) {
            const patchPath = join(patchRoot, patch.file);
            await copyFile(join(workspace, "tests/fixtures/upstream-questionnaire/patches", patch.file), patchPath);
            const changed = execFileSync("git", ["-C", "/acquired-upstream", "apply", "--numstat", "--summary", patchPath], { encoding: "utf8" });
            const changedPaths = changed.split("\n").filter((line) => line.includes("\t")).map((line) => line.slice(line.lastIndexOf("\t") + 1)).filter(Boolean);
            fail(changedPaths.length === 1 && changedPaths[0] === patch.target, `Herdr changed path mismatch: ${patch.file}`);
          }
          const staged = (await readdir(patchRoot)).sort();
          fail(JSON.stringify(staged) === JSON.stringify(expectedFiles), "Herdr staged patch set changed");
          await composeUpstreamQuestionnaireArtifact({ sourceRoot: "/acquired-upstream", patchRoot, outputRoot, manifest: composition });
          console.log(`herdr-composition-proof root=${outputRoot} base=${manifest.base.commit} patches=${expectedFiles.join(",")} targets=${expectedTargets.join(",")}`);
          NODE
            '

      - name: Run Herdr GREEN regression in read-only isolation
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          herdr_dir="$RUNNER_TEMP/questionnaire-upstream-herdr"
          derived_dir="$herdr_dir/herdr/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=2g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /derived "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            NODE_OPTIONS=--max-old-space-size=1536 GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              if touch /derived/.green-read-only-probe 2>/dev/null; then exit 1; fi
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ESNext --module ESNext --moduleResolution bundler \
                --allowImportingTsExtensions --lib ESNext,DOM --typeRoots /acquired-upstream/node_modules/@types --types node \
                /derived/extensions/ask-user-question.ts /derived/tests/ask-user-question.test.ts
              /usr/local/bin/node /derived/scripts/check-types.mjs
              set +e
              output="$(/usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/ask-user-question.test.ts /derived/tests/gentle-ai.test.ts 2>&1)"
              status=$?
              set -e
              printf "%s\\n" "$output" | tail -n 240
              test "$status" -eq 0
              test "$(printf "%s\\n" "$output" | grep -Ec "^ok [0-9]+ - ")" -eq 85
              test "$(printf "%s\\n" "$output" | grep -Ec "^not ok ")" -eq 0
              printf "%s\\n" "$output" | grep -Fx "1..85" >/dev/null
              printf "%s\\n" "$output" | grep -Fx "# tests 92" >/dev/null
              printf "%s\\n" "$output" | grep -Fx "# pass 92" >/dev/null
              printf "%s\\n" "$output" | grep -Fx "# fail 0" >/dev/null
              printf "%s\\n" "$output" | grep -Eq "^ok 85 - actual questionnaire producer drives Herdr through a private balanced compatibility lifecycle$"
              printf "%s\\n" "$output" | grep -Eq "^ok [0-9]+ - ask_user_question keeps the native blocked lifecycle private and balanced$"
            '

      - name: Compose partial-cancellation GREEN artifact offline
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          partial_dir="$RUNNER_TEMP/questionnaire-upstream-partial-cancellation"
          test ! -e "$partial_dir"
          mkdir "$partial_dir"
          chmod 0777 "$partial_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --mount "type=bind,source=$partial_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            ACQUIRED_UPSTREAM_ROOT=/acquired-upstream PARTIAL_ARTIFACT_ROOT=/artifact GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              /usr/local/bin/node --experimental-strip-types --input-type=module <<'"'"'NODE'"'"'
          import { execFileSync } from "node:child_process";
          import { copyFile, mkdir, readdir, readFile } from "node:fs/promises";
          import { join } from "node:path";
          import { composeUpstreamQuestionnaireArtifact } from "/workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts";

          const workspace = "/workspace";
          const artifact = "/artifact/partial";
          const base = JSON.parse(await readFile(`${workspace}/.github/workflows/fixtures/hosted-upstream-questionnaire.json`, "utf8"));
          const manifest = JSON.parse(await readFile(`${workspace}/.github/workflows/fixtures/hosted-upstream-questionnaire-partial-cancellation.json`, "utf8"));
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          const expected = [
            { file: "004-partial-cancellation-tests.patch", target: "tests/ask-user-question.test.ts", preimage: "cc439bdc0dcdfa1f8ade309abc63c1f565c64d40", postimage: "48018904a33e2dc470ecde770829087c8996c5d5" },
            { file: "002-blocked-event-herdr-integration-test.patch", target: "tests/gentle-ai.test.ts", preimage: "81f4a38ecdfd11b97ada81308f036e0d318726fe", postimage: "f379515bae4cdda5d4bd84bafbf761597dd7dfcd" },
            { file: "005-partial-cancellation-producer.patch", target: "extensions/ask-user-question.ts", preimage: "52d6ff35296af67f1f4240ef5fc6df8bfe15dab6", postimage: "4b4db5d2addfc9d4c1ff112b017276d56bcf4a81" },
          ];
          fail(manifest.schema === "gentle-pi.hosted-upstream-questionnaire-partial-cancellation/v1", "partial manifest schema changed");
          fail(manifest.baseManifest === ".github/workflows/fixtures/hosted-upstream-questionnaire.json", "partial base manifest changed");
          for (const key of ["repository", "commit", "tree"]) fail(manifest.base?.[key] === base[key], `partial base ${key} changed`);
          const composition = manifest.composition;
          fail(composition.schema === "gentle-pi.hosted-upstream-questionnaire-composition/v1", "partial composition schema changed");
          fail(composition.derivedDirectory === "derived" && composition.patchDirectory === "patches/questionnaire-upstream", "partial composition directories changed");
          fail(JSON.stringify(composition.patchInventory) === JSON.stringify(expected), "partial patch inventory identity changed");
          fail(JSON.stringify(composition.allowedTargets) === JSON.stringify(["extensions/ask-user-question.ts", "tests/ask-user-question.test.ts", "tests/gentle-ai.test.ts"]), "partial allowed targets changed");
          fail(JSON.stringify(composition.protectedTargets) === JSON.stringify(["extensions/gentle-ai.ts", "lib/native-fullscreen-interaction.ts", "lib/questionnaire/questionnaire-view.ts"]), "partial protected targets changed");

          const patchRoot = join(artifact, composition.patchDirectory);
          const outputRoot = join(artifact, composition.derivedDirectory);
          await mkdir(patchRoot, { recursive: true });
          for (const patch of expected) {
            const patchPath = join(patchRoot, patch.file);
            await copyFile(join(workspace, "tests/fixtures/upstream-questionnaire/patches", patch.file), patchPath);
            const changed = execFileSync("git", ["-C", "/acquired-upstream", "apply", "--numstat", "--summary", patchPath], { encoding: "utf8" });
            const changedPaths = changed.split("\n").filter((line) => line.includes("\t")).map((line) => line.slice(line.lastIndexOf("\t") + 1)).filter(Boolean);
            fail(changedPaths.length === 1 && changedPaths[0] === patch.target, `partial changed path mismatch: ${patch.file}`);
          }
          fail(JSON.stringify((await readdir(patchRoot)).sort()) === JSON.stringify(expected.map((patch) => patch.file).sort()), "partial staged patch set changed");
          await composeUpstreamQuestionnaireArtifact({ sourceRoot: "/acquired-upstream", patchRoot, outputRoot, manifest: composition });
          console.log(`partial-green-composition-proof root=${outputRoot} base=${manifest.base.commit} patches=${expected.map((patch) => patch.file).join(",")} targets=${expected.map((patch) => patch.target).join(",")}`);
          NODE
            '

      - name: Verify staged partial-cancellation artifact is read-only
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          partial_dir="$RUNNER_TEMP/questionnaire-upstream-partial-cancellation"
          derived_dir="$partial_dir/partial/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=256m --pids-limit=64 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin \
            /bin/sh -eu -c '
              test -f /derived/tests/ask-user-question.test.ts
              if touch /derived/.green-read-only-probe 2>/dev/null; then exit 1; fi
            '

      - name: Run partial-cancellation GREEN contract
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          partial_dir="$RUNNER_TEMP/questionnaire-upstream-partial-cancellation"
          derived_dir="$partial_dir/partial/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=2g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /derived "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            NODE_OPTIONS=--max-old-space-size=1536 GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              if touch /derived/.green-test-read-only-probe 2>/dev/null; then exit 1; fi
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ESNext --module ESNext --moduleResolution bundler \
                --allowImportingTsExtensions --lib ESNext,DOM --typeRoots /acquired-upstream/node_modules/@types --types node \
                /derived/extensions/ask-user-question.ts /derived/tests/ask-user-question.test.ts
              /usr/local/bin/node /derived/scripts/check-types.mjs
              set +e
              output="$(/usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/ask-user-question.test.ts /derived/tests/gentle-ai.test.ts 2>&1)"
              status=$?
              set -e
              printf "%s\\n" "$output" | tail -n 80
              test "$status" -eq 0
              test "$(printf "%s\\n" "$output" | grep -Ec "^ok [0-9]+ - ")" -eq 89
              test "$(printf "%s\\n" "$output" | grep -Ec "^not ok [0-9]+ - ")" -eq 0
              printf "%s\\n" "$output" | grep -Fx "1..89" >/dev/null
              printf "%s\\n" "$output" | grep -Fx "# tests 96" >/dev/null
              printf "%s\\n" "$output" | grep -Fx "# pass 96" >/dev/null
              printf "%s\\n" "$output" | grep -Fx "# fail 0" >/dev/null
              printf "%s\\n" "$output" | grep -Eq "^ok [0-9]+ - ask_user_question returns partial answers for committed options on cancellation$"
              printf "%s\\n" "$output" | grep -Eq "^ok [0-9]+ - ask_user_question preserves custom and multi answers on partial cancellation$"
              printf "%s\\n" "$output" | grep -Eq "^ok [0-9]+ - ask_user_question omits uncommitted state on cancellation$"
              printf "%s\\n" "$output" | grep -Eq "^ok [0-9]+ - ask_user_question retains committed answers when a replacement draft is cancelled$"
              printf "%s\\n" "$output" | grep -Eq "^ok [0-9]+ - ask_user_question reports cancellation without answers$"
              printf "%s\\n" "$output" | grep -Eq "^ok [0-9]+ - ask_user_question keeps the native blocked lifecycle private and balanced$"
              printf "%s\\n" "$output" | grep -Eq "^ok [0-9]+ - actual questionnaire producer drives Herdr through a private balanced compatibility lifecycle$"
            '

      - name: Compose abort RED artifact offline
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          abort_dir="$RUNNER_TEMP/questionnaire-upstream-abort"
          test ! -e "$abort_dir"
          mkdir "$abort_dir"
          chmod 0777 "$abort_dir"
          compose_log="$RUNNER_TEMP/questionnaire-upstream-abort-composition.log"
          docker run --rm -i --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --mount "type=bind,source=$abort_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            ACQUIRED_UPSTREAM_ROOT=/acquired-upstream ABORT_ARTIFACT_ROOT=/artifact/abort GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /usr/local/bin/node --experimental-strip-types --input-type=module <<'NODE' | tee "$compose_log"
          import { execFileSync } from "node:child_process";
          import { copyFile, mkdir, readdir, readFile } from "node:fs/promises";
          import { join } from "node:path";
          import { composeUpstreamQuestionnaireArtifact } from "/workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts";

          const workspace = "/workspace";
          const source = "/acquired-upstream";
          const artifact = "/artifact/abort";
          const base = JSON.parse(await readFile(`${workspace}/.github/workflows/fixtures/hosted-upstream-questionnaire.json`, "utf8"));
          const manifest = JSON.parse(await readFile(`${workspace}/.github/workflows/fixtures/hosted-upstream-questionnaire-abort.json`, "utf8"));
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          const expectedBase = {
            repository: "https://github.com/Gentleman-Programming/gentle-shell.git",
            commit: "f2d9d073ffc2299eb501902753b42789f7cdc461",
            tree: "6fb42857f728f131da054e592ec5d22c691ce870",
          };
          const expectedInventory = [
            { file: "006-abort-lifecycle-tests.patch", target: "tests/ask-user-question.test.ts", preimage: "cc439bdc0dcdfa1f8ade309abc63c1f565c64d40", postimage: "e0afc181256c503efa3fb3fd2fb54899abe4cc0c" },
            { file: "002-blocked-event-herdr-integration-test.patch", target: "tests/gentle-ai.test.ts", preimage: "81f4a38ecdfd11b97ada81308f036e0d318726fe", postimage: "f379515bae4cdda5d4bd84bafbf761597dd7dfcd" },
            { file: "005-partial-cancellation-producer.patch", target: "extensions/ask-user-question.ts", preimage: "52d6ff35296af67f1f4240ef5fc6df8bfe15dab6", postimage: "4b4db5d2addfc9d4c1ff112b017276d56bcf4a81" },
          ];
          const expectedAllowed = ["extensions/ask-user-question.ts", "tests/ask-user-question.test.ts", "tests/gentle-ai.test.ts", "lib/questionnaire/questionnaire-view.ts"];
          const expectedProtected = ["extensions/gentle-ai.ts", "lib/native-fullscreen-interaction.ts"];
          fail(base.schema === "gentle-pi.hosted-upstream-questionnaire-acquisition/v1", "base manifest schema changed");
          for (const key of Object.keys(expectedBase)) fail(base[key] === expectedBase[key], `base ${key} changed`);
          fail(manifest.schema === "gentle-pi.hosted-upstream-questionnaire-abort/v1", "abort manifest schema changed");
          fail(manifest.baseManifest === ".github/workflows/fixtures/hosted-upstream-questionnaire.json", "abort base manifest changed");
          for (const key of Object.keys(expectedBase)) fail(manifest.base?.[key] === expectedBase[key], `abort base ${key} changed`);
          const composition = manifest.composition;
          fail(composition.schema === "gentle-pi.hosted-upstream-questionnaire-composition/v1", "abort composition schema changed");
          fail(composition.derivedDirectory === "derived" && composition.patchDirectory === "patches/questionnaire-upstream", "abort composition directories changed");
          fail(JSON.stringify(composition.patchInventory) === JSON.stringify(expectedInventory), "abort patch inventory identity changed");
          fail(JSON.stringify(composition.allowedTargets) === JSON.stringify(expectedAllowed), "abort allowed targets changed");
          fail(JSON.stringify(composition.protectedTargets) === JSON.stringify(expectedProtected), "abort protected targets changed");

          const patchRoot = join(artifact, composition.patchDirectory);
          const outputRoot = join(artifact, composition.derivedDirectory);
          await mkdir(patchRoot, { recursive: true });
          for (const patch of expectedInventory) {
            const patchPath = join(patchRoot, patch.file);
            await copyFile(join(workspace, "tests/fixtures/upstream-questionnaire/patches", patch.file), patchPath);
            const changed = execFileSync("git", ["-C", source, "apply", "--numstat", "--summary", patchPath], { encoding: "utf8" });
            const changedPaths = changed.split("\n").filter((line) => line.includes("\t")).map((line) => line.slice(line.lastIndexOf("\t") + 1)).filter(Boolean);
            fail(changedPaths.length === 1 && changedPaths[0] === patch.target, `abort changed path mismatch: ${patch.file}`);
          }
          fail(JSON.stringify((await readdir(patchRoot)).sort()) === JSON.stringify(expectedInventory.map((patch) => patch.file).sort()), "abort staged patch set changed");
          await composeUpstreamQuestionnaireArtifact({ sourceRoot: source, patchRoot, outputRoot, manifest: composition });
          console.log(`abort-red-composition-proof root=${outputRoot} base=${manifest.base.commit} patches=${expectedInventory.map((patch) => patch.file).join(",")} postimage=${expectedInventory[0].postimage}`);
          NODE
          grep -Fx "abort-red-composition-proof root=/artifact/abort/derived base=f2d9d073ffc2299eb501902753b42789f7cdc461 patches=006-abort-lifecycle-tests.patch,002-blocked-event-herdr-integration-test.patch,005-partial-cancellation-producer.patch postimage=e0afc181256c503efa3fb3fd2fb54899abe4cc0c" "$compose_log" >/dev/null

      - name: Verify staged abort RED artifact is read-only
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          abort_dir="$RUNNER_TEMP/questionnaire-upstream-abort"
          derived_dir="$abort_dir/abort/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=256m --pids-limit=64 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              test -f /derived/tests/ask-user-question.test.ts
              test -f /derived/tests/gentle-ai.test.ts
              test -f /derived/extensions/ask-user-question.ts
              test -f /derived/lib/questionnaire/questionnaire-view.ts
              if touch /derived/.abort-red-read-only-probe 2>/dev/null; then exit 1; fi
            '

      - name: Run abort RED contract in read-only isolation
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          abort_dir="$RUNNER_TEMP/questionnaire-upstream-abort"
          derived_dir="$abort_dir/abort/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=2g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /derived "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            NODE_OPTIONS=--max-old-space-size=1536 GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              if touch /derived/.abort-red-test-read-only-probe 2>/dev/null; then exit 1; fi
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ESNext --module ESNext --moduleResolution bundler \
                --allowImportingTsExtensions --lib ESNext,DOM --typeRoots /acquired-upstream/node_modules/@types --types node \
                /derived/extensions/ask-user-question.ts /derived/tests/ask-user-question.test.ts
              /usr/local/bin/node /derived/scripts/check-types.mjs
              log=/tmp/uqa04b-red.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/ask-user-question.test.ts /derived/tests/gentle-ai.test.ts >"$log" 2>&1
              status=$?
              set -e
              tail -n 240 "$log"
              test "$status" -ne 0
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$log")" -eq 97
              test "$(grep -Ec "^ok [0-9]+ - " "$log")" -eq 89
              test "$(grep -Ec "^not ok [0-9]+ - " "$log")" -eq 8
              grep -Fx "1..97" "$log" >/dev/null
              grep -Fx "# tests 104" "$log" >/dev/null
              grep -Fx "# pass 96" "$log" >/dev/null
              grep -Fx "# fail 8" "$log" >/dev/null
              grep -Fx "# cancelled 0" "$log" >/dev/null
              grep -Fx "# skipped 0" "$log" >/dev/null
              grep -Fx "# todo 0" "$log" >/dev/null
              cat > /tmp/uqa04b-failures <<EOF
          ask_user_question returns empty cancellation before mounting an already-aborted request
          ask_user_question aborts a mounted questionnaire with committed partial answers
          ask_user_question aborts during a custom draft without committing the draft
          ask_user_question abort wins over later submit, cancel, and repeated abort
          ask_user_question keeps a completed submit ahead of later abort and cancel
          ask_user_question keeps a completed cancel ahead of later abort and submit
          ask_user_question closes before a deferred custom factory can mount a dock
          ask_user_question removes abort listeners while preserving a custom UI rejection
          EOF
              while IFS= read -r name; do
                test -n "$name"
                grep -Eq "^not ok [0-9]+ - ${name}$" "$log"
              done < /tmp/uqa04b-failures
              printf "%s\\n" "abort-red-failure-diagnostics"
              while IFS= read -r name; do
                printf "%s\\n" "--- $name ---"
                grep -E -A 18 -B 2 "^not ok [0-9]+ - ${name}$" "$log" | head -n 24 || true
              done < /tmp/uqa04b-failures
              printf "%s\\n" "abort-red-contract-markers"
              grep -E -i "fallback|added|removed|doneCalls|doneSources|mounted|customCalls|abort" "$log" | tail -n 120 || true
            '

      - name: Compose abort GREEN artifact offline
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          green_dir="$RUNNER_TEMP/questionnaire-upstream-abort-green"
          test ! -e "$green_dir"
          mkdir "$green_dir"
          chmod 0777 "$green_dir"
          compose_log="$RUNNER_TEMP/questionnaire-upstream-abort-green-composition.log"
          docker run --rm -i --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --mount "type=bind,source=$green_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            ACQUIRED_UPSTREAM_ROOT=/acquired-upstream ABORT_GREEN_ARTIFACT_ROOT=/artifact/abort-green GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /usr/local/bin/node --experimental-strip-types --input-type=module <<'NODE' | tee "$compose_log"
          import { execFileSync } from "node:child_process";
          import { copyFile, mkdir, readdir, readFile } from "node:fs/promises";
          import { join } from "node:path";
          import { composeUpstreamQuestionnaireArtifact } from "/workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts";

          const workspace = "/workspace";
          const source = "/acquired-upstream";
          const artifact = "/artifact/abort-green";
          const base = JSON.parse(await readFile(`${workspace}/.github/workflows/fixtures/hosted-upstream-questionnaire.json`, "utf8"));
          const manifest = JSON.parse(await readFile(`${workspace}/.github/workflows/fixtures/hosted-upstream-questionnaire-abort-green.json`, "utf8"));
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          const expectedBase = {
            repository: "https://github.com/Gentleman-Programming/gentle-shell.git",
            commit: "f2d9d073ffc2299eb501902753b42789f7cdc461",
            tree: "6fb42857f728f131da054e592ec5d22c691ce870",
          };
          const expectedInventory = [
            { file: "006-abort-lifecycle-tests.patch", target: "tests/ask-user-question.test.ts", preimage: "cc439bdc0dcdfa1f8ade309abc63c1f565c64d40", postimage: "e0afc181256c503efa3fb3fd2fb54899abe4cc0c" },
            { file: "002-blocked-event-herdr-integration-test.patch", target: "tests/gentle-ai.test.ts", preimage: "81f4a38ecdfd11b97ada81308f036e0d318726fe", postimage: "f379515bae4cdda5d4bd84bafbf761597dd7dfcd" },
            { file: "007-abort-lifecycle-producer.patch", target: "extensions/ask-user-question.ts", preimage: "52d6ff35296af67f1f4240ef5fc6df8bfe15dab6", postimage: "d7dea145b52cfe446083ca881be088b89f964ea6" },
            { file: "008-abort-lifecycle-view.patch", target: "lib/questionnaire/questionnaire-view.ts", preimage: "61284f0110522f2e65a43271f9e23f5bfe350a72", postimage: "bfb3e5d871ac0918aa00c4c6ef8aa9bbd690eac1" },
          ];
          const expectedAllowed = ["extensions/ask-user-question.ts", "tests/ask-user-question.test.ts", "tests/gentle-ai.test.ts", "lib/questionnaire/questionnaire-view.ts"];
          const expectedProtected = ["extensions/gentle-ai.ts", "lib/native-fullscreen-interaction.ts"];
          fail(base.schema === "gentle-pi.hosted-upstream-questionnaire-acquisition/v1", "base manifest schema changed");
          for (const key of Object.keys(expectedBase)) fail(base[key] === expectedBase[key], `base ${key} changed`);
          fail(manifest.schema === "gentle-pi.hosted-upstream-questionnaire-abort-green/v1", "abort GREEN manifest schema changed");
          fail(manifest.baseManifest === ".github/workflows/fixtures/hosted-upstream-questionnaire.json", "abort GREEN base manifest changed");
          for (const key of Object.keys(expectedBase)) fail(manifest.base?.[key] === expectedBase[key], `abort GREEN base ${key} changed`);
          const composition = manifest.composition;
          fail(composition.schema === "gentle-pi.hosted-upstream-questionnaire-composition/v1", "abort GREEN composition schema changed");
          fail(composition.derivedDirectory === "derived" && composition.patchDirectory === "patches/questionnaire-upstream", "abort GREEN composition directories changed");
          fail(JSON.stringify(composition.patchInventory) === JSON.stringify(expectedInventory), "abort GREEN patch inventory identity changed");
          fail(JSON.stringify(composition.allowedTargets) === JSON.stringify(expectedAllowed), "abort GREEN allowed targets changed");
          fail(JSON.stringify(composition.protectedTargets) === JSON.stringify(expectedProtected), "abort GREEN protected targets changed");

          const patchRoot = join(artifact, composition.patchDirectory);
          const outputRoot = join(artifact, composition.derivedDirectory);
          await mkdir(patchRoot, { recursive: true });
          for (const patch of expectedInventory) {
            const patchPath = join(patchRoot, patch.file);
            await copyFile(join(workspace, "tests/fixtures/upstream-questionnaire/patches", patch.file), patchPath);
            const changed = execFileSync("git", ["-C", source, "apply", "--numstat", "--summary", patchPath], { encoding: "utf8" });
            const changedPaths = changed.split("\n").filter((line) => line.includes("\t")).map((line) => line.slice(line.lastIndexOf("\t") + 1)).filter(Boolean);
            fail(changedPaths.length === 1 && changedPaths[0] === patch.target, `abort GREEN changed path mismatch: ${patch.file}`);
          }
          fail(JSON.stringify((await readdir(patchRoot)).sort()) === JSON.stringify(expectedInventory.map((patch) => patch.file).sort()), "abort GREEN staged patch set changed");
          await composeUpstreamQuestionnaireArtifact({ sourceRoot: source, patchRoot, outputRoot, manifest: composition });
          console.log(`abort-green-composition-proof root=${outputRoot} base=${manifest.base.commit} patches=${expectedInventory.map((patch) => patch.file).join(",")} postimages=${expectedInventory.map((patch) => patch.postimage).join(",")}`);
          NODE
          grep -Fx "abort-green-composition-proof root=/artifact/abort-green/derived base=f2d9d073ffc2299eb501902753b42789f7cdc461 patches=006-abort-lifecycle-tests.patch,002-blocked-event-herdr-integration-test.patch,007-abort-lifecycle-producer.patch,008-abort-lifecycle-view.patch postimages=e0afc181256c503efa3fb3fd2fb54899abe4cc0c,f379515bae4cdda5d4bd84bafbf761597dd7dfcd,d7dea145b52cfe446083ca881be088b89f964ea6,bfb3e5d871ac0918aa00c4c6ef8aa9bbd690eac1" "$compose_log" >/dev/null

      - name: Verify staged abort GREEN artifact is read-only
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          green_dir="$RUNNER_TEMP/questionnaire-upstream-abort-green"
          derived_dir="$green_dir/abort-green/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=256m --pids-limit=64 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin \
            /bin/sh -eu -c '
              test -f /derived/tests/ask-user-question.test.ts
              test -f /derived/tests/gentle-ai.test.ts
              test -f /derived/extensions/ask-user-question.ts
              test -f /derived/lib/questionnaire/questionnaire-view.ts
              if touch /derived/.abort-green-read-only-probe 2>/dev/null; then exit 1; fi
            '

      - name: Run abort GREEN contract in read-only isolation
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          green_dir="$RUNNER_TEMP/questionnaire-upstream-abort-green"
          derived_dir="$green_dir/abort-green/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=2g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /derived "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            NODE_OPTIONS=--max-old-space-size=1536 GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              if touch /derived/.abort-green-test-read-only-probe 2>/dev/null; then exit 1; fi
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ESNext --module ESNext --moduleResolution bundler \
                --allowImportingTsExtensions --lib ESNext,DOM --typeRoots /acquired-upstream/node_modules/@types --types node \
                /derived/extensions/ask-user-question.ts /derived/lib/questionnaire/questionnaire-view.ts \
                /derived/tests/ask-user-question.test.ts
              /usr/local/bin/node /derived/scripts/check-types.mjs
              log=/tmp/uqa04b-green.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/ask-user-question.test.ts /derived/tests/gentle-ai.test.ts >"$log" 2>&1
              status=$?
              set -e
              tail -n 240 "$log"
              test "$status" -eq 0
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$log")" -eq 97
              test "$(grep -Ec "^ok [0-9]+ - " "$log")" -eq 97
              test "$(grep -Ec "^not ok [0-9]+ - " "$log")" -eq 0
              grep -Fx "1..97" "$log" >/dev/null
              grep -Fx "# tests 104" "$log" >/dev/null
              grep -Fx "# pass 104" "$log" >/dev/null
              grep -Fx "# fail 0" "$log" >/dev/null
              grep -Fx "# cancelled 0" "$log" >/dev/null
              grep -Fx "# skipped 0" "$log" >/dev/null
              grep -Fx "# todo 0" "$log" >/dev/null
              printf "%s\n" "ask_user_question returns empty cancellation before mounting an already-aborted request" "ask_user_question aborts a mounted questionnaire with committed partial answers" "ask_user_question aborts during a custom draft without committing the draft" "ask_user_question abort wins over later submit, cancel, and repeated abort" "ask_user_question keeps a completed submit ahead of later abort and cancel" "ask_user_question keeps a completed cancel ahead of later abort and submit" "ask_user_question closes before a deferred custom factory can mount a dock" "ask_user_question removes abort listeners while preserving a custom UI rejection" | while IFS= read -r name; do
                grep -Eq "^ok [0-9]+ - ${name}$" "$log"
              done
              grep -Eq "^ok [0-9]+ - ask_user_question returns partial answers for committed options on cancellation$" "$log"
              grep -Eq "^ok [0-9]+ - ask_user_question preserves custom and multi answers on partial cancellation$" "$log"
              grep -Eq "^ok [0-9]+ - ask_user_question omits uncommitted state on cancellation$" "$log"
              grep -Eq "^ok [0-9]+ - ask_user_question retains committed answers when a replacement draft is cancelled$" "$log"
              grep -Eq "^ok [0-9]+ - ask_user_question reports cancellation without answers$" "$log"
              grep -Eq "^ok [0-9]+ - ask_user_question keeps the native blocked lifecycle private and balanced$" "$log"
              grep -Eq "^ok [0-9]+ - actual questionnaire producer drives Herdr through a private balanced compatibility lifecycle$" "$log"
            '

      - name: Compose controls RED artifact offline
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          controls_dir="$RUNNER_TEMP/questionnaire-upstream-controls-red"
          test ! -e "$controls_dir"
          mkdir "$controls_dir"
          chmod 0777 "$controls_dir"
          compose_log="$RUNNER_TEMP/questionnaire-upstream-controls-red-composition.log"
          docker run --rm -i --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --mount "type=bind,source=$controls_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            ACQUIRED_UPSTREAM_ROOT=/acquired-upstream CONTROLS_RED_ARTIFACT_ROOT=/artifact/controls-red GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /usr/local/bin/node --experimental-strip-types --input-type=module <<'NODE' | tee "$compose_log"
          import { execFileSync } from "node:child_process";
          import { copyFile, mkdir, readdir, readFile } from "node:fs/promises";
          import { join } from "node:path";
          import { composeUpstreamQuestionnaireArtifact } from "/workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts";

          const workspace = "/workspace";
          const source = "/acquired-upstream";
          const artifact = "/artifact/controls-red";
          const base = JSON.parse(await readFile(`${workspace}/.github/workflows/fixtures/hosted-upstream-questionnaire.json`, "utf8"));
          const manifest = JSON.parse(await readFile(`${workspace}/.github/workflows/fixtures/hosted-upstream-questionnaire-controls-red.json`, "utf8"));
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          const expectedBase = {
            repository: "https://github.com/Gentleman-Programming/gentle-shell.git",
            commit: "f2d9d073ffc2299eb501902753b42789f7cdc461",
            tree: "6fb42857f728f131da054e592ec5d22c691ce870",
          };
          const expectedInventory = [
            { file: "006-abort-lifecycle-tests.patch", target: "tests/ask-user-question.test.ts", preimage: "cc439bdc0dcdfa1f8ade309abc63c1f565c64d40", postimage: "e0afc181256c503efa3fb3fd2fb54899abe4cc0c" },
            { file: "002-blocked-event-herdr-integration-test.patch", target: "tests/gentle-ai.test.ts", preimage: "81f4a38ecdfd11b97ada81308f036e0d318726fe", postimage: "f379515bae4cdda5d4bd84bafbf761597dd7dfcd" },
            { file: "007-abort-lifecycle-producer.patch", target: "extensions/ask-user-question.ts", preimage: "52d6ff35296af67f1f4240ef5fc6df8bfe15dab6", postimage: "d7dea145b52cfe446083ca881be088b89f964ea6" },
            { file: "008-abort-lifecycle-view.patch", target: "lib/questionnaire/questionnaire-view.ts", preimage: "61284f0110522f2e65a43271f9e23f5bfe350a72", postimage: "bfb3e5d871ac0918aa00c4c6ef8aa9bbd690eac1" },
            { file: "009-controls-multi-view-tests.patch", target: "tests/questionnaire-view.test.ts", preimage: "9ea1ebae3d9a42d3e1281352adb10c337ec9846a", postimage: "158fb1897e21776f3fa798c5e6ce99c51fd0336f" },
            { file: "010-controls-existing-view-tests.patch", target: "tests/questionnaire-view.test.ts", preimage: "158fb1897e21776f3fa798c5e6ce99c51fd0336f", postimage: "26f9ff873cc88ee7f00c6d0eb78a3c34c33e546d" },
          ];
          const expectedAllowed = ["extensions/ask-user-question.ts", "tests/ask-user-question.test.ts", "tests/gentle-ai.test.ts", "lib/questionnaire/questionnaire-view.ts", "tests/questionnaire-view.test.ts"];
          const expectedProtected = ["extensions/gentle-ai.ts", "lib/native-fullscreen-interaction.ts"];
          fail(base.schema === "gentle-pi.hosted-upstream-questionnaire-acquisition/v1", "base manifest schema changed");
          for (const key of Object.keys(expectedBase)) fail(base[key] === expectedBase[key], `base ${key} changed`);
          fail(manifest.schema === "gentle-pi.hosted-upstream-questionnaire-controls-red/v1", "controls RED manifest schema changed");
          fail(manifest.baseManifest === ".github/workflows/fixtures/hosted-upstream-questionnaire.json", "controls RED base manifest changed");
          for (const key of Object.keys(expectedBase)) fail(manifest.base?.[key] === expectedBase[key], `controls RED base ${key} changed`);
          const composition = manifest.composition;
          fail(composition.schema === "gentle-pi.hosted-upstream-questionnaire-composition/v2", "controls RED composition schema changed");
          fail(composition.derivedDirectory === "derived" && composition.patchDirectory === "patches/questionnaire-upstream", "controls RED composition directories changed");
          fail(JSON.stringify(composition.patchInventory) === JSON.stringify(expectedInventory), "controls RED patch inventory identity changed");
          fail(JSON.stringify(composition.allowedTargets) === JSON.stringify(expectedAllowed), "controls RED allowed targets changed");
          fail(JSON.stringify(composition.protectedTargets) === JSON.stringify(expectedProtected), "controls RED protected targets changed");

          const patchRoot = join(artifact, composition.patchDirectory);
          const outputRoot = join(artifact, composition.derivedDirectory);
          await mkdir(patchRoot, { recursive: true });
          for (const patch of expectedInventory) {
            const patchPath = join(patchRoot, patch.file);
            await copyFile(join(workspace, "tests/fixtures/upstream-questionnaire/patches", patch.file), patchPath);
            const changed = execFileSync("git", ["-C", source, "apply", "--numstat", "--summary", patchPath], { encoding: "utf8" });
            const changedPaths = changed.split("\n").filter((line) => line.includes("\t")).map((line) => line.slice(line.lastIndexOf("\t") + 1)).filter(Boolean);
            fail(changedPaths.length === 1 && changedPaths[0] === patch.target, `controls RED changed path mismatch: ${patch.file}`);
          }
          fail(JSON.stringify((await readdir(patchRoot)).sort()) === JSON.stringify(expectedInventory.map((patch) => patch.file).sort()), "controls RED staged patch set changed");
          await composeUpstreamQuestionnaireArtifact({ sourceRoot: source, patchRoot, outputRoot, manifest: composition });
          console.log(`controls-red-composition-proof root=${outputRoot} base=${manifest.base.commit} patches=${expectedInventory.map((patch) => patch.file).join(",")} postimage=${expectedInventory.at(-1).postimage}`);
          NODE
          grep -Fx "controls-red-composition-proof root=/artifact/controls-red/derived base=f2d9d073ffc2299eb501902753b42789f7cdc461 patches=006-abort-lifecycle-tests.patch,002-blocked-event-herdr-integration-test.patch,007-abort-lifecycle-producer.patch,008-abort-lifecycle-view.patch,009-controls-multi-view-tests.patch,010-controls-existing-view-tests.patch postimage=26f9ff873cc88ee7f00c6d0eb78a3c34c33e546d" "$compose_log" >/dev/null

      - name: Verify staged controls RED artifact is read-only
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          controls_dir="$RUNNER_TEMP/questionnaire-upstream-controls-red"
          derived_dir="$controls_dir/controls-red/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=256m --pids-limit=64 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin \
            /bin/sh -eu -c '
              test -f /derived/tests/questionnaire-view.test.ts
              test -f /derived/lib/questionnaire/questionnaire-view.ts
              if touch /derived/.controls-red-read-only-probe 2>/dev/null; then exit 1; fi
            '

      - name: Run controls RED contract in read-only isolation
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          controls_dir="$RUNNER_TEMP/questionnaire-upstream-controls-red"
          derived_dir="$controls_dir/controls-red/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=2g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /derived "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            NODE_OPTIONS=--max-old-space-size=1536 GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              if touch /derived/.controls-red-test-read-only-probe 2>/dev/null; then exit 1; fi
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ESNext --module ESNext --moduleResolution bundler \
                --allowImportingTsExtensions --lib ESNext,DOM --typeRoots /acquired-upstream/node_modules/@types --types node \
                /derived/extensions/ask-user-question.ts /derived/lib/questionnaire/questionnaire-view.ts \
                /derived/tests/ask-user-question.test.ts /derived/tests/questionnaire-view.test.ts
              /usr/local/bin/node /derived/scripts/check-types.mjs
              log=/tmp/uqa05-red.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/questionnaire-view.test.ts >"$log" 2>&1
              status=$?
              set -e
              tail -n 240 "$log"
              test "$status" -ne 0
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$log")" -eq 23
              test "$(grep -Ec "^ok [0-9]+ - " "$log")" -eq 20
              test "$(grep -Ec "^not ok [0-9]+ - " "$log")" -eq 3
              grep -Fx "1..23" "$log" >/dev/null
              grep -Fx "# tests 23" "$log" >/dev/null
              grep -Fx "# pass 20" "$log" >/dev/null
              grep -Fx "# fail 3" "$log" >/dev/null
              grep -Fx "# cancelled 0" "$log" >/dev/null
              grep -Fx "# skipped 0" "$log" >/dev/null
              grep -Fx "# todo 0" "$log" >/dev/null
              printf "%s\n" "renders explicit Submit and Cancel rows after the custom row" "selecting an option requires explicit submit and cancel remains independently reachable" "multi-select submit preserves an empty selection and cancel remains answerless" | while IFS= read -r name; do
                grep -Eq "^not ok [0-9]+ - ${name}$" "$log"
              done
              printf "%s\n" "controls-red-failure-diagnostics"
              while IFS= read -r name; do
                printf "%s\n" "--- $name ---"
                grep -E -A 18 -B 2 "^not ok [0-9]+ - ${name}$" "$log" | head -n 24 || true
              done <<'EOF'
          renders explicit Submit and Cancel rows after the custom row
          selecting an option requires explicit submit and cancel remains independently reachable
          multi-select submit preserves an empty selection and cancel remains answerless
          EOF
            '

      - name: Compose controls GREEN artifact offline
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          green_dir="$RUNNER_TEMP/questionnaire-upstream-controls-green"
          test ! -e "$green_dir"
          mkdir "$green_dir"
          chmod 0777 "$green_dir"
          compose_log="$RUNNER_TEMP/questionnaire-upstream-controls-green-composition.log"
          docker run --rm -i --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --mount "type=bind,source=$green_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            ACQUIRED_UPSTREAM_ROOT=/acquired-upstream CONTROLS_GREEN_ARTIFACT_ROOT=/artifact/controls-green GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /usr/local/bin/node --experimental-strip-types --input-type=module <<'NODE' | tee "$compose_log"
          import { execFileSync } from "node:child_process";
          import { copyFile, mkdir, readdir, readFile } from "node:fs/promises";
          import { join } from "node:path";
          import { composeUpstreamQuestionnaireArtifact } from "/workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts";

          const workspace = "/workspace";
          const source = "/acquired-upstream";
          const artifact = "/artifact/controls-green";
          const base = JSON.parse(await readFile(`${workspace}/.github/workflows/fixtures/hosted-upstream-questionnaire.json`, "utf8"));
          const manifest = JSON.parse(await readFile(`${workspace}/.github/workflows/fixtures/hosted-upstream-questionnaire-controls-green.json`, "utf8"));
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          const expectedBase = { repository: "https://github.com/Gentleman-Programming/gentle-shell.git", commit: "f2d9d073ffc2299eb501902753b42789f7cdc461", tree: "6fb42857f728f131da054e592ec5d22c691ce870" };
          const expectedInventory = [
            { file: "006-abort-lifecycle-tests.patch", target: "tests/ask-user-question.test.ts", preimage: "cc439bdc0dcdfa1f8ade309abc63c1f565c64d40", postimage: "e0afc181256c503efa3fb3fd2fb54899abe4cc0c" },
            { file: "012-controls-ask-tests.patch", target: "tests/ask-user-question.test.ts", preimage: "e0afc181256c503efa3fb3fd2fb54899abe4cc0c", postimage: "4534749ee354b4aea1278652c865dcbb3ce3f6ae" },
            { file: "002-blocked-event-herdr-integration-test.patch", target: "tests/gentle-ai.test.ts", preimage: "81f4a38ecdfd11b97ada81308f036e0d318726fe", postimage: "f379515bae4cdda5d4bd84bafbf761597dd7dfcd" },
            { file: "013-controls-herdr-test.patch", target: "tests/gentle-ai.test.ts", preimage: "f379515bae4cdda5d4bd84bafbf761597dd7dfcd", postimage: "c660cb38d4612545db38ce2d11890731de5b3220" },
            { file: "007-abort-lifecycle-producer.patch", target: "extensions/ask-user-question.ts", preimage: "52d6ff35296af67f1f4240ef5fc6df8bfe15dab6", postimage: "d7dea145b52cfe446083ca881be088b89f964ea6" },
            { file: "014-controls-producer.patch", target: "extensions/ask-user-question.ts", preimage: "d7dea145b52cfe446083ca881be088b89f964ea6", postimage: "716f3f03a4fc42b2f9958cfccb425a23ec2fe1c9" },
            { file: "008-abort-lifecycle-view.patch", target: "lib/questionnaire/questionnaire-view.ts", preimage: "61284f0110522f2e65a43271f9e23f5bfe350a72", postimage: "bfb3e5d871ac0918aa00c4c6ef8aa9bbd690eac1" },
            { file: "011-controls-view.patch", target: "lib/questionnaire/questionnaire-view.ts", preimage: "bfb3e5d871ac0918aa00c4c6ef8aa9bbd690eac1", postimage: "5e7f058a727ce84c79b8f5a5571dcaf42163dd48" },
            { file: "009-controls-multi-view-tests.patch", target: "tests/questionnaire-view.test.ts", preimage: "9ea1ebae3d9a42d3e1281352adb10c337ec9846a", postimage: "158fb1897e21776f3fa798c5e6ce99c51fd0336f" },
            { file: "010-controls-existing-view-tests.patch", target: "tests/questionnaire-view.test.ts", preimage: "158fb1897e21776f3fa798c5e6ce99c51fd0336f", postimage: "26f9ff873cc88ee7f00c6d0eb78a3c34c33e546d" },
          ];
          const expectedAllowed = ["extensions/ask-user-question.ts", "tests/ask-user-question.test.ts", "tests/gentle-ai.test.ts", "lib/questionnaire/questionnaire-view.ts", "tests/questionnaire-view.test.ts"];
          const expectedProtected = ["extensions/gentle-ai.ts", "lib/native-fullscreen-interaction.ts"];
          fail(base.schema === "gentle-pi.hosted-upstream-questionnaire-acquisition/v1", "base manifest schema changed");
          for (const key of Object.keys(expectedBase)) fail(base[key] === expectedBase[key], `base ${key} changed`);
          fail(manifest.schema === "gentle-pi.hosted-upstream-questionnaire-controls-green/v1", "controls GREEN manifest schema changed");
          fail(manifest.baseManifest === ".github/workflows/fixtures/hosted-upstream-questionnaire.json", "controls GREEN base manifest changed");
          for (const key of Object.keys(expectedBase)) fail(manifest.base?.[key] === expectedBase[key], `controls GREEN base ${key} changed`);
          const composition = manifest.composition;
          fail(composition.schema === "gentle-pi.hosted-upstream-questionnaire-composition/v2", "controls GREEN composition schema changed");
          fail(composition.derivedDirectory === "derived" && composition.patchDirectory === "patches/questionnaire-upstream", "controls GREEN composition directories changed");
          fail(JSON.stringify(composition.patchInventory) === JSON.stringify(expectedInventory), "controls GREEN patch inventory identity changed");
          fail(JSON.stringify(composition.allowedTargets) === JSON.stringify(expectedAllowed), "controls GREEN allowed targets changed");
          fail(JSON.stringify(composition.protectedTargets) === JSON.stringify(expectedProtected), "controls GREEN protected targets changed");
          const patchRoot = join(artifact, composition.patchDirectory);
          const outputRoot = join(artifact, composition.derivedDirectory);
          await mkdir(patchRoot, { recursive: true });
          for (const patch of expectedInventory) {
            const patchPath = join(patchRoot, patch.file);
            await copyFile(join(workspace, "tests/fixtures/upstream-questionnaire/patches", patch.file), patchPath);
            const changed = execFileSync("git", ["-C", source, "apply", "--numstat", "--summary", patchPath], { encoding: "utf8" });
            const changedPaths = changed.split("\n").filter((line) => line.includes("\t")).map((line) => line.slice(line.lastIndexOf("\t") + 1)).filter(Boolean);
            fail(changedPaths.length === 1 && changedPaths[0] === patch.target, `controls GREEN changed path mismatch: ${patch.file}`);
          }
          fail(JSON.stringify((await readdir(patchRoot)).sort()) === JSON.stringify(expectedInventory.map((patch) => patch.file).sort()), "controls GREEN staged patch set changed");
          await composeUpstreamQuestionnaireArtifact({ sourceRoot: source, patchRoot, outputRoot, manifest: composition });
          console.log(`controls-green-composition-proof root=${outputRoot} base=${manifest.base.commit} patches=${expectedInventory.map((patch) => patch.file).join(",")} postimages=${expectedInventory.map((patch) => patch.postimage).join(",")}`);
          NODE
          grep -Fx "controls-green-composition-proof root=/artifact/controls-green/derived base=f2d9d073ffc2299eb501902753b42789f7cdc461 patches=006-abort-lifecycle-tests.patch,012-controls-ask-tests.patch,002-blocked-event-herdr-integration-test.patch,013-controls-herdr-test.patch,007-abort-lifecycle-producer.patch,014-controls-producer.patch,008-abort-lifecycle-view.patch,011-controls-view.patch,009-controls-multi-view-tests.patch,010-controls-existing-view-tests.patch postimages=e0afc181256c503efa3fb3fd2fb54899abe4cc0c,4534749ee354b4aea1278652c865dcbb3ce3f6ae,f379515bae4cdda5d4bd84bafbf761597dd7dfcd,c660cb38d4612545db38ce2d11890731de5b3220,d7dea145b52cfe446083ca881be088b89f964ea6,716f3f03a4fc42b2f9958cfccb425a23ec2fe1c9,bfb3e5d871ac0918aa00c4c6ef8aa9bbd690eac1,5e7f058a727ce84c79b8f5a5571dcaf42163dd48,158fb1897e21776f3fa798c5e6ce99c51fd0336f,26f9ff873cc88ee7f00c6d0eb78a3c34c33e546d" "$compose_log" >/dev/null

      - name: Verify staged controls GREEN artifact is read-only
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          green_dir="$RUNNER_TEMP/questionnaire-upstream-controls-green"
          derived_dir="$green_dir/controls-green/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=256m --pids-limit=64 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            /bin/sh -eu -c '
              test -f /derived/tests/ask-user-question.test.ts
              test -f /derived/tests/gentle-ai.test.ts
              test -f /derived/extensions/ask-user-question.ts
              test -f /derived/lib/questionnaire/questionnaire-view.ts
              test -f /derived/tests/questionnaire-view.test.ts
              if touch /derived/.controls-green-read-only-probe 2>/dev/null; then exit 1; fi
            '

      - name: Compose controls correction RED artifact offline
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          controls_dir="$RUNNER_TEMP/questionnaire-upstream-controls-green"
          correction_dir="$RUNNER_TEMP/questionnaire-upstream-controls-correction-red"
          test -d "$controls_dir/controls-green/derived"
          test ! -e "$correction_dir"
          mkdir "$correction_dir"
          chmod 0777 "$correction_dir"
          compose_log="$RUNNER_TEMP/questionnaire-upstream-controls-correction-red-composition.log"
          docker run --rm -i --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$controls_dir/controls-green/derived,target=/base-green,readonly" \
            --mount "type=bind,source=$correction_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /usr/local/bin/node --experimental-strip-types --input-type=module <<'NODE' | tee "$compose_log"
          import { execFileSync } from "node:child_process";
          import { copyFile, mkdir, readFile } from "node:fs/promises";
          import { join } from "node:path";
          import { composeUpstreamQuestionnaireArtifact } from "/workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts";

          const source = "/base-green";
          const artifact = "/artifact";
          const manifest = {
            schema: "gentle-pi.hosted-upstream-questionnaire-composition/v2",
            derivedDirectory: "derived",
            patchDirectory: "patches/questionnaire-upstream",
            patchInventory: [{ file: "015-controls-regression-tests.patch", target: "tests/questionnaire-view.test.ts", preimage: "26f9ff873cc88ee7f00c6d0eb78a3c34c33e546d", postimage: "65d8c01f2857164f2582590a003b7f67144457cd" }],
            allowedTargets: ["tests/questionnaire-view.test.ts"],
            protectedTargets: ["lib/native-fullscreen-interaction.ts"],
          };
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          fail(manifest.schema === "gentle-pi.hosted-upstream-questionnaire-composition/v2", "correction composition schema changed");
          fail(manifest.derivedDirectory === "derived" && manifest.patchDirectory === "patches/questionnaire-upstream", "correction composition directories changed");
          fail(JSON.stringify(manifest.allowedTargets) === JSON.stringify(["tests/questionnaire-view.test.ts"]), "correction allowed targets changed");
          fail(JSON.stringify(manifest.protectedTargets) === JSON.stringify(["lib/native-fullscreen-interaction.ts"]), "correction protected targets changed");
          const patch = manifest.patchInventory[0];
          fail(patch?.file === "015-controls-regression-tests.patch" && patch.target === "tests/questionnaire-view.test.ts", "correction patch identity changed");
          fail(execFileSync("git", ["-C", source, "hash-object", "--no-filters", "--", patch.target], { encoding: "utf8" }).trim() === patch.preimage, "correction parent preimage changed");
          const patchRoot = join(artifact, manifest.patchDirectory);
          const outputRoot = join(artifact, manifest.derivedDirectory);
          await mkdir(patchRoot, { recursive: true });
          const patchPath = join(patchRoot, patch.file);
          await copyFile(join("/workspace/tests/fixtures/upstream-questionnaire/patches", patch.file), patchPath);
          const changed = execFileSync("git", ["-C", source, "apply", "--numstat", "--summary", patchPath], { encoding: "utf8" });
          const changedPaths = changed.split("\n").filter((line) => line.includes("\t")).map((line) => line.slice(line.lastIndexOf("\t") + 1)).filter(Boolean);
          fail(changedPaths.length === 1 && changedPaths[0] === patch.target, "correction changed path mismatch");
          await composeUpstreamQuestionnaireArtifact({ sourceRoot: source, patchRoot, outputRoot, manifest });
          console.log(`controls-correction-red-proof root=${outputRoot} base=${patch.preimage} patch=${patch.file} postimage=${patch.postimage}`);
          NODE
          grep -Fx "controls-correction-red-proof root=/artifact/derived base=26f9ff873cc88ee7f00c6d0eb78a3c34c33e546d patch=015-controls-regression-tests.patch postimage=65d8c01f2857164f2582590a003b7f67144457cd" "$compose_log" >/dev/null

      - name: Run controls correction RED contract in read-only isolation
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          correction_dir="$RUNNER_TEMP/questionnaire-upstream-controls-correction-red"
          derived_dir="$correction_dir/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=2g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /derived "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            NODE_OPTIONS=--max-old-space-size=1536 GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              if touch /derived/.controls-correction-red-read-only-probe 2>/dev/null; then exit 1; fi
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ESNext --module ESNext --moduleResolution bundler \
                --allowImportingTsExtensions --lib ESNext,DOM --typeRoots /acquired-upstream/node_modules/@types --types node \
                /derived/lib/questionnaire/questionnaire-view.ts /derived/tests/questionnaire-view.test.ts
              log=/tmp/uqa05-correction-red.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/questionnaire-view.test.ts >"$log" 2>&1
              status=$?
              set -e
              tail -n 200 "$log"
              test "$status" -ne 0
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$log")" -eq 26
              test "$(grep -Ec "^ok [0-9]+ - " "$log")" -eq 25
              test "$(grep -Ec "^not ok [0-9]+ - " "$log")" -eq 1
              grep -Fx "1..26" "$log" >/dev/null
              grep -Fx "# tests 26" "$log" >/dev/null
              grep -Fx "# pass 25" "$log" >/dev/null
              grep -Fx "# fail 1" "$log" >/dev/null
              grep -Fx "# cancelled 0" "$log" >/dev/null
              grep -Fx "# skipped 0" "$log" >/dev/null
              grep -Fx "# todo 0" "$log" >/dev/null
              grep -Eq "^not ok [0-9]+ - multi-select submit uses the latest toggles after an earlier commit$" "$log"
            '

      - name: Compose controls correction GREEN artifact offline
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          correction_dir="$RUNNER_TEMP/questionnaire-upstream-controls-correction-red"
          green_dir="$RUNNER_TEMP/questionnaire-upstream-controls-correction-green"
          test -d "$correction_dir/derived"
          test ! -e "$green_dir"
          mkdir "$green_dir"
          chmod 0777 "$green_dir"
          compose_log="$RUNNER_TEMP/questionnaire-upstream-controls-correction-green-composition.log"
          docker run --rm -i --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$correction_dir/derived,target=/base-correction-red,readonly" \
            --mount "type=bind,source=$green_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /usr/local/bin/node --experimental-strip-types --input-type=module <<'NODE' | tee "$compose_log"
          import { execFileSync } from "node:child_process";
          import { copyFile, mkdir, readdir } from "node:fs/promises";
          import { join } from "node:path";
          import { composeUpstreamQuestionnaireArtifact } from "/workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts";

          const source = "/base-correction-red";
          const artifact = "/artifact";
          const manifest = {
            schema: "gentle-pi.hosted-upstream-questionnaire-composition/v2",
            derivedDirectory: "derived",
            patchDirectory: "patches/questionnaire-upstream",
            patchInventory: [
              { file: "016-controls-latest-multi.patch", target: "lib/questionnaire/questionnaire-view.ts", preimage: "5e7f058a727ce84c79b8f5a5571dcaf42163dd48", postimage: "185fd41bbee0de644fdcc7d493db62e0ff9484ae" },
              { file: "017-controls-upstream-success-tests.patch", target: "tests/ask-user-question.test.ts", preimage: "4534749ee354b4aea1278652c865dcbb3ce3f6ae", postimage: "a09fb580406dff77e7093863b36c658a384393b4" },
            ],
            allowedTargets: ["lib/questionnaire/questionnaire-view.ts", "tests/ask-user-question.test.ts"],
            protectedTargets: ["lib/native-fullscreen-interaction.ts"],
          };
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          fail(manifest.schema === "gentle-pi.hosted-upstream-questionnaire-composition/v2", "correction GREEN composition schema changed");
          fail(manifest.derivedDirectory === "derived" && manifest.patchDirectory === "patches/questionnaire-upstream", "correction GREEN composition directories changed");
          fail(JSON.stringify(manifest.allowedTargets) === JSON.stringify(["lib/questionnaire/questionnaire-view.ts", "tests/ask-user-question.test.ts"]), "correction GREEN allowed targets changed");
          fail(JSON.stringify(manifest.protectedTargets) === JSON.stringify(["lib/native-fullscreen-interaction.ts"]), "correction GREEN protected targets changed");
          for (const patch of manifest.patchInventory) {
            fail(execFileSync("git", ["-C", source, "hash-object", "--no-filters", "--", patch.target], { encoding: "utf8" }).trim() === patch.preimage, `correction GREEN preimage changed: ${patch.file}`);
          }
          const patchRoot = join(artifact, manifest.patchDirectory);
          const outputRoot = join(artifact, manifest.derivedDirectory);
          await mkdir(patchRoot, { recursive: true });
          for (const patch of manifest.patchInventory) await copyFile(join("/workspace/tests/fixtures/upstream-questionnaire/patches", patch.file), join(patchRoot, patch.file));
          fail(JSON.stringify((await readdir(patchRoot)).sort()) === JSON.stringify(manifest.patchInventory.map((patch) => patch.file).sort()), "correction GREEN staged patch set changed");
          await composeUpstreamQuestionnaireArtifact({ sourceRoot: source, patchRoot, outputRoot, manifest });
          console.log(`controls-correction-green-proof root=${outputRoot} patches=${manifest.patchInventory.map((patch) => patch.file).join(",")} postimages=${manifest.patchInventory.map((patch) => patch.postimage).join(",")}`);
          NODE
          grep -Fx "controls-correction-green-proof root=/artifact/derived patches=016-controls-latest-multi.patch,017-controls-upstream-success-tests.patch postimages=185fd41bbee0de644fdcc7d493db62e0ff9484ae,a09fb580406dff77e7093863b36c658a384393b4" "$compose_log" >/dev/null

      - name: Run controls correction GREEN contract in read-only isolation
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          green_dir="$RUNNER_TEMP/questionnaire-upstream-controls-correction-green"
          derived_dir="$green_dir/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=2g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /derived "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            NODE_OPTIONS=--max-old-space-size=1536 GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              if touch /derived/.controls-correction-green-read-only-probe 2>/dev/null; then exit 1; fi
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ESNext --module ESNext --moduleResolution bundler \
                --allowImportingTsExtensions --lib ESNext,DOM --typeRoots /acquired-upstream/node_modules/@types --types node \
                /derived/extensions/ask-user-question.ts /derived/lib/questionnaire/questionnaire-view.ts \
                /derived/tests/ask-user-question.test.ts /derived/tests/questionnaire-view.test.ts
              /usr/local/bin/node /derived/scripts/check-types.mjs
              ask_log=/tmp/uqa05-correction-green-ask.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/ask-user-question.test.ts /derived/tests/gentle-ai.test.ts >"$ask_log" 2>&1
              ask_status=$?
              set -e
              cat "$ask_log"
              test "$ask_status" -eq 0
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$ask_log")" -eq 98
              test "$(grep -Ec "^ok [0-9]+ - " "$ask_log")" -eq 98
              test "$(grep -Ec "^not ok [0-9]+ - " "$ask_log")" -eq 0
              grep -Fx "1..98" "$ask_log" >/dev/null
              grep -Fx "# tests 105" "$ask_log" >/dev/null
              grep -Fx "# pass 105" "$ask_log" >/dev/null
              grep -Fx "# fail 0" "$ask_log" >/dev/null
              grep -Fx "# cancelled 0" "$ask_log" >/dev/null
              grep -Fx "# skipped 0" "$ask_log" >/dev/null
              grep -Fx "# todo 0" "$ask_log" >/dev/null
              cat > /tmp/uqa05-correction-green-ask-names <<EOF
          ask_user_question commits a single-select answer end-to-end
          ask_user_question echoes an option preview beside the answer
          ask_user_question commits a multiSelect answer with every toggled option
          ask_user_question commits a free-text custom answer
          ask_user_question keeps toggled options in a multiSelect custom answer
          ask_user_question submits an empty multi-select through explicit Submit
          ask_user_question keeps the native blocked lifecycle private and balanced
          ask_user_question returns empty cancellation before mounting an already-aborted request
          ask_user_question aborts a mounted questionnaire with committed partial answers
          ask_user_question aborts during a custom draft without committing the draft
          ask_user_question abort wins over later submit, cancel, and repeated abort
          ask_user_question keeps a completed submit ahead of later abort and cancel
          ask_user_question keeps a completed cancel ahead of later abort and submit
          ask_user_question closes before a deferred custom factory can mount a dock
          ask_user_question removes abort listeners while preserving a custom UI rejection
          actual questionnaire producer drives Herdr through a private balanced compatibility lifecycle
          EOF
              while IFS= read -r name; do grep -Eq "^ok [0-9]+ - ${name}$" "$ask_log"; done < /tmp/uqa05-correction-green-ask-names
              view_log=/tmp/uqa05-correction-green-view.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/questionnaire-view.test.ts >"$view_log" 2>&1
              view_status=$?
              set -e
              tail -n 200 "$view_log"
              test "$view_status" -eq 0
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$view_log")" -eq 26
              test "$(grep -Ec "^ok [0-9]+ - " "$view_log")" -eq 26
              test "$(grep -Ec "^not ok [0-9]+ - " "$view_log")" -eq 0
              grep -Fx "1..26" "$view_log" >/dev/null
              grep -Fx "# tests 26" "$view_log" >/dev/null
              grep -Fx "# pass 26" "$view_log" >/dev/null
              grep -Fx "# fail 0" "$view_log" >/dev/null
              grep -Fx "# cancelled 0" "$view_log" >/dev/null
              grep -Fx "# skipped 0" "$view_log" >/dev/null
              grep -Fx "# todo 0" "$view_log" >/dev/null
              cat > /tmp/uqa05-correction-green-view-names <<EOF
          renders explicit Submit and Cancel rows after the custom row
          selecting an option requires explicit submit and cancel remains independently reachable
          multi-select submit preserves an empty selection and cancel remains answerless
          multi-select submit uses the latest toggles after an earlier commit
          direct mouse clicks on Submit and Cancel are independent and idempotent
          failed submit leaves synthesized multi answers uncommitted
          EOF
              while IFS= read -r name; do grep -Eq "^ok [0-9]+ - ${name}$" "$view_log"; done < /tmp/uqa05-correction-green-view-names
            '

      - name: Compose UQA-06a notes view RED artifact offline
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          source_dir="$RUNNER_TEMP/questionnaire-upstream-controls-correction-green/derived"
          red_dir="$RUNNER_TEMP/questionnaire-upstream-notes-view-red"
          test -d "$source_dir"
          test ! -e "$red_dir"
          mkdir "$red_dir"
          chmod 0777 "$red_dir"
          compose_log="$RUNNER_TEMP/questionnaire-upstream-notes-view-red-composition.log"
          docker run --rm -i --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$source_dir,target=/base-green,readonly" \
            --mount "type=bind,source=$red_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /usr/local/bin/node --experimental-strip-types --input-type=module <<'NODE' | tee "$compose_log"
          import { execFileSync } from "node:child_process";
          import { copyFile, mkdir, readdir } from "node:fs/promises";
          import { join } from "node:path";
          import { composeUpstreamQuestionnaireArtifact } from "/workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts";

          const source = "/base-green";
          const artifact = "/artifact";
          const patch = { file: "018-notes-view-tests.patch", target: "tests/questionnaire-view.test.ts", preimage: "65d8c01f2857164f2582590a003b7f67144457cd", postimage: "eac8474634620cd723ed8775a5c19f3e7f6fe35a" };
          const manifest = {
            schema: "gentle-pi.hosted-upstream-questionnaire-composition/v2",
            derivedDirectory: "derived",
            patchDirectory: "patches/questionnaire-upstream",
            patchInventory: [patch],
            allowedTargets: [patch.target],
            protectedTargets: ["lib/native-fullscreen-interaction.ts"],
          };
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          fail(execFileSync("git", ["-C", source, "hash-object", "--no-filters", "--", patch.target], { encoding: "utf8" }).trim() === patch.preimage, "notes view RED preimage changed");
          const changed = execFileSync("git", ["-C", source, "apply", "--numstat", "--summary", join("/workspace/tests/fixtures/upstream-questionnaire/patches", patch.file)], { encoding: "utf8" });
          const changedPaths = changed.split("\n").filter((line) => line.includes("\t")).map((line) => line.slice(line.lastIndexOf("\t") + 1)).filter(Boolean);
          fail(changedPaths.length === 1 && changedPaths[0] === patch.target, "notes view RED changed path mismatch");
          const patchRoot = join(artifact, manifest.patchDirectory);
          const outputRoot = join(artifact, manifest.derivedDirectory);
          await mkdir(patchRoot, { recursive: true });
          await copyFile(join("/workspace/tests/fixtures/upstream-questionnaire/patches", patch.file), join(patchRoot, patch.file));
          fail(JSON.stringify(await readdir(patchRoot)) === JSON.stringify([patch.file]), "notes view RED staged patch set changed");
          await composeUpstreamQuestionnaireArtifact({ sourceRoot: source, patchRoot, outputRoot, manifest });
          console.log(`notes-view-red-proof root=${outputRoot} base=${patch.preimage} patch=${patch.file} postimage=${patch.postimage}`);
          NODE
          grep -Fx "notes-view-red-proof root=/artifact/derived base=65d8c01f2857164f2582590a003b7f67144457cd patch=018-notes-view-tests.patch postimage=eac8474634620cd723ed8775a5c19f3e7f6fe35a" "$compose_log" >/dev/null

      - name: Run UQA-06a notes view RED in read-only isolation
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          derived_dir="$RUNNER_TEMP/questionnaire-upstream-notes-view-red/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=2g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /derived "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            NODE_OPTIONS=--max-old-space-size=1536 GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              if touch /derived/.notes-view-red-read-only-probe 2>/dev/null; then exit 1; fi
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ESNext --module ESNext --moduleResolution bundler \
                --allowImportingTsExtensions --lib ESNext,DOM --typeRoots /acquired-upstream/node_modules/@types --types node \
                /derived/lib/questionnaire/questionnaire-view.ts /derived/tests/questionnaire-view.test.ts
              log=/tmp/uqa06a-notes-view-red.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/questionnaire-view.test.ts >"$log" 2>&1
              status=$?
              set -e
              cat "$log"
              test "$status" -ne 0
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$log")" -eq 30
              test "$(grep -Ec "^ok [0-9]+ - " "$log")" -eq 26
              test "$(grep -Ec "^not ok [0-9]+ - " "$log")" -eq 4
              grep -Fx "1..30" "$log" >/dev/null
              grep -Fx "# tests 30" "$log" >/dev/null
              grep -Fx "# pass 26" "$log" >/dev/null
              grep -Fx "# fail 4" "$log" >/dev/null
              grep -Fx "# cancelled 0" "$log" >/dev/null
              grep -Fx "# skipped 0" "$log" >/dev/null
              grep -Fx "# todo 0" "$log" >/dev/null
              cat > /tmp/uqa06a-notes-view-red-names <<EOF
          renders question and final global note rows before Submit and Cancel
          successful submit attaches a trimmed question note and global note
          note drafts survive tab switches and cancellation preserves committed notes
          omits whitespace-only notes and keeps a global note on answerless cancellation
          EOF
              while IFS= read -r name; do grep -Eq "^not ok [0-9]+ - ${name}$" "$log"; done < /tmp/uqa06a-notes-view-red-names
            '

      - name: Compose UQA-06a notes view GREEN artifact offline
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          source_dir="$RUNNER_TEMP/questionnaire-upstream-notes-view-red/derived"
          green_dir="$RUNNER_TEMP/questionnaire-upstream-notes-view-green"
          test -d "$source_dir"
          test ! -e "$green_dir"
          mkdir "$green_dir"
          chmod 0777 "$green_dir"
          compose_log="$RUNNER_TEMP/questionnaire-upstream-notes-view-green-composition.log"
          docker run --rm -i --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$source_dir,target=/base-red,readonly" \
            --mount "type=bind,source=$green_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /usr/local/bin/node --experimental-strip-types --input-type=module <<'NODE' | tee "$compose_log"
          import { execFileSync } from "node:child_process";
          import { copyFile, mkdir, readdir } from "node:fs/promises";
          import { join } from "node:path";
          import { composeUpstreamQuestionnaireArtifact } from "/workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts";

          const source = "/base-red";
          const artifact = "/artifact";
          const patches = [
            { file: "019-notes-existing-view-tests.patch", target: "tests/questionnaire-view.test.ts", preimage: "eac8474634620cd723ed8775a5c19f3e7f6fe35a", postimage: "bafb34ee73d201eaaedceda404efe874e461d331" },
            { file: "021-notes-stale-multi-driver.patch", target: "tests/questionnaire-view.test.ts", preimage: "bafb34ee73d201eaaedceda404efe874e461d331", postimage: "c84be45f48c13771135a698db4f1a6a79f4be13e" },
            { file: "020-notes-view.patch", target: "lib/questionnaire/questionnaire-view.ts", preimage: "185fd41bbee0de644fdcc7d493db62e0ff9484ae", postimage: "e9269108f2fc2644a7bfee638f155836b3ddb4d1" },
          ];
          const manifest = {
            schema: "gentle-pi.hosted-upstream-questionnaire-composition/v2",
            derivedDirectory: "derived",
            patchDirectory: "patches/questionnaire-upstream",
            patchInventory: patches,
            allowedTargets: ["tests/questionnaire-view.test.ts", "lib/questionnaire/questionnaire-view.ts"],
            protectedTargets: ["lib/native-fullscreen-interaction.ts"],
          };
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          const expectedByTarget = new Map();
          for (const patch of patches) {
            const expected = expectedByTarget.get(patch.target)
              ?? execFileSync("git", ["-C", source, "hash-object", "--no-filters", "--", patch.target], { encoding: "utf8" }).trim();
            fail(expected === patch.preimage, `notes view GREEN chain preimage changed: ${patch.file}`);
            expectedByTarget.set(patch.target, patch.postimage);
            const changed = execFileSync("git", ["-C", source, "apply", "--numstat", "--summary", join("/workspace/tests/fixtures/upstream-questionnaire/patches", patch.file)], { encoding: "utf8" });
            const changedPaths = changed.split("\n").filter((line) => line.includes("\t")).map((line) => line.slice(line.lastIndexOf("\t") + 1)).filter(Boolean);
            fail(changedPaths.length === 1 && changedPaths[0] === patch.target, `notes view GREEN changed path mismatch: ${patch.file}`);
          }
          const patchRoot = join(artifact, manifest.patchDirectory);
          const outputRoot = join(artifact, manifest.derivedDirectory);
          await mkdir(patchRoot, { recursive: true });
          for (const patch of patches) await copyFile(join("/workspace/tests/fixtures/upstream-questionnaire/patches", patch.file), join(patchRoot, patch.file));
          fail(JSON.stringify((await readdir(patchRoot)).sort()) === JSON.stringify(patches.map((patch) => patch.file).sort()), "notes view GREEN staged patch set changed");
          await composeUpstreamQuestionnaireArtifact({ sourceRoot: source, patchRoot, outputRoot, manifest });
          console.log(`notes-view-green-proof root=${outputRoot} patches=${patches.map((patch) => patch.file).join(",")} postimages=${patches.map((patch) => patch.postimage).join(",")}`);
          NODE
          grep -Fx "notes-view-green-proof root=/artifact/derived patches=019-notes-existing-view-tests.patch,021-notes-stale-multi-driver.patch,020-notes-view.patch postimages=bafb34ee73d201eaaedceda404efe874e461d331,c84be45f48c13771135a698db4f1a6a79f4be13e,e9269108f2fc2644a7bfee638f155836b3ddb4d1" "$compose_log" >/dev/null

      - name: Run UQA-06a notes view GREEN in read-only isolation
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          derived_dir="$RUNNER_TEMP/questionnaire-upstream-notes-view-green/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=2g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /derived "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            NODE_OPTIONS=--max-old-space-size=1536 GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              if touch /derived/.notes-view-green-read-only-probe 2>/dev/null; then exit 1; fi
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ESNext --module ESNext --moduleResolution bundler \
                --allowImportingTsExtensions --lib ESNext,DOM --typeRoots /acquired-upstream/node_modules/@types --types node \
                /derived/extensions/ask-user-question.ts /derived/lib/questionnaire/questionnaire-view.ts \
                /derived/tests/ask-user-question.test.ts /derived/tests/questionnaire-view.test.ts
              /usr/local/bin/node /derived/scripts/check-types.mjs
              ask_log=/tmp/uqa06a-notes-view-green-ask.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/ask-user-question.test.ts /derived/tests/gentle-ai.test.ts >"$ask_log" 2>&1
              ask_status=$?
              set -e
              cat "$ask_log"
              test "$ask_status" -eq 0
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$ask_log")" -eq 98
              test "$(grep -Ec "^ok [0-9]+ - " "$ask_log")" -eq 98
              test "$(grep -Ec "^not ok [0-9]+ - " "$ask_log")" -eq 0
              grep -Fx "1..98" "$ask_log" >/dev/null
              grep -Fx "# tests 105" "$ask_log" >/dev/null
              grep -Fx "# pass 105" "$ask_log" >/dev/null
              grep -Fx "# fail 0" "$ask_log" >/dev/null
              grep -Fx "# cancelled 0" "$ask_log" >/dev/null
              grep -Fx "# skipped 0" "$ask_log" >/dev/null
              grep -Fx "# todo 0" "$ask_log" >/dev/null
              view_log=/tmp/uqa06a-notes-view-green-view.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/questionnaire-view.test.ts >"$view_log" 2>&1
              view_status=$?
              set -e
              cat "$view_log"
              test "$view_status" -eq 0
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$view_log")" -eq 30
              test "$(grep -Ec "^ok [0-9]+ - " "$view_log")" -eq 30
              test "$(grep -Ec "^not ok [0-9]+ - " "$view_log")" -eq 0
              grep -Fx "1..30" "$view_log" >/dev/null
              grep -Fx "# tests 30" "$view_log" >/dev/null
              grep -Fx "# pass 30" "$view_log" >/dev/null
              grep -Fx "# fail 0" "$view_log" >/dev/null
              grep -Fx "# cancelled 0" "$view_log" >/dev/null
              grep -Fx "# skipped 0" "$view_log" >/dev/null
              grep -Fx "# todo 0" "$view_log" >/dev/null
              cat > /tmp/uqa06a-notes-view-green-names <<EOF
          renders question and final global note rows before Submit and Cancel
          successful submit attaches a trimmed question note and global note
          note drafts survive tab switches and cancellation preserves committed notes
          omits whitespace-only notes and keeps a global note on answerless cancellation
          multi-select submit uses the latest toggles after an earlier commit
          failed submit leaves synthesized multi answers uncommitted
          EOF
              while IFS= read -r name; do grep -Eq "^ok [0-9]+ - ${name}$" "$view_log"; done < /tmp/uqa06a-notes-view-green-names
            '

      - name: Compose UQA-06b notes producer RED artifact offline
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          source_dir="$RUNNER_TEMP/questionnaire-upstream-notes-view-green/derived"
          red_dir="$RUNNER_TEMP/questionnaire-upstream-notes-producer-red"
          test -d "$source_dir"
          test ! -e "$red_dir"
          mkdir "$red_dir"
          chmod 0777 "$red_dir"
          compose_log="$RUNNER_TEMP/questionnaire-upstream-notes-producer-red-composition.log"
          docker run --rm -i --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$source_dir,target=/base-green,readonly" \
            --mount "type=bind,source=$red_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /usr/local/bin/node --experimental-strip-types --input-type=module <<'NODE' | tee "$compose_log"
          import { execFileSync } from "node:child_process";
          import { copyFile, mkdir, readdir } from "node:fs/promises";
          import { join } from "node:path";
          import { composeUpstreamQuestionnaireArtifact } from "/workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts";

          const source = "/base-green";
          const artifact = "/artifact";
          const patch = { file: "022-notes-producer-tests.patch", target: "tests/ask-user-question.test.ts", preimage: "a09fb580406dff77e7093863b36c658a384393b4", postimage: "55149f3a1656b0eea71d6b6005f4a22113529662" };
          const manifest = {
            schema: "gentle-pi.hosted-upstream-questionnaire-composition/v2",
            derivedDirectory: "derived",
            patchDirectory: "patches/questionnaire-upstream",
            patchInventory: [patch],
            allowedTargets: [patch.target],
            protectedTargets: ["lib/native-fullscreen-interaction.ts"],
          };
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          fail(execFileSync("git", ["-C", source, "hash-object", "--no-filters", "--", patch.target], { encoding: "utf8" }).trim() === patch.preimage, "notes producer RED preimage changed");
          const changed = execFileSync("git", ["-C", source, "apply", "--numstat", "--summary", join("/workspace/tests/fixtures/upstream-questionnaire/patches", patch.file)], { encoding: "utf8" });
          const changedPaths = changed.split("\n").filter((line) => line.includes("\t")).map((line) => line.slice(line.lastIndexOf("\t") + 1)).filter(Boolean);
          fail(changedPaths.length === 1 && changedPaths[0] === patch.target, "notes producer RED changed path mismatch");
          const patchRoot = join(artifact, manifest.patchDirectory);
          const outputRoot = join(artifact, manifest.derivedDirectory);
          await mkdir(patchRoot, { recursive: true });
          await copyFile(join("/workspace/tests/fixtures/upstream-questionnaire/patches", patch.file), join(patchRoot, patch.file));
          fail(JSON.stringify(await readdir(patchRoot)) === JSON.stringify([patch.file]), "notes producer RED staged patch set changed");
          await composeUpstreamQuestionnaireArtifact({ sourceRoot: source, patchRoot, outputRoot, manifest });
          console.log(`notes-producer-red-proof root=${outputRoot} base=${patch.preimage} patch=${patch.file} postimage=${patch.postimage}`);
          NODE
          grep -Fx "notes-producer-red-proof root=/artifact/derived base=a09fb580406dff77e7093863b36c658a384393b4 patch=022-notes-producer-tests.patch postimage=55149f3a1656b0eea71d6b6005f4a22113529662" "$compose_log" >/dev/null

      - name: Run UQA-06b notes producer RED in read-only isolation
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          derived_dir="$RUNNER_TEMP/questionnaire-upstream-notes-producer-red/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=2g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /derived "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            NODE_OPTIONS=--max-old-space-size=1536 GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              if touch /derived/.notes-producer-red-read-only-probe 2>/dev/null; then exit 1; fi
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ESNext --module ESNext --moduleResolution bundler \
                --allowImportingTsExtensions --lib ESNext,DOM --typeRoots /acquired-upstream/node_modules/@types --types node \
                /derived/extensions/ask-user-question.ts /derived/lib/questionnaire/questionnaire-view.ts \
                /derived/tests/ask-user-question.test.ts
              /usr/local/bin/node /derived/scripts/check-types.mjs
              log=/tmp/uqa06b-notes-producer-red.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/ask-user-question.test.ts /derived/tests/gentle-ai.test.ts >"$log" 2>&1
              status=$?
              set -e
              cat "$log"
              test "$status" -ne 0
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$log")" -eq 101
              test "$(grep -Ec "^ok [0-9]+ - " "$log")" -eq 98
              test "$(grep -Ec "^not ok [0-9]+ - " "$log")" -eq 3
              grep -Fx "1..101" "$log" >/dev/null
              grep -Fx "# tests 108" "$log" >/dev/null
              grep -Fx "# pass 105" "$log" >/dev/null
              grep -Fx "# fail 3" "$log" >/dev/null
              grep -Fx "# cancelled 0" "$log" >/dev/null
              grep -Fx "# skipped 0" "$log" >/dev/null
              grep -Fx "# todo 0" "$log" >/dev/null
              cat > /tmp/uqa06b-notes-producer-red-names <<EOF
          ask_user_question preserves answer notes and a global note on success
          ask_user_question preserves answer notes and a global note on partial cancellation
          ask_user_question preserves a global note on answerless cancellation
          EOF
              while IFS= read -r name; do grep -Eq "^not ok [0-9]+ - ${name}$" "$log"; done < /tmp/uqa06b-notes-producer-red-names
            '

      - name: Compose UQA-06b notes producer GREEN artifact offline
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          source_dir="$RUNNER_TEMP/questionnaire-upstream-notes-producer-red/derived"
          green_dir="$RUNNER_TEMP/questionnaire-upstream-notes-producer-green"
          test -d "$source_dir"
          test ! -e "$green_dir"
          mkdir "$green_dir"
          chmod 0777 "$green_dir"
          compose_log="$RUNNER_TEMP/questionnaire-upstream-notes-producer-green-composition.log"
          docker run --rm -i --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$source_dir,target=/base-red,readonly" \
            --mount "type=bind,source=$green_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /usr/local/bin/node --experimental-strip-types --input-type=module <<'NODE' | tee "$compose_log"
          import { execFileSync } from "node:child_process";
          import { copyFile, mkdir, readdir } from "node:fs/promises";
          import { join } from "node:path";
          import { composeUpstreamQuestionnaireArtifact } from "/workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts";

          const source = "/base-red";
          const artifact = "/artifact";
          const patch = { file: "023-notes-producer.patch", target: "extensions/ask-user-question.ts", preimage: "716f3f03a4fc42b2f9958cfccb425a23ec2fe1c9", postimage: "236ce72f8147a24041c28195235a56ec500256f0" };
          const manifest = {
            schema: "gentle-pi.hosted-upstream-questionnaire-composition/v2",
            derivedDirectory: "derived",
            patchDirectory: "patches/questionnaire-upstream",
            patchInventory: [patch],
            allowedTargets: [patch.target],
            protectedTargets: ["lib/native-fullscreen-interaction.ts"],
          };
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          fail(execFileSync("git", ["-C", source, "hash-object", "--no-filters", "--", patch.target], { encoding: "utf8" }).trim() === patch.preimage, "notes producer GREEN preimage changed");
          const changed = execFileSync("git", ["-C", source, "apply", "--numstat", "--summary", join("/workspace/tests/fixtures/upstream-questionnaire/patches", patch.file)], { encoding: "utf8" });
          const changedPaths = changed.split("\n").filter((line) => line.includes("\t")).map((line) => line.slice(line.lastIndexOf("\t") + 1)).filter(Boolean);
          fail(changedPaths.length === 1 && changedPaths[0] === patch.target, "notes producer GREEN changed path mismatch");
          const patchRoot = join(artifact, manifest.patchDirectory);
          const outputRoot = join(artifact, manifest.derivedDirectory);
          await mkdir(patchRoot, { recursive: true });
          await copyFile(join("/workspace/tests/fixtures/upstream-questionnaire/patches", patch.file), join(patchRoot, patch.file));
          fail(JSON.stringify(await readdir(patchRoot)) === JSON.stringify([patch.file]), "notes producer GREEN staged patch set changed");
          await composeUpstreamQuestionnaireArtifact({ sourceRoot: source, patchRoot, outputRoot, manifest });
          console.log(`notes-producer-green-proof root=${outputRoot} base=${patch.preimage} patch=${patch.file} postimage=${patch.postimage}`);
          NODE
          grep -Fx "notes-producer-green-proof root=/artifact/derived base=716f3f03a4fc42b2f9958cfccb425a23ec2fe1c9 patch=023-notes-producer.patch postimage=236ce72f8147a24041c28195235a56ec500256f0" "$compose_log" >/dev/null

      - name: Run UQA-06b notes producer GREEN in read-only isolation
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          derived_dir="$RUNNER_TEMP/questionnaire-upstream-notes-producer-green/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=2g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /derived "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            NODE_OPTIONS=--max-old-space-size=1536 GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              if touch /derived/.notes-producer-green-read-only-probe 2>/dev/null; then exit 1; fi
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ESNext --module ESNext --moduleResolution bundler \
                --allowImportingTsExtensions --lib ESNext,DOM --typeRoots /acquired-upstream/node_modules/@types --types node \
                /derived/extensions/ask-user-question.ts /derived/lib/questionnaire/questionnaire-view.ts \
                /derived/tests/ask-user-question.test.ts /derived/tests/questionnaire-view.test.ts
              /usr/local/bin/node /derived/scripts/check-types.mjs
              ask_log=/tmp/uqa06b-notes-producer-green-ask.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/ask-user-question.test.ts /derived/tests/gentle-ai.test.ts >"$ask_log" 2>&1
              ask_status=$?
              set -e
              cat "$ask_log"
              test "$ask_status" -eq 0
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$ask_log")" -eq 101
              test "$(grep -Ec "^ok [0-9]+ - " "$ask_log")" -eq 101
              test "$(grep -Ec "^not ok [0-9]+ - " "$ask_log")" -eq 0
              grep -Fx "1..101" "$ask_log" >/dev/null
              grep -Fx "# tests 108" "$ask_log" >/dev/null
              grep -Fx "# pass 108" "$ask_log" >/dev/null
              grep -Fx "# fail 0" "$ask_log" >/dev/null
              grep -Fx "# cancelled 0" "$ask_log" >/dev/null
              grep -Fx "# skipped 0" "$ask_log" >/dev/null
              grep -Fx "# todo 0" "$ask_log" >/dev/null
              cat > /tmp/uqa06b-notes-producer-green-names <<EOF
          ask_user_question preserves answer notes and a global note on success
          ask_user_question preserves answer notes and a global note on partial cancellation
          ask_user_question preserves a global note on answerless cancellation
          ask_user_question keeps the native blocked lifecycle private and balanced
          actual questionnaire producer drives Herdr through a private balanced compatibility lifecycle
          EOF
              while IFS= read -r name; do grep -Eq "^ok [0-9]+ - ${name}$" "$ask_log"; done < /tmp/uqa06b-notes-producer-green-names
              view_log=/tmp/uqa06b-notes-producer-green-view.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/questionnaire-view.test.ts >"$view_log" 2>&1
              view_status=$?
              set -e
              cat "$view_log"
              test "$view_status" -eq 0
              grep -Fx "1..30" "$view_log" >/dev/null
              grep -Fx "# tests 30" "$view_log" >/dev/null
              grep -Fx "# pass 30" "$view_log" >/dev/null
              grep -Fx "# fail 0" "$view_log" >/dev/null
              grep -Fx "# cancelled 0" "$view_log" >/dev/null
              grep -Fx "# skipped 0" "$view_log" >/dev/null
              grep -Fx "# todo 0" "$view_log" >/dev/null
            '

      - name: Run pre-correction controls contract in read-only isolation
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          green_dir="$RUNNER_TEMP/questionnaire-upstream-controls-green"
          derived_dir="$green_dir/controls-green/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=2g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /derived "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            NODE_OPTIONS=--max-old-space-size=1536 GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              if touch /derived/.controls-green-test-read-only-probe 2>/dev/null; then exit 1; fi
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ESNext --module ESNext --moduleResolution bundler \
                --allowImportingTsExtensions --lib ESNext,DOM --typeRoots /acquired-upstream/node_modules/@types --types node \
                /derived/extensions/ask-user-question.ts /derived/lib/questionnaire/questionnaire-view.ts \
                /derived/tests/ask-user-question.test.ts /derived/tests/questionnaire-view.test.ts
              /usr/local/bin/node /derived/scripts/check-types.mjs
              ask_log=/tmp/uqa05-green-ask.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/ask-user-question.test.ts /derived/tests/gentle-ai.test.ts >"$ask_log" 2>&1
              ask_status=$?
              set -e
              cat "$ask_log"
              test "$ask_status" -ne 0
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$ask_log")" -eq 98
              test "$(grep -Ec "^ok [0-9]+ - " "$ask_log")" -eq 93
              test "$(grep -Ec "^not ok [0-9]+ - " "$ask_log")" -eq 5
              grep -Fx "1..98" "$ask_log" >/dev/null
              grep -Fx "# tests 105" "$ask_log" >/dev/null
              grep -Fx "# pass 100" "$ask_log" >/dev/null
              grep -Fx "# fail 5" "$ask_log" >/dev/null
              grep -Fx "# cancelled 0" "$ask_log" >/dev/null
              grep -Fx "# skipped 0" "$ask_log" >/dev/null
              grep -Fx "# todo 0" "$ask_log" >/dev/null
              cat > /tmp/uqa05-pre-correction-ask-failures <<EOF
          ask_user_question commits a single-select answer end-to-end
          ask_user_question echoes an option preview beside the answer
          ask_user_question commits a multiSelect answer with every toggled option
          ask_user_question commits a free-text custom answer
          ask_user_question keeps toggled options in a multiSelect custom answer
          EOF
              while IFS= read -r name; do grep -Eq "^not ok [0-9]+ - ${name}$" "$ask_log"; done < /tmp/uqa05-pre-correction-ask-failures
              cat > /tmp/uqa05-green-ask-names <<EOF
          ask_user_question submits an empty multi-select through explicit Submit
          ask_user_question returns partial answers for committed options on cancellation
          ask_user_question preserves custom and multi answers on partial cancellation
          ask_user_question omits uncommitted state on cancellation
          ask_user_question retains committed answers when a replacement draft is cancelled
          ask_user_question reports cancellation without answers
          ask_user_question keeps the native blocked lifecycle private and balanced
          ask_user_question returns empty cancellation before mounting an already-aborted request
          ask_user_question aborts a mounted questionnaire with committed partial answers
          ask_user_question aborts during a custom draft without committing the draft
          ask_user_question abort wins over later submit, cancel, and repeated abort
          ask_user_question keeps a completed submit ahead of later abort and cancel
          ask_user_question keeps a completed cancel ahead of later abort and submit
          ask_user_question closes before a deferred custom factory can mount a dock
          ask_user_question removes abort listeners while preserving a custom UI rejection
          actual questionnaire producer drives Herdr through a private balanced compatibility lifecycle
          EOF
              while IFS= read -r name; do
                test -n "$name"
                grep -Eq "^ok [0-9]+ - ${name}$" "$ask_log"
              done < /tmp/uqa05-green-ask-names
              view_log=/tmp/uqa05-green-view.tap
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/questionnaire-view.test.ts >"$view_log" 2>&1
              tail -n 160 "$view_log"
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$view_log")" -eq 23
              test "$(grep -Ec "^ok [0-9]+ - " "$view_log")" -eq 23
              test "$(grep -Ec "^not ok [0-9]+ - " "$view_log")" -eq 0
              grep -Fx "1..23" "$view_log" >/dev/null
              grep -Fx "# tests 23" "$view_log" >/dev/null
              grep -Fx "# pass 23" "$view_log" >/dev/null
              grep -Fx "# fail 0" "$view_log" >/dev/null
              grep -Fx "# cancelled 0" "$view_log" >/dev/null
              grep -Fx "# skipped 0" "$view_log" >/dev/null
              grep -Fx "# todo 0" "$view_log" >/dev/null
              cat > /tmp/uqa05-green-view-names <<EOF
          renders explicit Submit and Cancel rows after the custom row
          selecting an option requires explicit submit and cancel remains independently reachable
          multi-select submit preserves an empty selection and cancel remains answerless
          real key sequences drive the cursor and commit in legacy and application-cursor form
          the injected KeybindingsManager drives the same navigation and commit
          single-select commits on Enter and advances to the next unanswered question
          multi-select toggles with space and requires a non-empty selection to commit
          pointer press focuses a row and click commits it
          pointer click toggles a multi-select option instead of committing
          EOF
              while IFS= read -r name; do
                test -n "$name"
                grep -Eq "^ok [0-9]+ - ${name}$" "$view_log"
              done < /tmp/uqa05-green-view-names
            '

      - name: Compose UQA-07a bindings editor RED artifact offline
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          source_dir="$RUNNER_TEMP/questionnaire-upstream-notes-producer-green/derived"
          red_dir="$RUNNER_TEMP/questionnaire-upstream-bindings-editor-red"
          test -d "$source_dir"
          test ! -e "$red_dir"
          mkdir "$red_dir"
          chmod 0777 "$red_dir"
          compose_log="$RUNNER_TEMP/questionnaire-upstream-bindings-editor-red-composition.log"
          docker run --rm -i --network=none --read-only \
            --mount "type=bind,source=$GITHUB_WORKSPACE,target=/workspace,readonly" \
            --mount "type=bind,source=$source_dir,target=/base-green,readonly" \
            --mount "type=bind,source=$red_dir,target=/artifact" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=1g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /workspace "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null \
            GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /usr/local/bin/node --experimental-strip-types --input-type=module <<'NODE' | tee "$compose_log"
          import { execFileSync } from "node:child_process";
          import { copyFile, mkdir, readdir } from "node:fs/promises";
          import { join } from "node:path";
          import { composeUpstreamQuestionnaireArtifact } from "/workspace/tests/fixtures/hosted-compose-upstream-questionnaire.ts";

          const source = "/base-green";
          const artifact = "/artifact";
          const patch = { file: "024-bindings-editor-tests.patch", target: "tests/questionnaire-view.test.ts", preimage: "c84be45f48c13771135a698db4f1a6a79f4be13e", postimage: "2c94588545908d1c1bab18e5f0056b3dfdc354f4" };
          const manifest = {
            schema: "gentle-pi.hosted-upstream-questionnaire-composition/v2",
            derivedDirectory: "derived",
            patchDirectory: "patches/questionnaire-upstream",
            patchInventory: [patch],
            allowedTargets: [patch.target],
            protectedTargets: ["lib/native-fullscreen-interaction.ts"],
          };
          const fail = (condition, message) => { if (!condition) throw new Error(message); };
          fail(execFileSync("git", ["-C", source, "hash-object", "--no-filters", "--", patch.target], { encoding: "utf8" }).trim() === patch.preimage, "bindings editor RED preimage changed");
          const patchPath = join("/workspace/tests/fixtures/upstream-questionnaire/patches", patch.file);
          const changed = execFileSync("git", ["-C", source, "apply", "--numstat", "--summary", patchPath], { encoding: "utf8" });
          const changedPaths = changed.split("\n").filter((line) => line.includes("\t")).map((line) => line.slice(line.lastIndexOf("\t") + 1)).filter(Boolean);
          fail(changedPaths.length === 1 && changedPaths[0] === patch.target, "bindings editor RED changed path mismatch");
          const patchRoot = join(artifact, manifest.patchDirectory);
          const outputRoot = join(artifact, manifest.derivedDirectory);
          await mkdir(patchRoot, { recursive: true });
          await copyFile(patchPath, join(patchRoot, patch.file));
          fail(JSON.stringify(await readdir(patchRoot)) === JSON.stringify([patch.file]), "bindings editor RED staged patch set changed");
          await composeUpstreamQuestionnaireArtifact({ sourceRoot: source, patchRoot, outputRoot, manifest });
          console.log(`bindings-editor-red-proof root=${outputRoot} base=${patch.preimage} patch=${patch.file} postimage=${patch.postimage}`);
          NODE
          grep -Fx "bindings-editor-red-proof root=/artifact/derived base=c84be45f48c13771135a698db4f1a6a79f4be13e patch=024-bindings-editor-tests.patch postimage=2c94588545908d1c1bab18e5f0056b3dfdc354f4" "$compose_log" >/dev/null

      - name: Run UQA-07a bindings editor RED in read-only isolation
        env:
          IMAGE_REF: ${{ steps.image.outputs.ref }}
          IMAGE_ID: ${{ steps.image.outputs.id }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(docker image inspect --format '{{.Id}}' "$IMAGE_REF")" = "$IMAGE_ID"
          acquisition_dir="$RUNNER_TEMP/questionnaire-upstream-acquisition"
          derived_dir="$RUNNER_TEMP/questionnaire-upstream-bindings-editor-red/derived"
          test -d "$derived_dir"
          docker run --rm --network=none --read-only \
            --mount "type=bind,source=$derived_dir,target=/derived,readonly" \
            --mount "type=bind,source=$acquisition_dir/upstream,target=/acquired-upstream,readonly" \
            --tmpfs /tmp:rw,nosuid,nodev,size=256m --cpus=1 --memory=2g --pids-limit=256 --cap-drop=ALL \
            --security-opt=no-new-privileges --user 1000:1000 -w /derived "$IMAGE_REF" \
            env -i PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin HOME=/tmp/hosted-home TMPDIR=/tmp \
            NODE_OPTIONS=--max-old-space-size=1536 GENTLE_PI_HOSTED_ISOLATION=docker-network-none-readonly-v1 \
            GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_SYSTEM=/dev/null GIT_OPTIONAL_LOCKS=0 GIT_TERMINAL_PROMPT=0 \
            /bin/sh -eu -c '
              if touch /derived/.bindings-editor-red-read-only-probe 2>/dev/null; then exit 1; fi
              /usr/local/bin/node /acquired-upstream/node_modules/typescript/bin/tsc \
                --noEmit --strict --skipLibCheck --target ESNext --module ESNext --moduleResolution bundler \
                --allowImportingTsExtensions --lib ESNext,DOM --typeRoots /acquired-upstream/node_modules/@types --types node \
                /derived/extensions/ask-user-question.ts /derived/lib/questionnaire/questionnaire-view.ts \
                /derived/tests/ask-user-question.test.ts /derived/tests/questionnaire-view.test.ts
              /usr/local/bin/node /derived/scripts/check-types.mjs
              log=/tmp/uqa07a-bindings-editor-red.tap
              set +e
              /usr/local/bin/node --experimental-strip-types --test --test-reporter=tap /derived/tests/questionnaire-view.test.ts >"$log" 2>&1
              status=$?
              set -e
              cat "$log"
              test "$status" -ne 0
              test "$(grep -Ec "^(ok|not ok) [0-9]+ - " "$log")" -eq 36
              test "$(grep -Ec "^ok [0-9]+ - " "$log")" -eq 32
              test "$(grep -Ec "^not ok [0-9]+ - " "$log")" -eq 4
              grep -Fx "1..36" "$log" >/dev/null
              grep -Fx "# tests 36" "$log" >/dev/null
              grep -Fx "# pass 32" "$log" >/dev/null
              grep -Fx "# fail 4" "$log" >/dev/null
              grep -Fx "# cancelled 0" "$log" >/dev/null
              grep -Fx "# skipped 0" "$log" >/dev/null
              grep -Fx "# todo 0" "$log" >/dev/null
              cat > /tmp/uqa07a-bindings-editor-red-failures <<EOF
          default editor keys create multiline custom text and visible Submit commits the preserved draft
          remapped newline submit and tab bindings preserve a multiline draft across questions
          newline wins when configured newline and submit bindings collide
          the multiline editor honors configured editing bindings before custom-answer commit
          EOF
              while IFS= read -r name; do grep -Eq "^not ok [0-9]+ - ${name}$" "$log"; done < /tmp/uqa07a-bindings-editor-red-failures
              grep -Eq "^ok [0-9]+ - configured select bindings remap navigation and explicit disabled arrays do not fall back$" "$log"
              grep -Eq "^ok [0-9]+ - explicit disabled input bindings leave Enter newline aliases and Tab with the editor$" "$log"
            '

      - name: Verify final hosted candidate checkout cleanliness
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"
