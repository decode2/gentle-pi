diff --git a/tests/ask-user-question.test.ts b/tests/ask-user-question.test.ts
index 55149f3a1656b0eea71d6b6005f4a22113529662..0361e28618c1cf797a98b1491602e431532d0009 100644
--- a/tests/ask-user-question.test.ts
+++ b/tests/ask-user-question.test.ts
@@ -300,7 +300,7 @@ test("ask_user_question commits a free-text custom answer", async () => {
 	const result = await run(
 		tool,
 		{ questions: single() },
-		tuiContext(["\x1b[B", "\x1b[B", "\r", "custom text", "\r", "\r"]),
+		tuiContext(["\x1b[B", "\x1b[B", "\r", "custom text", "\x1b", "\x1b[B", "\x1b[B", "\x1b[B", "\r"]),
 	);
 
 	assert.equal(result.content[0]?.text, "1. Proceed? — (custom) custom text");
@@ -315,11 +315,11 @@ test("ask_user_question keeps toggled options in a multiSelect custom answer", a
 		{ question: "Pick?", header: "Pick", options: [option("One"), option("Two")], multiSelect: true },
 	];
 
-	// Toggle One, move to the custom row, open the editor, type, and submit.
+	// Toggle One, move to the custom row, preserve the draft, and use visible Submit.
 	const result = await run(
 		tool,
 		{ questions },
-		tuiContext([" ", "\x1b[B", "\x1b[B", "\r", "free note", "\r", "\r"]),
+		tuiContext([" ", "\x1b[B", "\x1b[B", "\r", "free note", "\x1b", "\x1b[B", "\x1b[B", "\x1b[B", "\r"]),
 	);
 
 	assert.equal(result.content[0]?.text, "1. Pick? — (custom) free note — selected: One");
