# Published Gentle-Shell manual smoke — native OS evidence

**Status:** Native artifact preflight and npm install smoke precede proposed core Engram member validation. If this unit's native CI passes, Darwin will stage a disposable verified member and Windows will inspect it in memory without a binary handoff; neither is native evidence yet. Installed npm package bytes remain unverified. No Engram binary is executed, and no postinstall, setup, launcher, Pi RPC/TUI, or Ready claim is enabled.

**Scope:** User selected a fast manual, separate-home path before any real install. The Linux disposable run of published `gentle-pi@3.7.0` with Pi `0.85.1` and official core Engram `v2.1.0` completed first setup, returned a Pi RPC state response, and rendered an interactive TUI editor. It did not authenticate a model. Do not extrapolate to Darwin or Windows.

## Prior unit: native artifact preflight

- A fork draft PR runs on native `macos-latest` and `windows-latest` with `contents: read` only.
- Stage published `gentle-pi@3.7.0` and `@earendil-works/pi-coding-agent@0.85.1` tarballs using `npm pack --ignore-scripts`; verify their registry SRI, including Gentle-Shell's exact SHA-512 SRI: `sha512-SXBp9jIRnVIcOsLCW/Zw4XDTXxhViXNxrCZS7LyioB6kdRl99Ohojeaj+yIH5+K/ucwLlTlHYBz8zwue9aspNQ==`.
- Resolve official core Engram `v2.1.0` separately. Require the exact native OS/architecture asset, release API SHA-256 digest, pinned release digest, and matching row in the verified official `checksums.txt`; keep verified archives in runner-disposable scratch.
- This unit does not install or unpack packages, execute any lifecycle script, postinstall, setup, or launcher, access real user config, run a Go source build, or claim setup/RPC/TUI/Ready evidence. It is artifact-integrity evidence only; runtime smoke evidence belongs to a later unit.

## Prior unit: bounded native artifact-preflight failure diagnosis

- PR#8 run `36081864141` attempts 1–2: Windows npm smoke passed; macOS failed before npm. PR#9 run `36085123196` produced the safe category `release-metadata-fetch-rate-limited` on macOS; Windows passed. This identifies anonymous API throttling, not a particular HTTP status or permission to skip the live release digest.
- Closed mock-tested failure codes distinguish staged npm read/digest, release metadata/checksums/native archive fetch, release digest/checksum mismatch, and unknown; only fixed HTTP/transport classes are displayed. Never emit paths, URLs, argv, raw errors, headers, bodies, or metadata. Preserve all original SRI and release digest/checksum gates.
- Sanitized preflight tests passed 12/12; parent-run `env -i` scratch passed all 20 artifact/staging tests. No local npm install/config/product runtime was run. Roll back only this diagnostic script/tests/section; preserve earlier receipts.

## Prior unit: isolated native npm install smoke only

- After artifact preflight passes, create owner-only runner-disposable scratch with real non-symlink ancestors, bounded-reverify both staged archive SRIs, and invoke the exact npm CLI bundled beside setup-node's `process.execPath`. Use only the two staged archives as package inputs, `--ignore-scripts`, the isolated prefix/config/cache/temp, and an allowlisted environment; never forward host PATH, credentials, or NODE_OPTIONS. Suppress npm output and report a fixed failure category.
- Success means only `npm install` returned zero. The staged paths can change between SRI verification and npm reopening them; installed bytes are unverified. Do not extract archives, read installed manifests or locks, claim package-content identity, execute lifecycle/postinstall, set up Go, execute a binary, run setup/launcher/Pi RPC/TUI, or claim Ready.
- Focused tests establish RED before GREEN for Node/npm CLI layout selection, missing/symlinked/oversized scratch inputs, exact arguments, and environment isolation. Draft #8 run `36081864141` passed the Windows npm smoke twice; macOS failed in artifact preflight before reaching npm.
- Rollback: revert only the npm-smoke workflow/script/tests and that unit's record text; preserve preceding artifact-preflight receipts.

## This unit: platform-specific official core Engram inspection/staging

- On both native OSes, bounded-read the selected official core `Gentleman-Programming/engram` v2.1.0 archive from approved preflight scratch, recheck its exact pinned SHA-256, and pass that same buffer to the native OS `tar` reader on stdin. Never reopen the archive path or use npm Gentle Engram. Validate the exact pinned manifest and regular root member (`engram` on Darwin, `engram.exe` on Windows); reject unsafe/duplicate/missing members, non-regular members including symlinks, and output over the fixed bound.
- Windows stops after validating the selected member in memory: create no staging directory or file, change no ACL, and provide no executable path or binary handoff. Darwin alone writes to a fresh disposable scratch bin and compares the bounded readback digest to the extracted buffer. Darwin file staging assumes the hosted runner scratch is single-user and non-adversarial; path checks are defense-in-depth, not race-free isolation or managed Ready evidence. Retain only fixed safe diagnostics; never execute the binary.
- Checked official v2.1.0 manifests (SHA-256 pins already in preflight): Darwin amd64 `2c8f56f36c6779b1c0f5f56bf9339ed121192218272e4fc144a8ad7bed2da22c` and arm64 `b9167999ba6deca652e367bd7d44766afa33430ab93b01f7cc0be42e6364d806` each contain exactly regular files `tools/cloud-sync-projects.ps1` (4,980 bytes), `tools/cloud-sync-projects.sh` (3,340 bytes), and `engram` (20,973,344 / 19,819,762 bytes respectively). Windows amd64 `342ace84c1a716c5e6cd969ed92fe63c8b00304cd8bd1bd6455247db4f9afc9c` and arm64 `cdd2ec6e718140cb2d67e0170151baecba63fdcbaa09ef23107459aa2cce695e` each contain exactly regular files with Unix mode 100755: the same two tool scripts and `engram.exe` (21,125,632 / 19,554,304 bytes respectively). No directory or symlink members were observed. Inspection used verified in-memory buffers and listing/metadata only; no member was executed.
- Add this validation after artifact verification. Strict RED-before-GREEN pure tests cover Windows memory-only disposition, exact member selection, unsafe names, duplicate/missing/symlink entries, Darwin scratch ancestor/leaf safety, oversize rejection, and byte identity. All 21 focused Node 24 tests passed under disposable HOME/TMPDIR. Keep the work-unit diff under 400 lines. Windows ZIP-on-stdin and Darwin staging remain pending native CI proof; no Linux run establishes native success. The macOS PR #8 attempts currently fail earlier at artifact verification; diagnostics-03 owns that investigation, so do not attribute those failures to this validation.
- No model credentials, GitHub token, global install, package lifecycle/postinstall, Go build, product execution, setup, launcher, Pi RPC/TUI, or Ready claim. Rollback only this unit's workflow/script/tests and this scope record; preserve earlier receipts.

## Diagnostic follow-up: native member-validation failure

- Native run `36087132970` at PR#10 head `2c57c36cb8eeff6876d5d22d7e18004c7dff4a5f` on pinned parent PR#9 head `37c5ca7f`: both macOS and Windows passed official asset preflight, then failed `Validate official Engram member (Windows memory-only; Darwin scratch stage)`. Neither job executed npm, a product binary, or an Engram binary after that failure. The precise cause is unknown; do not infer tar flags, ACL behavior, or another platform-specific cause from the generic catch.
- Bounded diagnostic-only follow-up: use private branded failures and a closed reason-code whitelist so the CLI reports only `member-validation-<known-reason>` or `unknown`. Cover archive read/digest, native reader invocation/list/manifest/verbose/extract, Darwin private root/write/readback, and memory-only success. Tests use pure injected/mocked invalid inputs, hostile sentinel exceptions, and invalid CLI arguments only; do not download or invoke a real product or tar. Emit no path, member filename, URL, argv, raw tar stderr, OS error, HTTP detail, archive bytes, or metadata. Preserve every existing pinned SHA/manifest/symlink/size gate, Windows memory-only behavior (including no `icacls`), and Darwin's non-adversarial scratch-runner assumption. The native cause remains pending a safe diagnostic CI rerun.
- Native run `36089857003` at the exact PR#10 parent head `2e8d6db9c0a77f002b220893544d213d85784d68` failed on Windows with `member-validation-native-reader-list` and on macOS with `member-validation-native-reader-invocation`, each before npm or product execution. These fixed codes localize the failures to stage validation only; they do not establish a tar/ZIP reader or archive root cause.

## Diagnostic follow-up: native reader preconditions and output parsing

- Implemented a closed operation label (`list`, `verbose`, or `extract`) and private fixed `native-reader-<operation>-<condition>` codes for executable/cwd preconditions, spawn errors, nonzero status, and zero output. List and verbose output retain separate fixed invalid-UTF-8, empty, newline, and malformed codes.
- Initial strict RED observed 8/11 stage tests pass and 3 fail; GREEN passed 11/11 and expanded the prior 29/29 focused suite to 31/31. In the reviewer-requested phase-label follow-up, RED observed 9/12 pass and 3 fail: operation failures collapsed to generic codes, a hostile phase reached the injected spawn, and phase-specific nested codes were not whitelisted. GREEN passed 12/12 stage tests and all four suites passed 32/32. These use injected reader dependencies only; no native tar, archive, product, or binary was invoked.
- Historical interpretation: run `36089857003`'s `native-reader-list` and `native-reader-invocation` labels came from the prior catch-all path; the current stage path emits operation-qualified reader codes instead. Those earlier labels localize only to stage validation and do not establish a tar/ZIP cause. Native CI remains required before any success claim.
- Public output remains a closed reason code only: no raw stdout/stderr, paths, arguments, URLs, metadata, or error text. Only whitelisted branded nested stage reasons survive wrapping; arbitrary unbranded nested throws remain `unknown`.
- Preserved pinned digests/manifests, Windows archive-in-memory-only behavior (no executable staging or ACL changes), Darwin disposable scratch-only staging, and all existing gates. Native CI must pass before any success claim; neither current native failure proves an archive-format root cause.

## Diagnostic experiment: synthetic Windows ZIP stdin reader

- Native PR#11 run `36092622830` passed Windows artifact preflight, then failed `member-validation-native-reader-list-zero-output`: `System32\tar.exe -tf -` returned exit code zero with empty stdout for the official pinned Windows ZIP. This is the observed failure, not an established archive-reader root cause. The macOS preflight was separately blocked by anonymous release API rate limiting.
- External libarchive issue [#2288](https://github.com/libarchive/libarchive/issues/2288) reports that ZIP-over-stdin may omit members from a Node child process. It does not establish this incident or specifically explain empty stdout with exit zero.
- Add a minimal, independent Windows GitHub Actions fork-draft experiment (under 400 lines) that creates one tiny nonsecret synthetic ZIP containing text in runner-disposable temp, then compares `System32\tar.exe -tf <zipfile>` with `System32\tar.exe -tf -` using `child_process.spawnSync`; supply the ZIP `Buffer` directly as `input`, never via a PowerShell binary pipeline. Emit only fixed status categories such as path-reader-valid/invalid and stdin-exit/empty/nonempty/member-valid/member-invalid. No paths, arguments, stderr, member names, raw errors, or URLs. Use `permissions: {}`, no `GITHUB_TOKEN`, no checkout, and no npm/product/Engram artifact or binary. Guard execution to a draft PR in exactly `decode2/gentle-pi`; create no executable or marker and clean up only the generated fixture scratch.
- The experiment assumes a non-adversarial hosted runner scratch area and tests only the runner's synthetic ZIP behavior. Even if native CI reproduces empty stdin output, it does not prove why the official pinned ZIP behaved that way. Do not alter release-digest/managed-Ready gates or infer official archive compatibility from this fixture. A narrow static contract test may verify workflow containment without invoking Windows tar; native CI is the only behavioral receipt.
- GitHub Actions may echo the static, public `Run` source, including the synthetic fixture name and text; this is distinct from probe runtime output. The experiment does not execute Gentle-Pi product code or exercise its public typed diagnostic. The script can restrict runtime categories, but cannot prove category-only logging for Actions infrastructure or a PowerShell parse error that occurs before user code starts. Never claim the entire job log is category-only. If that becomes a requirement, stop for user interaction and do not publish; do not obfuscate the fixture or add a token to suppress source echo.

## Next unit: verify installed package bytes before postinstall

- In a separate bounded unit, verify every executable and imported file in the installed package against an immutable verified archive buffer, with bounded reads and real non-symlink ancestor checks. Only then may a later authorized scope execute postinstall and verify native Gentle AI provenance.
- Add independent RED/GREEN tests and stay below 400 diff lines. No package postinstall, Go, binary, setup, launcher, Pi RPC/TUI, or Ready evidence is part of this npm-smoke unit.

## Runtime evidence reserved for a later unit

- Use a native macOS runner and a native Windows runner with fresh disposable HOME/config/npm/cache/agent directories and an empty project cwd. No global npm install, user config, real account credentials, or published-package lifecycle script may execute before inspection.
- Pin the exact published Gentle-Shell archive and verify registry SRI. Before any postinstall, verify every installed executable/imported file against an immutable verified archive buffer with bounded reads and symlink-ancestor refusal. Only a separately reviewed later unit may then run the audited Gentle-Shell postinstall in the disposable prefix and verify the package-local Gentle AI binary and its pin before setup.
- Core Engram is distinct from the npm Gentle Engram package. The Darwin scratch binary is non-executed and must never enter a real user's PATH; this unit stages no Windows binary. Never forward a GitHub token to Gentle-Shell, Gentle AI, npm, Pi, or Engram.
- Do not rely on the published launcher's peer `package.json` lookup: Pi `0.85.1` does not export that path. Point `GENTLE_SHELL_PI` at the exact scratch Pi executable and prove the child preflight sees only that scratch command.
- Run one fresh separate-home launch after preflight. Assert setup completed and Pi RPC initialized independently. A provisioning marker, version, command exit, or RPC state response alone does not establish interactive Ready. Native TUI evidence must be identified separately; report it unverified if a reliable PTY is unavailable.
- Windows needs a known Go `1.25.10+` source-build toolchain for the pinned package-local Gentle AI. Preserve necessary Windows system environment fields while scrubbing user configuration selectors. No Windows native proof may be inferred from Linux.

## Work-unit and authority boundary

Keep test script, workflow, and this record in reviewable units of at most 400 diff lines, with focused red/green checks where a behavior harness is added. Fail closed for unsafe paths, empty selectors, missing digests, or incomplete runtime evidence. Fork draft PRs may target pinned non-`main` bases for native CI; do not merge, auto-merge, push to `main`, publish, install globally, replace a preview, or touch the real Pi home without fresh exact user approval.

**Rollback:** Revert only this Engram commit/unit; preserve chained parent PR#9 and the separate Linux scratch evidence and earlier failed attempts.
