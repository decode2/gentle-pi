# Upstream questionnaire adaptation

Adapt Alan's merged upstream questionnaire instead of transplanting the owned implementation, while retaining the already-approved RPC and UX requirements.

## Identity and authorization

- Repository/worktree: `decode2/gentle-pi`, `questionnaire-visual-polish-5594c1f4`; keep the existing branch and one writer.
- Upstream base: `Gentleman-Programming/gentle-shell` commit `f2d9d073ffc2299eb501902753b42789f7cdc461`, tree `6fb42857f728f131da054e592ec5d22c691ce870`, merged PR #1274 by Alan-TheGentleman.
- User selected upstream adaptation in decision 16829 and authorized ODD implementation in decision 16831.
- Preserve the historical `fe70f7fd2e1d692eb3790adebb53a10c8b34e64c` artifact-composition evidence and its immutable old-main pin. Do not retarget or relax that artifact.
- No new branch, activation, package removal, normal/lab configuration change, PR, main push/merge or auto-merge is authorized.

## Constraints and verification

- No local Node/npm/SDK/compiler/test/build/install/formatter/Docker/native execution. Execution evidence comes from exact-SHA hosted CI.
- Strict TDD remains enabled from the retained session choice. Behavioral units require observed hosted RED before implementation, then GREEN. Acquisition/characterization work does not fabricate production RED.
- General hard cap: 400 additions plus deletions per unit, including tests and tracking; the historical 475-line exception does not carry forward. No line-golfing or assertion removal.
- Generated artifacts, source/dependencies and runtime inputs remain immutable and isolated. Network is allowed only during bounded acquisition; validation uses network none, read-only inputs/root, UID 1000, zero capabilities, no-new-privileges and clean credentials.
- Upstream owns the questionnaire extension unconditionally. Hosted profiles must select exactly one provider. The legacy owner gate does not govern upstream.
- Preserve upstream custom-plus-MULTI selected results. Do not import the owned validator's narrower rejection.
- Keep shared `lib/native-fullscreen-interaction.ts` unchanged unless separately justified and authorized.
- Full-package startup, native review, human Herder acceptance and migration remain separate gates.

## Retained requirements

1. RPC `select` plus editor/input fallback, abort handling and partial results.
2. Question/global notes in success and cancellation results.
3. Valid empty MULTI and always-available custom response; preserve upstream custom-plus-selected results.
4. Explicit vertical Submit above Cancel and reachable controls.
5. Preview scrolling/resizing and usable narrow/short terminal layout, adapted to the upstream dock.
6. Configurable navigation/editor bindings, including explicit disabled `[]`, multiline editing and the required external-editor binding.
7. Balanced blocked events compatible with the real Herdr consumer, reload safety and exactly one selected provider.

## Tasks

- [x] **UQA-00 — Select and map the upstream base.** Read-only evidence in Engram 16794 and planning handoff `muaf395g-1b-0raa`; upstream is not a drop-in replacement. Route: delegated mapping because more than four files/dependencies were required.
- [x] **UQA-01 — Acquire and identify current upstream immutably.** Verified at `33cdcd47172ff0dc39412fd0c387eb8f3578ceb5`, CI 35549012990: two acquisition tests, scoped strict TypeScript 5.9.3, exact source/dependency identity, offline isolation and final candidate cleanliness passed. Final scope is 382 additions across three new paths, under the 400-line cap; the old pin and legacy lanes remain unchanged. Native review consent was declined for this candidate, with no lineage or mutation; independent high-risk verification passed under ordinary policy. No questionnaire behavior or runtime execution.
- [x] **UQA-02 — Build a strict derived adaptation artifact.** Verified at `fc1a54c8aa3a2712ce9697026e11e802a78e3779`, CI 35552590669: acquisition and both strict TypeScript gates passed; all 10 composition tests, derived read-only verification and final cleanliness passed. Final scope is 485 changed lines across five paths, inside the user-approved 550-line exception; the general cap remains 400. Exact preimages/postimages, path inventory, standard Git application, offset/fuzz rejection, source immutability and failure output absence were exercised. Staging cleanup runs through awaited `finally` but lacks a separate directory-absence assertion. Native review consent was declined for this candidate, with no lineage or mutation; independent high-risk verification passed under ordinary policy. No questionnaire behavior or runtime execution.
- [x] **UQA-03 — Restore Herdr blocked-event compatibility.** Verified at `4052df260a731dc8a596e59ae8b9f45a2aa86a63`, CI 35556873719, after genuine RED at `8a701b8a4b9bb0123984c253c8b109365722213c`, CI 35555277959. Exact hosted GREEN: acquisition 2/2, zero-patch composition 10/10, strict producer/test types, upstream type ratchet with no regressions, three-patch composition/read-only checks, and 92/92 selected behavior tests passed. Native and compatibility events are private, ordered and balanced through answered/cancelled/thrown and guarded overlap. Final scope 383 additions plus one deletion across six paths, under 400; consumer/shared helper unchanged. Native START produced two fresh-but-expired consent envelopes with no invocation, lineage or mutation; independent high-risk verification passed.
- [x] **UQA-04a — Preserve partial cancellation results.** Verified RED `a5d49950d7ba2e4a7f2f7faf7227ece90f2dc906`, CI 35604269961, and GREEN `1009889ca53e8dfc442586d1f848e9d9ef9756ad`, CI 35606579713. Per explicit user decision, these are two bounded TDD work units with no size exception: RED 379 lines; GREEN incremental 76 lines. Exact GREEN passed 96/96 with committed option+preview, custom+MULTI and prior-answer retention, draft omission, unchanged empty cancellation, UQA-03 events/Herdr, type/composition/read-only and cleanliness gates. Cumulative behavior scope is 415 lines and is reported separately. Native high-risk review lineage `review-1ab1dd6b86f1527c` approved across four lenses and was acknowledged/burned. Notes remain UQA-06.
- [x] **UQA-04b — Settle abort and races exactly once.** User approved readability-safe RED work units A1 → A2 → B with no size exception after rejecting a code-golfed combined candidate. A1 `42939637c2c0a0312e139249e72e500961c1e8f2` is verified authored-only at 391 lines. A2 `1a7deb68fd8e631506b749485d88f139b0e76eda` is verified authored-only at 246 incremental lines with all eight readable abort/race contracts and unchanged prior gates. Exact standard-Git application/postimage and behavioral RED remain pending B. Initial B `44be140c0dc49e332892a1679a002a6809b1eca8` failed infrastructure because Docker stdin omitted `-i`; no abort composition or behavior ran. The hosted RED closed at `3ad033ef12d72b0894dc284bf6c76bf3e51527cc`, CI 35636241557, after bounded stdin and nullable-listener corrections. Exact composition/postimage `e0afc181256c503efa3fb3fd2fb54899abe4cc0c`, read-only isolation, strict types and ratchet passed; genuine RED was 97 top-level / 104 total / 96 pass / 8 fail. Evidence isolates ignored aborts: pre-abort mounted UI, mounted paths used fallback with listeners 0/0, deferred mount continued, and submit/cancel/rejection preserved outcomes but lacked listener lifecycle. Combined GREEN `29d18e8ab8dd42bc09266038b70a0514dd6949e8` plus scoped-compiler correction `85c333314f46f9123e2b45b97562bf99b02412df` is verified by CI 35643190033: all four exact postimages, read-only isolation, strict types, ratchet and 104/104 behavior passed; the separate RED lane still reproduces exactly eight failures. Producer `007` preserves `003` events and `005` partials while adding explicit listener cleanup and real view cancellation; view `008` delegates to guarded `finish`. Native review START twice returned fresh-but-expired consent bindings with no invocation, lineage or mutation. Read-only assessment classified the exact range high-risk/process-boundary with native review unavailable, requiring writer self-verification plus an independent verifier; both are satisfied by the static writer evidence and exact-SHA verification above.
- [x] **UQA-05 — Add explicit controls and MULTI semantics.** Explicit Submit changes six upstream view expectations and cumulative `006` input drivers, so full replacement patches exceed 400 lines. First add a separately tested `composition/v2` prerequisite: v1 remains byte-compatible, while v2 admits only contiguous same-target chains with source/step hash continuity, staged preimage checks, metadata-change rejection and real target ancestors. Hosted read-only patch mounts and single-process composition remain the bounded TOCTOU mitigation. Then use small deltas for RED controls/MULTI contracts and GREEN producer/view behavior, preserving UQA-04b artifacts and shared fullscreen interaction.
  Controls RED `7063d163`, CI 35666637946, verified the exact `009 → 010` chain and 23/20/3 contract. Final `f4836a36`, CI 35675663316, verifies corrected stale-MULTI RED 26/25/1 at the final deep comparison; mouse controls and failed-submit atomicity; correction GREEN ask/Herdr 105/105 and view 26/26; pre-correction 105/100/5 and view 23/23; strict types, ratchet, composition/read-only, pinned image and final cleanliness. UX CI 35675663273 passed all six jobs. Native high-risk review `review-624fe0fa34b8a369` approved all four lenses and was acknowledged/burned.
- [x] **UQA-06 — Add question and global notes.** Preserve trimmed nonblank `notes` on committed answers and independent `globalNote` in successful and cancelled structured results; request schema remains unchanged and new textual/RPC formatting stays in UQA-09. Split under the 400-line cap: UQA-06a view/result RED→GREEN, then UQA-06b producer passthrough RED→GREEN. UQA-06a corrected RED `7690a49c`, CI 35717856901, observed 30/26/4; GREEN `6b6e9ae8`, CI 35741704413, passed ask/Herdr 105/105 and view 30/30. UQA-06b RED `26b32ea3`, CI 35743194386, observed 108/105/3 with only `globalNote` details absent; GREEN `981d296b`, CI 35746571050, passed ask/Herdr 108/108 and view 30/30. Both GREEN runs passed strict types/ratchet, exact composition/read-only isolation, all prior and pre-correction gates, pinned image and final cleanliness; UX CIs 35741704351 and 35746570884 each passed all six jobs. Native high-risk review `review-762138fc8e199721` approved all four lenses and was acknowledged/burned.
- [ ] **UQA-07 — Add configurable editor/navigation bindings.** Split UQA-07a multiline Pi `Editor` plus injected input/navigation bindings from UQA-07b external-editor behavior. UQA-07a preserves absent-vs-explicit-`[]`, gives newline precedence over submit collisions, and keeps completion on visible Submit. Public Pi APIs do not expose direct external-editor launch from a non-overlay `custom()` view: nested `ctx.ui.editor()` replaces and cannot restore that dock, while the exported `ExtensionEditorComponent` hides draft access and omits the configured command at this boundary. Therefore UQA-07b/07c require an explicit re-scope rather than importing internal/owned host modules. The first hosted UQA-07a observation at `66bb73e1`, CI 35755176535, passed strict types, exact composition and all prior gates before the RED assertion expected one extra failure: actual behavior was 36 total / 32 pass / 4 fail. Select remapping and explicit disabled input arrays were passing characterizations; default/remapped multiline text, collision precedence and Pi `Editor` editing remained the four genuine gaps. The expectation correction does not change the immutable test patch or observed behavior. GREEN is staged as eleven exact patches: immutable `025` updates existing view drivers to explicit remapped submit, `029` derives its editor-hint correction, producer drivers use visible Submit or an explicit submit binding, `032` supplies the explicit binding and Pi `Editor` terminal fixture, `033` keeps the shared factory's minimum TUI type while that fixture carries terminal rows at runtime, `034` makes both custom-success drivers commit through that binding before visible final Submit, the view replaces single-line `Input` with Pi `Editor`, immutable correction `030` expands its parameter properties for Node strip-only execution, `031` treats a partial injected manager without `getUserBindings()` as absent configuration, `035` lets visible Submit synthesize a preserved nonblank custom draft after the cursor moves off its row, and the producer forwards injected `tui`; the shared fullscreen helper remains protected. Diagnostic CI 35763269590 preserved the pre-TAP failure: both ask files failed at module load with `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX` on the new `CustomTextEditor` parameter properties, before any behavioral test ran. Correction CI 35765772165 then reached behavior and observed ask/Herdr 108/102/6: every failure was an existing custom-editor driver whose minimal mocked manager exposes `matches()` but not `getUserBindings()`. CI 35767373985 improved this to 108/104/4: two drivers overran the added question-note row, one partial-cancellation contract still used default Enter instead of an explicit editor-submit binding, and one render fixture lacked the terminal rows required by Pi `Editor`. CI 35769275452 stopped at strict TypeScript before TAP because `032` over-constrained the shared factory TUI type and exposed seven unrelated minimal fixtures; `033` restores the minimal type without removing the runtime terminal fixture. CI 35771025519 reached ask/Herdr 108/106/2; only the two custom-success drivers remained, both corrected by `034` to use configured editor submit followed by visible questionnaire Submit rather than row-count navigation. CI 35772417249 then passed ask/Herdr 108/108 and reached view 36/35/1; the sole genuine behavior gap was that final Submit ignored a preserved default-multiline custom draft after navigation moved the cursor to Submit. Derived production correction `035` removes that impossible cursor precondition while retaining the nonblank-draft guard. Final exact SHA `c73775fe`, Adaptation CI 35774924907, passed ask/Herdr 108/108, view 36/36, strict types, ratchet, exact composition/read-only isolation, every prior gate and final cleanliness; UX CI 35774924678 passed all six jobs. Native high-risk review `review-2b6762d6dc2661ff` approved all four lenses with two non-blocking informational warnings and was acknowledged/burned.
  - [x] **UQA-07b — Specify and defer the external-editor host contract.** `odd/tasks/custom-view-external-editor-contract.md` defines a host-owned `ctx.ui` operation for non-overlay custom views, with configured-command and terminal ownership, same-view restoration, explicit result/error behavior, abort/reload invalidation, late-result quarantine and single-operation concurrency. Pi core/SDK and questionnaire implementation remain a separate future work unit.
- [ ] **UQA-08 — Add dock viewport and preview behavior.** Split before writing because tests, production, hosted lanes and tracking cannot honestly fit one 400-line work unit.
  - [x] **UQA-08a — Preserve narrow/short reachability.** Use 20 columns and 10 rows as test floors rather than support cutoffs; cap rendered height and keep primary Submit and Cancel reachable without changing the protected fullscreen helper. RED work unit: 143 changed lines at `9243b8d8`, hosted CI `35786466584` proved 37/36/1 with only the intended 33-row overflow. GREEN was separately forecast at about 165 changed lines and landed in 148. Hosted diagnostic CI `35791162235` then exposed three partial-TUI fixture failures because their compatibility doubles omit `terminal`; correction `038` preserves the prior unbounded fallback only when terminal geometry is unavailable. Corrected GREEN `1c2e72a8`, hosted adaptation CI `35792642473`: 108/108 ask/Herdr, 37/37 view, strict types and read-only composition; UX CI `35792642307` passed. Independent verification found a P2 editor-visibility gap: `037` anchors at line zero while editing because editor lines have only header owners. Correction RED `189c71d0`, CI `35794148199` passed the hosted gate with 38/37/1: the sole failure is a typed editor draft hidden under a long question at 20×10. This RED unit changed 130 lines. Separate GREEN forecast: roughly 138 changed lines, actual 139 changed lines (138 additions, one deletion) at `d6143cb6`; `040` identifies editor-owned lines as viewport anchors without exposing them to click-row selection. Hosted CI `35796676548` passed strict types, immutable read-only composition, 108/108 ask/Herdr and 38/38 view; UX CI `35796676498` passed. Independent read-only correction verification accepted at `d6143cb6`; the tracking-only worktree modification was authored by this writer after the candidate and is closed in the subsequent tracking commit.
  - [x] **UQA-08b — Route preview/body scroll and resize safely.** Preview input wins only over visible preview geometry, other questionnaire scroll goes to the body, unclaimed scroll falls through to the transcript, and resize recomputes geometry while rejecting stale mouse coordinates. Read-only mapping of the upstream-derived dock found only left-button row press/click handling: no wheel ownership, preview hit-testing, manual scroll state or stale-geometry guard. Component tests can prove an unclaimed event returns `undefined`; a separate host-level test is needed to claim the transcript actually scrolls. Forecast bounded RED tests against the derived view, not the separate owned TUI presentation.
    - Verified hosted exact-SHA RED `40794e23`, CI `35805784055`: 42 total / 39 pass / 3 fail. Only preview wheel, body wheel, and stale resize coordinates failed; unclaimed wheel fall-through passed, without asserting transcript movement. GREEN candidate `042-viewport-scroll-view.patch` changes only the derived view. Parent regenerated a standard contextual patch from exact view preimage `f9b48b230c743e3f56820bad8a472dbb3cef1259`, proved offset-free `git apply`, and pinned postimage `fe93980c5773057031e4112fd214ff1cc6264d0d`; the temporary scratch source was moved out of the worktree. Workflow composes offline from the immutable RED artifact, protects the fullscreen helper and test names, runs strict types, and expects view 42/42 plus ask/Herdr 108/108. First GREEN candidate `a810f503` changed 338 lines, under 400; CI `35810180562` composed the exact patch but stopped before behavior at strict TypeScript with two scoped diagnostics: wheel dispatch lacks a required `target`, and `wheelDelta` may be undefined. Separate correction forecast: about 25 patch + 130 hosted lane + 8 tracking = 163 changed lines, under 400. Preserve `042` and derive a new exact `043` from its frozen postimage; prove the known type failure then run corrected GREEN. No behavior GREEN is claimed yet.
    - The hosted `35810180562` result is an observed GREEN-candidate failure at scoped strict TypeScript, before behavior ran. The original validation is marked continue-on-error; the following diagnostic requires its identified step outcome to be `failure` and runs a bounded network-none, read-only replay of the identical compiler command against frozen `042`; correction composition is gated on a nonzero exit with exactly two diagnostics at `lib/questionnaire/questionnaire-view.ts` (TS2416 details the missing required `target` property; TS18048 says `wheelDelta` is possibly undefined) and no other diagnostic/error.
    - Only after that diagnostic does composition/v2 derive the immutable `043` patch from `questionnaire-upstream-viewport-scroll-green/derived` at exact preimage `fe93980c5773057031e4112fd214ff1cc6264d0d` to postimage `f9eebba267ed1d2f0ca5a13a549c361ecb7aed0f`, with one allowed target (`lib/questionnaire/questionnaire-view.ts`) and protected `lib/native-fullscreen-interaction.ts`. Corrected GREEN is pending exact-SHA hosted CI; its read-only gate reruns strict types and ratchet, compares tests against both original RED/GREEN, and requires ask/Herdr 108/108 plus view 42/42. Exact-SHA hosted CI `35815683318` passed pre-correction type diagnostic replay, corrected strict types/ratchet, ask/Herdr 108/108 and view 42/42; UX CI `35815683183` passed. Independent read-only audit found a P1: a mid-list focused row can cancel bodyScroll against the existing centered boundViewport start while the wheel is still claimed. UQA-08b remains unchecked. Separate RED correction forecast: about 30 patch + 110 hosted lane + 6 tracking = 146 changed lines, below 400; prove visible body movement for a mid-list focus before any production correction.
    - Rollback/checks: if the corrected lane fails, keep UQA-08b open and preserve both immutable patches and prior RED evidence; do not proceed to behavior claims or broaden targets. Locally authorized checks are limited to `git diff --check`, scoped `git diff --numstat`, `git status --short`, and read-only patch/hash inspection; no local compiler or project tests ran. Correction additions plus deletions, counting immutable `043` as its 32 authored patch-file lines, are 209 lines against the strict 400-line cap (174 workflow + 3 new tracking + 32 patch); combined worktree scope is 211 lines when the preserved pre-existing forecast replacement is counted. The pre-existing untracked typed scratch is excluded and unchanged. Engram recovery mirror is pending because its local provider was unavailable in this session.
    - UQA-08b mid-focus RED forecast: patch `044-viewport-midfocus-scroll-tests.patch` is independently proven as a 34-physical-line, offset-free standard contextual Git patch targeting only `tests/questionnaire-view.test.ts`, from exact preimage `90088a79d8756da2e9a7533e32531be1c9ddf127` (041) to postimage `8cae509d3c38b6c47651808ee0d14b157905cbb7`. The new hosted exact-SHA lane is planned to compose after corrected `043` from `$RUNNER_TEMP/questionnaire-upstream-viewport-scroll-corrected/derived`, allow only that test target and protect `lib/native-fullscreen-interaction.ts`, then run network-none read-only isolated strict scoped TypeScript plus `scripts/check-types.mjs` ratchet and require view TAP 43 top-level / 42 pass / one failure named `wheel over a mid-list focus claims body scrolling only when the rendered body moves`; require its `a claimed body wheel must change the rendered body` `ERR_ASSERTION`/`notStrictEqual` diagnostic to distinguish unchanged rendered output from fixture/compiler failure. Hosted RED `ad579b43`, CI `35819312322` proved view 43/42/1 with only this behavioral failure: the claimed mid-focus wheel left rendered body byte-identical. RED work unit changed 154 lines, under 400. Separate GREEN forecast: about 80 production patch + 115 hosted lane + 8 tracking = 203 changed lines, under 400; preserve focus and preview, make wheel visibly move body, and do not claim an unchanged render.
    - UQA-08b mid-focus GREEN candidate forecast: 120 workflow lines + 3 tracking lines + immutable 045's 160 physical lines = 283 scoped changes, below 400; the preserved pre-existing forecast replacement adds 2 separately. Hosted GREEN `d158a1ec`, CI `35821643959` passed strict types/ratchet, ask/Herdr 108/108 and view 43/43; UX CI `35821643963` passed. Independent re-audit accepted the narrower mid-focus P1 fix but found a P2: manual body-wheel scrolling can move the editor caret out of view, and later editor input does not restore it. UQA-08b remains open. Separate RED forecast: about 36 patch + 115 hosted lane + 6 tracking = 157 changed lines, below 400; prove typing after scrolling restores caret visibility. Native review implicit committed scope spans prior work units, so no START was invoked. Transcript movement and full-package acceptance remain unproven.
    - UQA-08b editor-caret RED: the hosted offline composition/v2 lane derives from `$RUNNER_TEMP/questionnaire-upstream-viewport-scroll-midfocus-green/derived`, using only immutable 53-physical-line `046-viewport-editor-scroll-tests.patch`, target `tests/questionnaire-view.test.ts`, preimage `8cae509d3c38b6c47651808ee0d14b157905cbb7`, postimage `0f493774f4e98bf968303f8dfde155c05cbd25e2`, and protected `lib/native-fullscreen-interaction.ts`. Its network-none read-only gate requires unchanged production view/helper, scoped strict TypeScript and `/derived/scripts/check-types.mjs`, then only the view TAP file; RED must exit nonzero with 44 top-level / 43 ok / exactly one not ok named `typing restores a manually scrolled-off multiline editor caret`, 44 tests / 43 pass / 1 fail, and zero cancelled/skipped/todo. The sole failure diagnostic must be the `typing brings the changed caret line back into view` assertion with `ERR_ASSERTION` (operator `match`), excluding setup or wheel failures. New unit scope is 128 workflow additions + 1 tracking addition + 53 immutable patch lines = 182 additions, 0 deletions (<400); the untouched pre-existing forecast replacement is separately 1 addition + 1 deletion, making the full scoped working set 183 additions + 1 deletion (184 total). Hosted RED `d613ec93`, CI `35824486597` proved view 44/43/1: only the new editor-caret test failed because typing did not bring `caret-tailZ` back into view; setup and each claimed wheel movement passed. UQA-08b remains unchecked. Separate GREEN forecast: about 11 production patch + 115 hosted lane + 7 tracking = 133 changed lines (<400). Reset manual body-scroll ownership before forwarding editor input so the next render restores the editor caret; preserve the immutable RED and earlier tests.
    - UQA-08b editor-caret GREEN candidate: offline composition/v2 and read-only regression gates now apply immutable `047-viewport-editor-caret-view.patch` from RED preimage `107c6f415be8d74f73c81bcc8e8070a0a177cad3` to postimage `c038cefa266a707d7fcb5a923c0efdd269a0bd6b`, limited to the questionnaire view and protecting the fullscreen helper. Hosted exact-SHA GREEN `2591c3d6`, CI `35865996488` passed strict types/ratchet, ask/Herdr 108/108 and view 44/44; UX `35865996427` succeeded. Independent read-only re-audit ACCEPT found no remaining candidate-caused P1/P2. Native review pending user consent: ASSESS on committed parent `d613ec93` returned high risk with independent verifier required (fulfilled by this audit). Target-root INSPECT offered implicit default base `2e2ca315`, but the single START with explicit committed-only base `d613ec93` returned consent_required and `lineage_created: false` for the exact work-unit tree pair: base `53ee1104` equals `d613ec93^{tree}` and candidate `deb13177` equals `2591c3d6^{tree}`. The provider reports tree identities in its consent envelope; the initial mismatch interpretation was incorrect. That first consent binding expired with its Pi process; no lineage was created there. With explicit user approval, an isolated clean review worktree at `2591c3d6` preserved the exact `d613ec93^{tree}` → `2591c3d6^{tree}` candidate. Native lineage `review-76ce97da8ab942cd` reviewed the 150-line work unit with four lenses, closed `approved`, and was acknowledged/burned for target `sha256:af3fb4d7e521c6fb9307d7f064a106de06090b9c75f72fc9aad760946992f601`. ASSESS recorded `nativeReviewOutcome: closed` on this exact committed range; independent re-audit also ACCEPT. UQA-08b is complete at the component boundary. Component fall-through still does not prove host transcript scrolling; host-level proof remains a separate pending gate.
- [ ] **UQA-09 — Add RPC presentation.** Select plus editor/input fallback, empty/custom/MULTI/notes/partial results and abort behavior. Read-only producer/host/composition mapping at clean `3a468a5d` confirmed the derived producer remains TUI-only (`ctx.mode !== "tui"`), while the owned RPC driver is a reference, not the adapted source. Pi's public RPC UI has `select`, `editor`, and `input`, no native multi-select dialog and no response-bearing `custom()`. Retained decisions already require custom-plus-selected MULTI and question/global notes; the owned driver's exclusive custom/MULTI validator must not be transplanted. Route: three parallel read-only scouts (4+ files), then one target-root writer per bounded work unit; no concurrent writes in the feature branch. TDD source: retained strict hosted behavioral RED→GREEN; exact runner: pinned Node image in `.github/workflows/questionnaire-upstream-adaptation.yml`, scoped strict `tsc`, type ratchet and `node --experimental-strip-types --test --test-reporter=tap`, never local project execution. UQA-09a introduces supported RPC entry and select/editor/input single/custom fallback with separate immutable RED and GREEN units; forecast RED ~250 and GREEN ~350 additions+deletions including workflow/patch/tracking, each under the explicit 400 cap. UQA-09b splits into UQA-09b1 empty/custom-plus-selected MULTI and UQA-09b2 question/global notes text/details, avoiding a combined over-budget producer rewrite; UQA-09c covers partial cancellation, pre/mid-dialog abort and late-response quarantine. Forecast those after UQA-09a evidence, preserving the protected fullscreen helper and all historical artifacts.
  UQA-09a RED route is offline composition/v2 from `questionnaire-upstream-viewport-editor-scroll-green/derived`: immutable `048-rpc-entry-tests.patch` changes only `tests/ask-user-question.test.ts`, preimage `24063de1041e490df2524e213985ac64e1eb1f98`, postimage `69f08423a6b11686cfd7c28eeed3d1cd542761bf`, SHA-256 `27e4179f63c1400fb215105b1bd10280af1b99d6de72196164e831819dcd097d`, while protecting `lib/native-fullscreen-interaction.ts`.
  Forecast was 338 conservatively counting both the 94 logical test additions and the 105 physical patch lines; after moving the verified scratch source to `/tmp`, the commit scope is 244 additions (105 patch lines + 137 hosted lane + 2 tracking), below 400. Scratch evidence is excluded from the commit. Hosted exact-SHA RED `76d39125`, CI `35877347216` passed strict types/ratchet, composition and read-only gates, then observed only the three named RPC failures: ask/Herdr 111 total / 108 pass / 3 fail, with prior TUI contracts intact. UX CI `35877347302` passed. RED work unit committed 244 physical additions, below 400; the archived scratch preimage was excluded. Separate UQA-09a GREEN commit scope: 203 physical producer patch lines + 121 hosted lane lines + 3 tracking changed lines (including the prior RED status replacement) = 327 additions+deletions, below 400 (scratch excluded); implement public RPC select/dialog fallback without changing TUI, protect the fullscreen helper, and claim only the tested single/custom behavior. Static correction adds abort-raced dialogs, membership and availability guards, preview retention and a typed success result; hosted behavior remains unobserved. Preserve this RED artifact.
  UQA-09a GREEN composes immutable `049-rpc-entry-producer.patch` (SHA-256 `3f2e15a54504b30e1c03a7cc5807601971e97db19ad49b5111047766ddacad83`) from producer preimage `037574cd331f6ebb4cfb001bc0f6df5d905783d9` to postimage `2bcedddd299eab8e0161ac072e6f7ff9a36cff14`, targeting only `extensions/ask-user-question.ts` after RED `048`; its offline v2 lane preserves the tests and protected fullscreen helper, requires strict TypeScript and type ratchet, and forecasts ask/Herdr 111/111 plus view 44/44, each with zero fail/cancelled/skipped/todo. Hosted exact-SHA GREEN `d265e15f`, CI `35884580457` passed strict types/ratchet, ask/Herdr 111/111 and view 44/44; UX `35884580387` passed. Independent read-only audit ACCEPT applies only to single/custom RPC entry; abort/race, invalid dialog values, previews and partial results are not covered by these three new tests and remain later work. Native high-risk review `review-5ddfde84589b5b46` approved four lenses, with informational warnings `R3-001` (hosted lane) and `R4-editor-abort` (editor abort); no correction was opened. It was acknowledged/burned for the exact `76d39125..d265e15f` candidate, and ASSESS recorded `nativeReviewOutcome: closed`. This does not activate the package or prove full RPC parity.
  UQA-09b1 RED candidate: `050-rpc-multi-tests.patch` is a 91-physical-line standard contextual Git patch (SHA-256 `6c38bfa57cee20a178c15f0ec66174791282cd709a41fd1b8a4bdf2c8a2463b4`) targeting only `tests/ask-user-question.test.ts`, from frozen `048` postimage `69f08423a6b11686cfd7c28eeed3d1cd542761bf` to `4c0d54a0f2159964effd789e0736d15a064dd2cb`. Compose offline/v2 after `049` GREEN, pin producer `2bcedddd299eab8e0161ac072e6f7ff9a36cff14`, protect producer and `lib/native-fullscreen-interaction.ts`. The two tests require reverse-order RPC toggles plus editor custom text committed in authored option order through visible Submit, and empty MULTI Submit as `kind: multi`, `answer: null`, `selected: []`, `(no input)`, not cancelled, contrasted with Cancel; both assert balanced events and no `ctx.ui.custom` for submission. Hosted RED forecast: scoped strict TypeScript and ratchet pass, ask/Herdr 113 total / 111 pass / exactly two named failures with unavailable diagnostics; view 44/44 remains the prior GREEN evidence, not rerun here. Exact-SHA hosted CI `35891667554` composed `050` but stopped at scoped strict TypeScript before TAP: `tests/ask-user-question.test.ts(361,30) TS2339` after a deep-equality assertion narrowed `result.details` to an object without `cancelled`. Corrected exact-SHA hosted CI `35894952470` passed diagnostic and strict types/ratchet, establishing behavioral RED at ask/Herdr 113 total / 111 pass / exactly two named MULTI failures; UX `35894952647` passed. No GREEN or producer change yet. Separate correction forecast: ~10 physical test patch + ~155 hosted diagnostic/corrected RED lane + ~5 tracking = ~170 changed lines (<400). Preserve `050`, prove its exact TS2339, then derive immutable `051` from frozen `050` postimage, rerun strict types and require only the two named RPC MULTI failures with 111 previous passes. The 80 logical test additions were authored at the allowed scratch path and archived outside the checkout at `/tmp/uqa09b1-rpc-multi-red-scratch-evidence-4c0d54a0/ask-user-question.test.ts` (postimage `4c0d54a0f2159964effd789e0736d15a064dd2cb`); only the patch is delivered. New scope is 91 physical patch lines + 132 workflow additions + 1 new tracking line = 224 additions, 0 deletions (<400); the preserved parent planning replacement is separately 2 additions + 2 deletions, yielding 228 scoped physical changes. UQA-09b1 GREEN remains separate.
  UQA-09b1 test-TYPE correction (hosted CI `35894952470`): immutable standard contextual `051-rpc-multi-test-types.patch` changes only `tests/ask-user-question.test.ts` from frozen `050` blob `4c0d54a0f2159964effd789e0736d15a064dd2cb` to `f917898dda9eef1ca300df3cb09579890417e824`; SHA-256 `2ac3969f72e84f82c8ab50d1e653527ddbc904b7f018a025c1ad5536e5f78c6c`. Capture `details.cancelled` before the deep-equality narrowing, preserving every assertion and test name. The original RED step is continue-on-error only to permit a read-only network-none replay requiring its failed outcome and exactly the scoped TS2339; offline v2 composition from the frozen `050` derived tree is gated on that diagnosis and protects producer `2bcedddd299eab8e0161ac072e6f7ff9a36cff14` and fullscreen helper. Corrected read-only gate requires scoped strict types, ratchet, and exactly 113 total / 111 pass / 2 named MULTI failures with `unavailable_outside_tui`, no other failures. Static patch/application pins are local Git evidence only; the correction adds 182 physical lines (163 workflow, 18 patch, one new tracking line); including the preserved parent tracking replacement (one addition/one deletion), the full worktree scope is 184 (<400). Hosted behavioral RED passed 113/111/2 with only the named MULTI failures; GREEN remains separate.
  UQA-09b1 intermediate GREEN candidate (audited P2): immutable `052-rpc-multi-producer.patch` (SHA-256 `5ec22b4bc5086f3bd4815726dbe07a47947e26d8808ef742ac1460d2ad9ff80b`) derives only `extensions/ask-user-question.ts` from frozen `049` postimage `2bcedddd299eab8e0161ac072e6f7ff9a36cff14` to `315abb27bf471a15b21a99a84f65660bbcebdebe`. Offline composition/v2 starts at corrected `051` test blob `f917898dda9eef1ca300df3cb09579890417e824`, inventories only `052`, and protects the ask test and fullscreen helper; the network-none read-only GREEN gate requires strict TypeScript/ratchet, ask/Herdr 113/113 and view 44/44, plus clean checkout. Hosted CI `35898077866` passed strict TypeScript/ratchet, ask/Herdr 113/113 and view 44/44; UX `35898077927` passed. Independent read-only audit REJECTed closure: reopening RPC MULTI custom editor does not prefill its previous draft (`ctx.ui.editor(question.question)`), although Pi supports `editor(title, prefill)`. Freeze `052` as intermediate GREEN; add immutable `053` behavioral RED for two editor visits and `054` producer correction separately. RED forecast ~38 physical test patch + ~132 workflow + ~5 tracking = ~175; corrected GREEN ~15 producer patch + ~130 workflow + ~5 tracking = ~150, each <400. Do not claim UQA-09b1 complete until correction and re-audit. Local Git-only isolated apply had no offset/fuzz. MULTI uses a visible action loop with authored-order toggles, editable custom text and explicit Submit (including empty selection); single/custom and TUI paths stay unchanged. Scratch is excluded; physical candidate scope is 55 patch lines + 115 workflow additions + 3 incremental tracking additions/deletions = 173 (<400); the full working diff including preserved parent tracking is 177. Rollback removes only `052`, this hosted lane and this tracking line; UQA-09b2 notes and UQA-09c abort/races remain pending.
  UQA-09b1 independent re-edit RED candidate: immutable `053-rpc-multi-reedit-tests.patch` (SHA-256 `95442c3609422c3403d989d2da352d88f2427ea3da020fe3be14abbcc84d7895`) targets only `tests/ask-user-question.test.ts`, frozen `051` preimage `f917898dda9eef1ca300df3cb09579890417e824` → postimage `5833e71d606c3ff0df1c0221e18aae12d3192fb0`. Offline v2 derives from `052` GREEN, pins its producer `315abb27bf471a15b21a99a84f65660bbcebdebe`, protects producer/fullscreen helper, and requires network-none read-only scoped strict types/ratchet followed by ask/Herdr 114 total / 113 pass / exactly one named re-edit failure with the missing prefill deepStrictEqual diagnostic (107 top-level / 106 ok / 1 not ok). Prior view 44/44 is GREEN evidence, not rerun in this RED lane. The test records both editor prefills before asserting the revised committed answer, authored selected `[Two]`, rendered text, balanced events and no custom TUI mount. Hosted exact-SHA CI `35908258840` passed scoped strict TypeScript/ratchet and proved re-edit behavioral RED: ask/Herdr 114 total / 113 pass / one named missing-prefill failure and matching diagnostic; UX `35908258962` passed. Git-only static patch evidence alone was not behavioral RED. Corrected GREEN `054` must pass the saved custom draft as public editor prefill without altering single-select/TUI or prior immutable lanes; forecast ~13 patch + ~110 workflow + ~5 tracking = ~128 physical changes (<400). No corrected GREEN or native review is claimed. Scratch is excluded from delivery. Static scope: 46 patch lines + 110 workflow lines + one new tracking line = 157 authored additions; with the preserved parent tracking replacement (one addition/one deletion), the worktree has 159 tracked/untracked deliverable changed lines (<400). The 35 scratch test lines are excluded from delivery; preserve the parent's uncommitted audit and P2 forecast.
  UQA-09b1 P2 re-edit intermediate GREEN (input fallback unresolved): immutable standard contextual `054-rpc-multi-reedit-producer.patch` (SHA-256 `1a38fbcff3db3e9f95ae29563020b621751a9f18bb410bad5e2721fa1c088e3e`, 13 physical lines) changes only `extensions/ask-user-question.ts` from frozen `052` blob `315abb27bf471a15b21a99a84f65660bbcebdebe` to `b2ec2a3c30eb181ff3ddcbf4ed3ea3b0b9f12595`. The MULTI custom editor alone receives `custom` as its prefill: undefined on first visit, saved nonblank draft on revisit; the single-select branch, TUI, input fallback, abort, notes, toggles and empty MULTI remain unchanged. Git-only isolated `git apply --check` and clean, offset-free apply verified the exact postimage, not behavior. Offline v2 composition starts at the immutable `053` RED artifact (test `5833e71d606c3ff0df1c0221e18aae12d3192fb0`), inventories only `054`, and protects the ask test and fullscreen helper. Hosted CI `35912352199` passed network-none read-only strict TypeScript/ratchet, ask/Herdr 114/114, view 44/44 and checkout cleanliness; UX `35912352176` passed. Independent read-only re-audit confirms editor prefill but REJECTs UQA-09b1 closure: input-only MULTI also reopens with no saved draft (`ctx.ui.input(question.question, undefined, { signal })`). Pi's public API declares `input(title, placeholder?, opts?)` but `editor(title, prefill?)` supports editable prefill. User chose the honest input-only fallback: show the saved draft as placeholder; retyping changes it, submitting empty retains it. Cancellation remains distinct. Next immutable `055` RED derives from `054` GREEN (producer `b2ec2a3c30eb181ff3ddcbf4ed3ea3b0b9f12595`, test `5833e71d606c3ff0df1c0221e18aae12d3192fb0`), with two tests for retyping and empty retention; expect ask/Herdr 116 total / 114 pass / 2 named failures. Forecast RED ~80 test patch + ~130 workflow + ~5 tracking = ~215 lines, GREEN `056` ~18 producer patch + ~130 workflow + ~5 tracking = ~153; both <400, exact hosted RED before GREEN. No native review or full RPC parity claimed. This incremental work unit changes 13 patch lines + 117 workflow additions + 2 tracking additions and 1 deletion = 133 physical lines (<400); with the parent's separate tracking replacement the full deliverable diff is 135 lines. The parent uncommitted exact-SHA RED CI `35908258840` evidence and UX `35908258962` remain intact. No native review or UQA-09b1 closure is claimed.
  UQA-09b1 input-only `055` RED verified: immutable standard contextual `055-rpc-multi-input-reedit-tests.patch` (SHA-256 `e3e8a0cde7fe5ed73fb64800d9a73f1ec9cbf3cd7a6822f8807078495703c29f`, 81 physical lines) changes only the ask test from frozen `053` blob `5833e71d606c3ff0df1c0221e18aae12d3192fb0` to `3754a0972cf25e97d7f431f462bac4e2c46e9e1c`. Offline v2 composition starts at `054` GREEN producer `b2ec2a3c30eb181ff3ddcbf4ed3ea3b0b9f12595`, protects producer and fullscreen helper; network-none read-only gate requires scoped strict TS/ratchet then ask/Herdr 116 total / 114 pass / exactly two named input-only failures: missing second-visit placeholder and blank re-entry clearing the draft, both `ERR_ASSERTION`/`deepStrictEqual`. View 44/44 is prior GREEN evidence, not rerun. Both tests use input without editor, capture two input arguments, reach visible Submit, assert custom-plus-selected answer, balanced events and no TUI custom mount. Git-only isolated apply was offset-free with exact postimage; no local project runtime ran. Exact-SHA hosted CI `35916930085` passed strict TypeScript/ratchet and proved behavioral RED ask/Herdr 116 total / 114 pass / exactly two named failures with placeholder/blank-retention diagnostics; UX `35916929813` passed. Next immutable `056` producer correction from `054` blob `b2ec2a3c30eb181ff3ddcbf4ed3ea3b0b9f12595`: use `custom` as input placeholder and preserve saved nonblank custom on blank input-only revisit; retain editor blank semantics and all single/TUI paths. Forecast ~18 patch + ~125 workflow + ~5 tracking = ~148 physical changes (<400), hosted strict types/ratchet ask/Herdr 116/116 and view44/44. The 70-line scratch delta is archived outside the checkout at `/tmp/uqa09b1-input-red-scratch-evidence-3754a097/tests/ask-user-question.test.ts` and excluded from delivery. Incremental physical scope: 81 patch lines + 115 workflow additions + one tracking line and one current-step replacement pair = 199 (<400); including the parent's uncommitted replacement pair, the working set totals 201. Preserve that decision and CI evidence. Next `056` producer correction is separate; UQA-09b1 remains open.
  UQA-09b1 input-only `056` GREEN candidate (hosted timeout): immutable standard contextual `056-rpc-multi-input-reedit-producer.patch` (SHA-256 `96cfd4c415a015e75201b66ca4370d15409d0204999d8722e833a12eb6f46693`, 22 physical lines) changes only the derived producer from frozen `054` blob `b2ec2a3c30eb181ff3ddcbf4ed3ea3b0b9f12595` to `06417061d12482399b507cdc945b9252d0f24d7d`. Input-only MULTI uses the saved custom as a placeholder hint (undefined initially), keeps a nonblank draft after blank re-entry, and replaces it on nonblank input; undefined response remains Cancel. Editor retains editable prefill and blank-clears-draft behavior; single-select, TUI, toggles and explicit Submit remain unchanged. Git-only isolated apply/check was clean and offset-free with exact pre/post blobs; this is not runtime evidence. Offline v2 composition starts at frozen `055` RED test `3754a0972cf25e97d7f431f462bac4e2c46e9e1c`, inventories only `056`, and protects test and fullscreen helper. Hosted network-none read-only gate pins patch SHA-256, source/post blobs, protected hashes, scoped strict TS/ratchet, ask/Herdr 116/116, view 44/44 and final checkout cleanliness. Exact-SHA CI `35924367849` passed scoped strict TS/ratchet but timed out at 25 minutes before TAP. The 35-minute execution-budget correction reran immutable `056` at exact SHA `27a5ff3b`: CI `35927358672` passed strict TS/ratchet, ask/Herdr 116/116, view 44/44 and checkout cleanliness; UX `35927358690` passed. Independent read-only re-audit ACCEPT found no scoped candidate-caused P1/P2; it did not execute tests. Historical frozen patches/gates remain untouched. Long-term split historical lanes before adding UQA-09b2/09c. Preserve RED CI `35916930085` and UX `35916929813`; native review and ODD closure remain pending. This does not claim full RPC parity. Incremental deliverable scope is 22 patch lines + 121 workflow additions + 2 tracking additions/1 deletion = 146 physical changes (<400); including the parent's untouched uncommitted tracking replacement, the full worktree diff is 148. Scratch is temporary and excluded from delivery. Rollback removes only `056`, its GREEN lane and this tracking update.
- [ ] **UQA-10 — Verify adapted provider lifecycle.** Actual hosted protocol, package filtering, exactly one registration, provider context, reload without duplicate listeners and final cleanliness.
- [ ] **UQA-11 — Verify full package and human migration.** Isolated complete-main startup with scratch/descendant cleanup, then separately authorized Herder acceptance and reversible migration. No activation is implied by prior tasks.

## Workload and delivery

- Current rough envelope: 2,660–3,650 authored changed lines across coherent units; estimates become binding only when each task is forecast immediately before its first write.
- Each unit closes with its own Conventional Commit and exact hosted evidence. A native review candidate is a bounded work-unit commit or future PR slice, never the accumulated feature branch.
- Delivery strategy remains `ask-on-risk`; no PR chain strategy or PR delivery is authorized. The user requested the existing branch, so work stays there until a separate delivery decision.
- Stop and re-scope before writing if a unit cannot honestly fit 400 lines. Rollback removes that unit's patch/tests/artifact entry without deleting prior units or historical artifacts.

## Current next step

UQA-07b remains deferred; UQA-08a/b are closed at the component boundary, but host transcript wheel movement remains unproven. UQA-09a single/custom RPC entry is complete with hosted RED/GREEN and burned native review. UQA-09b1 `054` editor re-edit GREEN passed CI `35912352199` (ask/Herdr 114/114, view 44/44) and UX `35912352176`, but re-audit rejected input-only draft reopening. User chose placeholder reminder with blank retaining the draft. Hosted `055` RED passed at CI `35916930085` (116 total / 114 pass / two named input failures), UX `35916929813` passed; `056` input-only GREEN is staged, pending exact-SHA hosted proof and independent re-audit before native review/closure. Preserve visible Submit, custom-plus-selected authored order and empty Submit; no native multi-select. Historical type error remains a required frozen diagnostic. UQA-09b2 notes and UQA-09c abort/partial lifecycle stay separate; use read-only mapping concurrently when independent. The owned RPC driver is only a reference. Historical lanes and `lib/native-fullscreen-interaction.ts` remain protected; Engram mirror is unavailable until its local provider resolves.
