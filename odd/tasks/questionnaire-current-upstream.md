# Current-upstream questionnaire delivery

## Active UM-01 adaptation — current-main parent 2b864a37

Branch `fix/questionnaire-current-main-explicit-submit` starts at reviewed UM-00 close `2b864a37e5b136cf9ddc6de599b9ffa8817a30e9`, not historical `4cc610b1`. Hosted run `36260119352` at that exact parent passed frozen Pi 0.87.1 acquisition, strict TS/type ratchet, ask/view 49/49, selected Herdr 3/3 and clean checkout. Historical `4cc610b1..ae679b0c` is design/test evidence only: current view/ask test blobs match that old baseline, but other current-main blobs (including `tests/gentle-ai.test.ts`) do not. No blind cherry-pick or Pi 0.85.1 assumption.

Work units: (1) behavioral RED with seven named `UM-01:` view tests (including stale MULTI from `6a9ddb41`) and exact-SHA workflow gate: baseline ask/view 49/49 with UM-01 skipped, Herdr 3/3, isolated seven expected assertion failures with no passes; pin changed test and unchanged producer/view to current parent. **Stop before production writes until hosted RED is proven.** (2) GREEN view + compatibility fixtures, preserve the seven RED tests, pin test/producer/view identities, require all ask/view and isolated UM-01 plus Herdr green. Keep authored additions+deletions against `2b864a37` <=400, including this record and workflow; otherwise stop. Runtime harness is the pinned hosted offline container only; local project execution forbidden. Rollback RED to UM-00 by removing only new tests, workflow gate and this active section; rollback GREEN to accepted RED by reverting only view/compatibility/gate changes. Neither rollback touches RPC, Herdr or upstream CI. No PR, merge, main push, activation or package publication is authorized.

## Active UM-00 refresh — 06c9915c baseline

This section supersedes the historical 7f78c36d branch/base/next-step directions below, **not** their recorded CI claims. Active branch: `ci/questionnaire-current-main-baseline` in its dedicated worktree, based on verified canonical `Gentleman-Programming/gentle-pi` main `06c9915cdb247c99c25e5baa3d4148a97d362b6c` (local `upstream/main`, hosted main API, and `git ls-remote` agreed before the transplant). Only the five UM-00 commits from `7f78c36d..4cc610b1` were cherry-picked; no UM-01+ source or test changes. Allowed diff: this workflow and this task file only; keep total additions+deletions against 06c <=400. No PR, activation, merge, auto-merge, main push, or merge API is authorized.

Git-static comparison from 7f78c36d to 06c9915c found identical blobs for `extensions/ask-user-question.ts`, `lib/questionnaire/{questionnaire-view,schema,validate}.ts`, and `tests/{ask-user-question,questionnaire-schema,questionnaire-view}.test.ts`. Other files **did** change: `package.json` and `pnpm-lock.yaml` now select Pi 0.87.1 (rather than 0.85.1), while `packageManager` remains pnpm 11.1.1; the current `pnpm-workspace.yaml` includes release-age/trust policy. This refresh pins all source, lockfile, workspace and type-gate inputs to 06c; the workflow checks package-manager identity, frozen no-script acquisition, scoped strict TypeScript, type ratchet, focused TAP, exact event SHA and clean workspace. Any dependency, type, acquisition or TAP failure is a blocked baseline, **not** behavioral RED and not GREEN. Earlier run `35940964600` at `9f381591` proves only the old 7f baseline, not this one. Current-main hosted proof is recorded below, bound to its exact SHA only. No local project execution is authorized. Runtime harness: N/A for this workflow-only baseline; hosted offline gates are the runtime boundary. Rollback: remove only this workflow and task record without touching source or upstream CI.

First current-main hosted run `36259150936` at exact SHA `3a65148632a291893db16d5e92c04daf09e40e5f` **failed acquisition before npm**: checkout/scope and pinned image passed, independent clone checked out, then exit 1 before install output; offline gates were skipped and final workspace check passed. Static inspection found the new package-manager grep used inner single quotes inside the outer single-quoted `bash -c` script, so the regex was not passed intact. Corrected to escaped double quotes; this is an acquisition/gate failure, never behavioral RED or GREEN. Follow-up hosted push run [`36259247728`](https://github.com/decode2/gentle-pi/actions/runs/36259247728) at exact SHA `d40d375325229d844f314ecb08ccda39d14951b6` **passed**: checkout/scope and pinned image identity, independent frozen pnpm 11.1.1 no-script acquisition, scoped strict TypeScript, type ratchet (188 diagnostics, no regressions), ask/view 49/49, selected Herdr 3/3, and final clean checkout. This is baseline GREEN for that SHA only; no UM-01 behavioral RED/GREEN, full upstream suite, package, host or human validation is claimed. A later documentation-only commit must be checked on its own exact SHA before treating the branch tip as hosted GREEN.

Current-main baseline `ac43b0714f6935ec5051eebfa158fe30927ab5bb` passed exact-SHA CI `36259379539` (strict types, ratchet, ask/view **49/49**, Herdr **3/3**, clean checkout); independent verifier ACCEPT for this bounded **211-line** workflow/docs-only slice. Native review `review-b7cee973d2742726` approved and was acknowledged/burned for that exact candidate; R3-001 is informational. Next: adapt UM-01 on a separate dependent feature branch with fresh behavioral RED/GREEN evidence under Pi 0.87.1. The baseline GREEN does not certify UM-01, full-suite or host behavior. If assumptions or hosted checks fail, stop and report; do not mark GREEN.

## Objective and authority

Prepare reviewable, merge-ready questionnaire changes against `Gentleman-Programming/gentle-pi` current main; **do not merge, enable auto-merge, push main, or invoke a merge API** without fresh explicit approval for that repository and action. User chose stacked PRs toward main. New work begins from upstream main `7f78c36d4b50070008557a79b8734dd37222c569`; preserve historical fork branch `test/questionnaire-visual-polish-5594c1f4` and its frozen hosted evidence, but never retarget it or copy its patches blindly. Verify current main before each PR slice. First-party TUI and interactive RPC already landed in upstream PRs #1274 and #1328, respectively.

Approved issue #833 covers opt-in TUI-first behavior, explicit Next/Submit, multiline editing, viewport/pointer reachability, partial cancellation and owner/profile control; it explicitly hides note controls and defers rich RPC activation. Notes visibility and further RPC lifecycle work need their own approved issue coverage before opening PRs. A partial PR must not claim to close #833. Existing upstream trackers #1348 (implicit completion), #1340 (preview bounds), #1356 (notes) and #1277 (provider collision) are evidence, not substitute approval. PR issue-first requirements and size exceptions remain gates.

## Constraints and route

- User constraint: **no local project Node/npm/pnpm/SDK/compiler/parser/test/build/formatter/Docker/native execution or installs**. Git-only static read/apply/hash is allowed. Project execution must be exact-SHA hosted CI; RED must fail solely for intended behavior before GREEN. No retroactive certification.
- TDD mode: on, retained from upstream-questionnaire-adaptation task. Runner: pinned hosted Node 24, strict TypeScript and type ratchet, focused `node --experimental-strip-types --test --test-reporter=tap` (with exact selected test names), then applicable upstream CI/package checks. Separate host/runtime/human gates are not interchangeable.
- Route: one writer in this dedicated worktree for each bounded multi-file unit; read-only scouts and auditors may run in parallel. Parent reconciles evidence. Each unit has a Conventional Commit with focused checks, runtime-harness result or explicit N/A, rollback boundary, and exact reviewed range. Aim for <=400 authored additions+deletions **including** workflow/tracking; split cohesive work rather than compress code. Current branch is `fix/questionnaire-explicit-submit`.
- Delivery: stacked-to-main, selected by user. One honest slicing pass after implementation; no PR until a correctly approved issue covers that slice and current-main diff/CI are clean. No branch or PR delivery itself authorizes a merge. Forecast: multiple independent TUI units plus RPC/notes/lifecycle; accumulated work exceeds 400 lines and requires slices. Count measured diffs before commits.
- Engram mirror key `odd/questionnaire-current-upstream/tasks` is **pending**: local Engram provider could not resolve its server identity. This file is the recoverable current record.

## Tasks

- [x] **UM-00 — Establish exact-SHA hosted RED/GREEN lane.** Baseline workflow authored at `.github/workflows/questionnaire-current-upstream.yml`; first hosted CI `35938607836` on SHA `61b18876` failed before dependency install: Git clone rejected `/workspace/.git` as dubious ownership (the prior exact `/workspace` exception alone was insufficient). This is an acquisition/environment failure, **not** behavioral RED. Second hosted CI `35938945199` failed identically: parent `git -c` exceptions did not satisfy the local `upload-pack` child. Read-only incident diagnosis recommended artifact-local global Git config (`/artifact/home/gitconfig`) with only `/workspace` and `/workspace/.git` exceptions, visible to the child, while keeping source mount read-only and checkout SHA/clean checks. Third hosted CI `35940964600` at exact SHA `9f381591f958b66bda347d83c06ac48f50fb8b8a` passed acquisition with artifact-local exact Git trust, strict TypeScript and type ratchet (195 recorded baseline diagnostics, no regressions), ask/view 49/49, selected Herdr 3/3 and clean checkout. First two pre-TAP failures remain recorded; neither was behavioral RED. UM-00 is complete; rollback removes only this workflow and task tracking, not the existing upstream CI. It accepts only a push to `fix/questionnaire-explicit-submit` on the designated `decode2/gentle-pi` fork or upstream repository, or a PR from that branch in either repository to main; GitHub may not schedule an upstream `pull_request` workflow until the workflow exists on the PR's base branch, so the fork push is the initial hosted lane. Exact event `GITHUB_SHA` is checked out without persisted credentials; a PR evaluates its merge SHA, not a claimed head-SHA RED. Compare with pinned base `7f78c36d4b50070008557a79b8734dd37222c569`: only this workflow and task doc may differ, and questionnaire, tests, compiler/lockfile inputs must retain their base blobs. Networked acquisition clones the exact checkout into an independent artifact, installs pnpm 11.1.1 and frozen lockfile dependencies without lifecycle scripts; pinned Node 24 image ID is checked before both containers. Offline network-none/read-only UID 1000 execution runs scoped strict TS, the repository type ratchet, full ask/view TAP and selected Herdr TAP; require nonzero tests and zero failures, print actual counts rather than assuming any. Initial and final workspace cleanliness are checked. This is a baseline lane only: later behavior slices must intentionally revise its pinned source/scope policy before they can pass. Verification: **N/A hosted** until an exact-SHA Actions run is recorded; no local project runtime authorized. Rollback: remove only the new workflow and this UM-00 tracking update; no source/CI changes.
- [ ] **UM-01 — Explicit TUI Next/Submit.** In progress after UM-00 hosted GREEN. First RED unit forecast <=400 physical changed lines: six new view tests in `tests/questionnaire-view.test.ts`, branch-scoped workflow gated only for exact named `UM-01:` failures while preserving the already-verified baseline tests, plus tracking. Expected from read-only map: view 26 top-level / 20 skipped / six named failures; this is a forecast, not evidence. The workflow source policy must pin the new test blob and unchanged producer/view. GREEN may require a separate test-compatibility unit because existing upstream tests assert automatic completion. Current `QuestionnaireView.afterCommit()` auto-finishes and an empty MULTI Enter does not submit. First author focused behavior tests that fail on current main for visible pointer/keyboard Next and Submit, editable answers until explicit submission, empty MULTI Submit, custom-plus-selected and Cancel distinction. Then implement only that behavior without importing frozen older view wholesale. Route: delegated writer (view/test), separate hosted RED and GREEN, each <=400 lines. Verify focused ask/view + Herdr compatibility, strict types, full upstream CI at GREEN. Rollback: only new view/test change.
- [ ] **UM-02 — Multiline editor, binding and draft preservation.** Adopt public Editor semantics and documented tab/newline normalization, keep caret/scroll visibility and custom-plus-MULTI drafts. Separate bounded RED/GREEN and issue #833 acceptance. Do not claim external-editor delivery; the deferred host-owned contract needs separate Pi authorization.
- [ ] **UM-03 — Bounded viewport, preview and wheel routing.** Preserve visible actions in narrow/short terminals, stale-hit rejection, resize geometry and scroll ownership. Coordinate with open preview PR #1385 rather than duplicate it. Component fall-through is not host transcript movement; prove both separately.
- [ ] **UM-04 — TUI partial cancellation and abort.** Preserve committed answers, cancel active view on pre/mid abort exactly once and remove listeners/components. Answerless cancellation remains backward compatible. Hosted RED/GREEN, no RPC lifecycle claim.
- [ ] **UM-05 — Profile owner opt-in and collision handling.** Implement only after checking current upstream launcher/package ownership; #833 requires profile-scoped opt-in and refusal on detectable incumbent, while unconditional current registration conflicts with external providers. Protect unrelated settings and separate `ask_user_choice` authority.
- [ ] **UM-06 — Current-main host and packed lifecycle.** Verify actual package registration once, reload/cleanup, questionnaire-specific packed startup, mouse/wheel host routing, and clean descendants in isolated hosted runs. Human Herder/TUI acceptance and reversible migration need separate explicit authorization; no normal-profile activation by default.
- [ ] **UM-07 — RPC and notes follow-up under approved scope.** Current upstream #1328 already supports select and MULTI toggle loops; compare remaining custom/draft/empty Submit, partial/abort and notes. Issue #833 excludes visible notes and defers rich RPC. Do not add those to a #833 PR without separate approved issue(s). Prior UQA-056 results are frozen-derived component evidence, not proof for current main.
- [ ] **UM-08 — Reviewable PR delivery.** Measure one honest set of coherent stacked-to-main slices, ensure each has accurate approved issue coverage, clean immediate-parent diff, required checks, docs/tests, rollback and `type:*` label as applicable. Do not close #833 prematurely or merge anything.

## Evidence and next step

Read-only source comparison at upstream main `7f78c36d` found missing explicit submission, multiline Editor, bounded viewport, partial abort and owner opt-in; current upstream RPC SELECT/MULTI basic paths are already implemented. Three independent maps compared source/tests, issue scope and host lifecycle; none ran project code. The stale fork branch differs from its `origin/main` by 427 files and is not a delivery base. Next: UM-01 hosted behavioral RED on current upstream source; keep UM-00 SHA/run/count evidence. Then implement only the observed defects in bounded GREEN units and run applicable upstream CI; one writer at a time. Checks pending: all new hosted and human gates. No PR or merge performed.
