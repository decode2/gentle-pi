# Current-upstream questionnaire delivery

## Objective and authority

Prepare reviewable, merge-ready questionnaire changes against `Gentleman-Programming/gentle-pi` current main; **do not merge, enable auto-merge, push main, or invoke a merge API** without fresh explicit approval for that repository and action. User chose stacked PRs toward main. New work begins from upstream main `7f78c36d4b50070008557a79b8734dd37222c569`; preserve historical fork branch `test/questionnaire-visual-polish-5594c1f4` and its frozen hosted evidence, but never retarget it or copy its patches blindly. Verify current main before each PR slice. First-party TUI and interactive RPC already landed in upstream PRs #1274 and #1328, respectively.

Approved issue #833 covers opt-in TUI-first behavior, explicit Next/Submit, multiline editing, viewport/pointer reachability, partial cancellation and owner/profile control; it explicitly hides note controls and defers rich RPC activation. Notes visibility and further RPC lifecycle work need their own approved issue coverage before opening PRs. A partial PR must not claim to close #833. Existing upstream trackers #1348 (implicit completion), #1340 (preview bounds), #1356 (notes) and #1277 (provider collision) are evidence, not substitute approval. PR issue-first requirements and size exceptions remain gates.

## Constraints and route

- User constraint: **no local project Node/npm/pnpm/SDK/compiler/parser/test/build/formatter/Docker/native execution or installs**. Git-only static read/apply/hash is allowed. Project execution must be exact-SHA hosted CI; RED must fail solely for intended behavior before GREEN. No retroactive certification.
- TDD mode: on, retained from upstream-questionnaire-adaptation task. Runner: pinned hosted Node 24, strict TypeScript and type ratchet, focused `node --experimental-strip-types --test --test-reporter=tap` (with exact selected test names), then applicable upstream CI/package checks. Separate host/runtime/human gates are not interchangeable.
- Route: one writer in this dedicated worktree for each bounded multi-file unit; read-only scouts and auditors may run in parallel. Parent reconciles evidence. Each unit has a Conventional Commit with focused checks, runtime-harness result or explicit N/A, rollback boundary, and exact reviewed range. Aim for <=400 authored additions+deletions **including** workflow/tracking; split cohesive work rather than compress code. Current UM-04a RED branch is `fix/questionnaire-partial-cancel`.
- Delivery: stacked-to-main, selected by user. One honest slicing pass after implementation; no PR until a correctly approved issue covers that slice and current-main diff/CI are clean. No branch or PR delivery itself authorizes a merge. Forecast: multiple independent TUI units plus RPC/notes/lifecycle; accumulated work exceeds 400 lines and requires slices. Count measured diffs before commits.
- Engram mirror key `odd/questionnaire-current-upstream/tasks` is **pending**: local Engram provider could not resolve its server identity. This file is the recoverable current record.

## Tasks

- [x] **UM-00 — Establish exact-SHA hosted RED/GREEN lane.** Baseline workflow authored at `.github/workflows/questionnaire-current-upstream.yml`; first hosted CI `35938607836` on SHA `61b18876` failed before dependency install: Git clone rejected `/workspace/.git` as dubious ownership (the prior exact `/workspace` exception alone was insufficient). This is an acquisition/environment failure, **not** behavioral RED. Second hosted CI `35938945199` failed identically: parent `git -c` exceptions did not satisfy the local `upload-pack` child. Read-only incident diagnosis recommended artifact-local global Git config (`/artifact/home/gitconfig`) with only `/workspace` and `/workspace/.git` exceptions, visible to the child, while keeping source mount read-only and checkout SHA/clean checks. Third hosted CI `35940964600` at exact SHA `9f381591f958b66bda347d83c06ac48f50fb8b8a` passed acquisition with artifact-local exact Git trust, strict TypeScript and type ratchet (195 recorded baseline diagnostics, no regressions), ask/view 49/49, selected Herdr 3/3 and clean checkout. First two pre-TAP failures remain recorded; neither was behavioral RED. UM-00 is complete; rollback removes only this workflow and task tracking, not the existing upstream CI. It accepts only a push to `fix/questionnaire-explicit-submit` on the designated `decode2/gentle-pi` fork or upstream repository, or a PR from that branch in either repository to main; GitHub may not schedule an upstream `pull_request` workflow until the workflow exists on the PR's base branch, so the fork push is the initial hosted lane. Exact event `GITHUB_SHA` is checked out without persisted credentials; a PR evaluates its merge SHA, not a claimed head-SHA RED. Compare with pinned base `7f78c36d4b50070008557a79b8734dd37222c569`: only this workflow and task doc may differ, and questionnaire, tests, compiler/lockfile inputs must retain their base blobs. Networked acquisition clones the exact checkout into an independent artifact, installs pnpm 11.1.1 and frozen lockfile dependencies without lifecycle scripts; pinned Node 24 image ID is checked before both containers. Offline network-none/read-only UID 1000 execution runs scoped strict TS, the repository type ratchet, full ask/view TAP and selected Herdr TAP; require nonzero tests and zero failures, print actual counts rather than assuming any. Initial and final workspace cleanliness are checked. This is a baseline lane only: later behavior slices must intentionally revise its pinned source/scope policy before they can pass. Verification: hosted CI `35940964600` GREEN at exact `9f381591`; no local project runtime authorized. Rollback: remove only the new workflow and this UM-00 tracking update; no source/CI changes.
- [x] **UM-01 — Explicit TUI Next/Submit.** RED accepted at exact SHA `77355955abb7ac08ed64fccade80d34221faeb2b`, CI `35946519778`: strict types/ratchet, baseline **49/49**, Herdr **3/3**, isolated **0/6** with six exact named assertion diagnostics and clean checkout. GREEN implementation at `39eae5d8` failed hosted CI `35947470138`: strict TS/ratchet passed, but ask/view had 55 tests, 49 pass and six fail (five older ask scripts still expect answer-time completion; the original pointer span case clicked beyond padded Submit text). This is not full GREEN. RED design: six deterministic top-level `UM-01:` tests in `tests/questionnaire-view.test.ts` cover keyboard Next/final Submit without answer-time callback, editability until Submit, empty optional MULTI Submit, custom-plus-selected draft retention, pointer Submit hit span, and visible Cancel distinct from Submit. Exact names are in the diff and pinned workflow. Test blob before `9ea1ebae3d9a42d3e1281352adb10c337ec9846a`, after `5def3e34bfcc01b3c23788862a841bcb06e5baf9`. Measured RED commit diff: test +103/-0, workflow +49/-11, tracking +2/-2 = 167 authored changed lines (<400); `git diff --check` passed. `.github/workflows/questionnaire-current-upstream.yml` admits only these three changed paths (including this tracking file), pins the exact new test blob, and compares all other critical source blobs with base; Hosted CI `35944144269` at `b36151aa` passed strict TS/type ratchet, then ran all 55 tests (49 pass / six named UM-01 failures) because negative name lookahead also matched Node's `<root>` ancestor. The baseline gate stopped before Herdr and isolated RED, so this was **not** a completed behavioral RED. Correct baseline to `--test-skip-pattern="^UM-01:"` and require 49/49 with zero skipped; isolated RED must return nonzero with complete TAP `1..6`, six named failures, zero passes/skips and all six assertion diagnostics (two appear after `error: |-`). Herdr 3/3 must pass between baseline and RED. CI `35945509877` at `2df92e70` proved strict types, baseline **49/49**, Herdr **3/3**, and six named UM-01 assertion failures; however the RED command included the `ask` test file with no matching tests and Node reported a synthetic extra pass (`7 tests / 1 pass / 6 fail`), so the strict RED gate failed. The final RED isolates `questionnaire-view.test.ts` and accepts only six failures / zero passes, with complete TAP and exact diagnostics in hosted CI `35946519778`. No TypeScript, setup or pre-TAP failure counts as RED. The prior view auto-finishes from `afterCommit()` and empty MULTI Enter is a no-op. The GREEN work unit keeps the six RED cases unchanged, records answers separately from Next/Submit, offers optional empty MULTI Submit and a separately hittable Cancel, and updates only older auto-completion expectations. The workflow now pins both test and view blobs, admits only these four files against base, and at `39eae5d8` required strict types/ratchet, all 55 ask/view tests, Herdr 3/3 and isolated UM-01 6/6 in the offline hosted container; the full-suite gate failed before Herdr/isolated execution, not a GREEN result. No production changes, local project execution or PR in the RED unit; committed/pushed work units `b36151aa`, `2df92e70` and `77355955` to the feature branch only. Runtime harness for GREEN: pinned hosted Node 24 test container, CI `35947470138` failed its full-suite gate. GREEN rollback: revert only view, view-test compatibility changes, GREEN workflow gate/pins and this GREEN tracking update to accepted RED; the RED tests and baseline remain. RED rollback: remove only the six new tests and restore this workflow's UM-00 baseline scope/gates and this paragraph, leaving producer/view/helper and upstream CI untouched.
  **UM-01 stale MULTI correction RED/GREEN accepted:** Exact commit `6a9ddb410f621ffecb1c3fe12ad7b28e653530d0`, hosted CI `35949065970` passed strict TS/ratchet and isolated stale test **0/1** with the exact stale-selection assertion. This seventh top-level `UM-01: pointer Submit delivers latest MULTI selection after commit and custom edit` tests both normal and custom MULTI after committed toggles; the original six RED cases remain unchanged. GREEN correction (this work unit): cap action pointer ownership at visible label cells even when `Text.render` pads lines; synchronize committed MULTI selections on every toggle, retaining `selected: []` for empty MULTI and omitting `selected` for empty custom. Update five legacy TUI ask fixtures to explicitly Submit and assert answer-time entry does not deliver; the dock-swap fixture also submits explicitly. Exact-SHA hosted workflow pins test blob `ecc198bb9476a4471f06f030de16a0d739c4e540` and view blob `683dc02041e2deda9d1e7f82d57cc6d8d688a21a`, permits the ask test fixture while guarding all other critical base blobs; requires strict TS/ratchet, full ask/view **56/56**, Herdr **3/3**, and seven exactly named isolated UM-01 **7/7**. GREEN commit `e7baa426e7a97bcf3954a5958a035f78f0a700c5` passed exact-SHA hosted CI `35949583912`: strict TS/ratchet, full ask/view **56/56**, Herdr **3/3**, isolated UM-01 **7/7**, identity pins and clean checkout. Independent verification ACCEPT; reviewed slice `31729ce5..e7baa426` changed 264 lines. Native review `review-f6be9060e02c395e` approved all four lenses, was acknowledged/burned for that exact range; nonblocking `R4-001` at `questionnaire-view.ts:608–611` remains a later follow-up, not a correction route. No local project runtime, PR or merge in this work unit. Rollback: revert only this correction's view changes, ask fixture changes, GREEN workflow restoration and tracking update to accepted `6a9ddb41` RED; leave all seven RED cases intact.
- [x] **UM-02 — Multiline editor, binding and draft preservation.** Hosted behavioral RED accepted at exact `ae1e77ec380c7d9aad81c4e6a74ddab55a85c70f`, CI `35955180713`: strict TS/type ratchet, baseline **56/56**, Herdr **3/3**, UM-01 **7/7**, isolated UM-02 **1 pass / 4 exact assertion failures** (large paste invariant passed). The five original UM-02 tests remain unchanged. GREEN candidate replaces custom Input with Pi v0.85.1 public Editor supplied the real `ctx.ui.custom` TUI and a theme adapter; one Editor per question retains caret, text and paste registry across Tab/Esc/reopen. Bracketed paste is routed before navigation, newline before submit (including binding collision), and custom Enter reads raw expanded text before Editor's trimming/clearing submit path. UM-01 Next/Submit remains explicit. Only test harness TUI fakes changed, not the original five UM-02 tests. Exact-SHA candidate `bb0abec6` stopped in hosted CI `35956224869` **before TAP** at strict TS2416 (`questionnaire-view.ts:228`): `Editor.handleMouse` returns `TuiMouseEventResult` without `Container`'s required `TuiMouseDispatchResult.target`; this is neither behavioral RED nor GREEN. Correction in this work unit forwards only editor-owned rendered rows with Editor-local y, returns this container's mouse target for handled editor events, and leaves unrelated header/blank clicks and unhandled wheel/drag to fall through. A separate pointer-caret view test was added without changing the original five RED cases. Candidate `60e6f262` CI `35957587481` passed strict TS/ratchet but full ask/view was **60/62**: two existing TUI custom-answer tests supplied a legacy keybindings object without `matches`, and the new `matchesNewline`/`matchesSubmit` method calls threw. This is behavioral failure, not GREEN. The subsequent mechanical correction optional-calls `matches` and falls back to public key recognition; the two original integration tests remain the regression. Workflow pins corrected view/test plus unchanged ask producer/test; other critical blobs remain frozen. Corrected GREEN commit `7a92619ef8e19a5bb5fa7fc38b8e164cd9abc450` passed exact-SHA hosted CI `35957872492`: strict types/ratchet, full ask/view **62/62**, Herdr **3/3**, UM-01 **7/7**, UM-02 **5/5**, exact blob pins and clean checkout. Independent read-only verification ACCEPT; the `ae679b0c..7a92619e` dependent slice is **395 changed lines**. Native review `review-ff6c1283a61d97a8` approved all four lenses and was acknowledged/burned for exactly this range. No local project runtime, external-editor delivery or human TUI acceptance; the hosted run is the runtime harness. Rollback GREEN to accepted RED `ae1e77ec`: restore only view, ask producer, two TUI test fake adjustments, workflow GREEN gates and this paragraph; retain all five UM-02 RED cases. RED rollback is separate: remove only the five UM-02 tests and restore UM-01 gates, leaving UM-01 source and tests untouched.
- [x] **UM-03a — Short/narrow body and action reachability.** At 40 columns × 20 rows, reserve two native `DynamicBorder` rows (18 for the view); this is a component minimum, not a whole-host guarantee. Original RED `55324a9a` / CI `35960611088`: strict types/ratchet, baseline 62/62, Herdr 3/3, UM-01 7/7, UM-02 5/5, original UM-03a 0/2 with named assertions. Editor RED `f7173570` / CI `35962328409`: same baseline, UM-03a 2/2, editor 0/1 on draft/caret visibility. Prior GREEN `28bdb6c0` / CI `35963042349` passed 65/65, but independent P1 found long split preview clipped when the short left body has no scroll overflow. Its native review `review-5f6c891b1a26f40b` approved/burned only that old candidate. Accepted preview RED `8b75eced` / CI `35964388074`: frozen view `154e9c81`, prior 65/65 and isolated preview 0/1 with exact owned-wheel assertion. First fallback `616a459a` / CI `36009648810` failed full ask/view **65/66** because full-width inline wrapping exposed the tail before scrolling; downstream gates did not run. Corrected GREEN `bb718041afc50c6257fae9018ce2105e15468853` / CI `36010393858` passed strict types/ratchet, full ask/view **66/66**, Herdr **3/3**, UM-01 **7/7**, UM-02 **5/5**, original UM-03a **2/2**, editor **1/1**, preview **1/1**, exact blob pins and clean checkout. The four UM-03a/editor/preview tests remained unchanged through their respective GREENs. Oversized split previews fall back into an owned scrollable body with a readable 40-column preview; normal short split and narrow inline previews remain distinct. Independent final read-only verification ACCEPT; `767d1705..bb718041` is **353 changed lines**, within 400. Native review `review-3e5c1d1c11a4e010` approved all four lenses and was acknowledged/burned for exactly this final range; `R3-001` and `R4-001` are informational warnings at view lines 254–258, not an open correction. No local project execution, host transcript motion proof, packed startup, human TUI acceptance, PR or merge. GREEN rollback restores only view/workflow/tracking to accepted preview RED `8b75eced`, retaining all four UM-03a/editor/preview tests; original RED rollback separately removes the original UM-03a cases and restores UM-02 gates, leaving UM-02 source/tests intact.
- [ ] **UM-03b — Preview and wheel ownership.** Coordinate with still-open PR #1385, whose older Input/implicit-completion patch overlaps the view; adapt rather than transplant after comparing its landing state. Add separate hosted RED/GREEN for bounded split/inline preview, regional wheel ownership and stale hit maps, without treating component fall-through as host transcript motion. Actual transcript movement remains a separate UM-06 host gate.
- [x] **UM-04a — TUI partial cancellation.** Certified behavioral RED `80bd1efb7b31c2c488340eca8f50c5cb57d1e769` / hosted CI `36015997150`: strict types/ratchet and prior 66/66 passed, isolated four named assertion failures (0/4). The original four tests remained unchanged (blob `bff4677daa035e18d83b1c7a15f2663def2ce136`). Initial GREEN `ef2ade7c71bc5833c0e0582881488b60c8dc357e` / CI `36019526190` and `36019526864` passed exact SHA, strict types/ratchet, full ask/view 70/70, Herdr 3/3, prior focused gates and UM-04a 4/4. Native review found the focused gate depended on filtered test totals/skips, despite hosted evidence showing the gate passed; bounded four-diff-line correction removes that portability dependency without weakening four named pass assertions or changing production/tests. Final GREEN `7d914acd00281a43187c00ddd9a9824821877aba` / hosted CI `36032674963` passed exact SHA and the same gates, including UM-04a 4/4, with a clean checkout. The `cfbb9c0f..7d914acd` review slice is 132 changed lines (<400), independently accepted, and native review `review-d9e75ac3da8d71b2` approved after targeted validation and was acknowledged/burned for exactly this corrected candidate. TUI producer delivers only committed rows on cancellation in numbered Partial answers text and details; answerless text/details and RPC are preserved. Runtime harness: hosted offline pinned Node 24 lane; no local project execution, real-host acceptance, PR or merge. GREEN rollback: restore only producer, workflow and this entry to accepted RED `80bd1efb`, retaining four RED tests and UM-03a. Full unit rollback to `cfbb9c0f` is separate.
- [x] **UM-04b — One-shot TUI abort lifecycle.** Certified behavioral RED `a756ed5a9167da65240bddfaea0c47627b1029af` / hosted CI `36036630493`: strict types/ratchet, baseline 70/70, Herdr 3/3, prior UM-01/02/03a/04a gates and isolated five exact `ERR_ASSERTION` failures; accepted ask/view test blobs `86bd0fe46849888ca264c982743dbf932eda37c4` and `449e88dc9ff33958887e19f62dab18ca247daf57` remained frozen. CI `36035696442` failed shell quoting before RED and is not behavioral evidence. Final GREEN `2b647eea1a822e649c8bff1ca041b928cbffb5e6` / hosted CI `36039632886` passed exact SHA/clean checkout, strict types/ratchet, full ask/view **75/75**, Herdr **3/3**, prior focused gates and UM-04b **5/5**. The dependent `7fb49b4f..2b647eea` slice is **234 changed lines** (<400), independently accepted. Native four-lens review `review-23ccc8e39ed778f6` approved and was acknowledged/burned for this exact candidate; informational warnings R2-001 and R3-001 are later follow-ups, not an open correction. TUI pre-abort returns answerless cancellation without mount; active abort invokes public view cancel through one-shot completion with committed rows only; listener cleanup runs on completion and finally across resolution/undefined/rejection. RPC path and native helper remain unchanged. Runtime harness is hosted offline pinned Node 24; no local project execution, real-host disposal proof, human TUI acceptance, PR or merge. GREEN rollback: restore only producer/view, workflow and this entry to accepted RED `a756ed5a`, retaining five RED tests; full UM-04b rollback separately removes the tests and restores UM-04a GREEN workflow.
- [x] **UM-05a — Profile owner TUI registration RED.** Certified behavioral RED `4cd5f078cf6a192e450e164946ec18f426cfb503` / CI `36052050706`: strict types/ratchet, prior ask/view **75/75**, Herdr **3/3**, prior focused gates and four named `ERR_ASSERTION` failures (**0/4**). Four original RED blocks are frozen. Ownership is `resolveGentlePiAgentHome()/gentle-ai/question-owner.json`, not the config store. RED rollback removes only its new ask tests, workflow gating and this entry; it does not change source or choice.
- [x] **UM-05a GREEN — Profile owner registration candidate.** Read only the exact version-1 `gentle-pi` enabled owner document from the resolved Pi agent profile; missing, malformed, unreadable, disabled, legacy and extraneous-key documents fail closed. At TUI `session_start`, refuse a detectable incumbent (active or inactive), or an unavailable/throwing tool inventory; do not register in RPC, even on an interactive host. Existing TUI-registered fake execution retains the old RPC fallback. Pi v0.85.1 has no atomic reservation against later dynamic registrations: do not claim exclusive ownership. The older registration fake seeds a temporary Pi agent home; two stale exclusivity tests now check empty-inventory registration and non-displacement. No real profile, `ask_user_choice`, view or view tests changed. Exact-SHA GREEN `4ff3217137f2f5ab78fd92a74f010a6faa6b74da` / hosted CI `36072557878` passed strict types/ratchet, full ask/view **79/79**, Herdr **3/3**, prior focused gates and UM-05a **4/4**, plus clean checkout. The dependent `b109311d..4ff32171` slice is **313 lines** (<400), independently statically ACCEPTed. Native review `review-cba941455066e0ca` approved and was acknowledged/burned for that exact candidate. No local project runtime, PR, merge, real-host proof or package activation; a symlinked owner document and post-registration revocation are not covered. GREEN rollback restores only producer, legacy fixture/assertions, workflow and this entry to `4cd5f078`, retaining all four RED tests. Revocation after registration requires a separate hosted RED before claiming support.
- [ ] **UM-05b — Owner delivery and lifecycle follow-up.** Check launcher/package ownership and hosted host integration before delivery; handle revocation only behind its own RED. No PR or merge authorization implied.
- [ ] **UM-06a — Hosted TUI bootstrap (in progress).** In isolated `test/questionnaire-hosted-tui-bootstrap-um06a` from UM-05a closure `042359072c8acd675fa147d64308a30ea0d9f0ef`, prove only exact-SHA hosted Node 24 / frozen Pi 0.85.1 real interactive CLI PTY `ctx.mode=tui` session_start and explicit-path questionnaire discovery with an isolated temporary owner profile and trace extension. Keep regular profile and package activation untouched. Packed default discovery, reload, mouse/wheel, cleanup and human acceptance remain separate unless directly proven. Limit authored diff to 400; record CI SHA/run and honest host/tooling failures before closure. First run `82aa632c` / CI `36255019937` passed frozen baseline but failed pre-host: fixture `pty.py` shadowed Python's standard-library `pty`, causing circular import; renamed fixture `driver.py`. This is tooling failure, not host evidence. Rollback: remove only the new probe/fixtures, its workflow adaptation and this entry; no production behavior changes.
- [ ] **UM-06 — Current-main host and packed lifecycle.** Verify actual package registration once, reload/cleanup, questionnaire-specific packed startup, mouse/wheel host routing, and clean descendants in isolated hosted runs. Human Herder/TUI acceptance and reversible migration need separate explicit authorization; no normal-profile activation by default.
- [ ] **UM-07 — RPC and notes follow-up under approved scope.** Current upstream #1328 already supports select and MULTI toggle loops; compare remaining custom/draft/empty Submit, partial/abort and notes. Issue #833 excludes visible notes and defers rich RPC. Do not add those to a #833 PR without separate approved issue(s). Prior UQA-056 results are frozen-derived component evidence, not proof for current main.
- [ ] **UM-08 — Reviewable PR delivery.** Measure one honest set of coherent stacked-to-main slices, ensure each has accurate approved issue coverage, clean immediate-parent diff, required checks, docs/tests, rollback and `type:*` label as applicable. Do not close #833 prematurely or merge anything.

## Evidence and next step

Read-only source comparison at upstream main `7f78c36d` found missing explicit submission, multiline Editor, bounded viewport, partial abort and owner opt-in; current upstream RPC SELECT/MULTI basic paths are already implemented. Three independent maps compared source/tests, issue scope and host lifecycle; none ran project code. The stale fork branch differs from its `origin/main` by 427 files and is not a delivery base. Next: coordinate still-open preview PR #1385 before UM-03b; UM-04 partial cancellation/abort can be mapped independently on this reviewed UM-03a base. UM-01, UM-02 and UM-03a component/integration GREEN and native review are complete; host transcript movement, packed startup, human TUI acceptance, UM-03b–UM-08, and PR slicing remain pending. No PR or merge performed.
